# todoAI — Master Task Table

*Every task in numeric order, as of **2026-08-21** (task 14 §13 backup wiring landed — `docs/eval/task14_wiring_findings_report.md`; the first on-device run of merged `main` was 2026-08-19, `docs/eval/device_session_2026-08-19_findings.md`). Working tree clean, **973 tests / 86 suites green** (raw jest reports 1767/154 — subtract the stale worktree's fixed 794/68). The first-ever green was `fc59b83` on 2026-08-17 (794 tests); 37, 48, 14 Phase A + §13 wiring, 41 Phase 2 and 44 have landed since. Status reflects the evidence base in `docs/eval/`, not summaries of it.*

**This file is the single source for per-task status.** It moved into the repo on 2026-08-07; `orientation_for_opus.md` no longer carries a second copy of it. On conflict: **this table wins on status**; orientation wins on contracts, constraints, settled decisions, and ship gates; a per-task brief wins for its own task.

**`master_task_table.html` is generated — do not hand-edit it.** After changing this file run `node scripts/gen-task-table.js`. 🔴 **Enforced 2026-08-18:** `scripts/__tests__/gen-task-table.test.js` runs the generator's `--check` mode in the suite and **fails if the committed HTML is stale** — so a markdown edit that forgets to regenerate is caught by `npx jest`, not shipped. *(This exists because a stale HTML was committed in `e7e3340` when the generator crashed unnoticed; the guard would have caught it.)* The coordinator also **surfaces the rendered HTML in the chat after every board change**, per Jason's standing request.

**Status key:** ✅ **Done** · 🟡 **Partial** · ⬜ **Undone** · 🔴 **Undone + gate/live bug**
**Model key:** **You** (device/human judgment) · **Fable** *(access ended)* · **Opus** · **Sonnet** · **Jason/Human**
**`P`** in the Dep column = **requires on-device (Phase B) verification.** Desktop reasoning does not close these; a `P` task is not done until it has run on the S23 FE. Sequence headless work and device batches separately.

*(There is no task 16 — retired and split into 23 + 24. Q1 sits between 5 and 6, where it ran. **39 is reserved and unused** — held for an optional corpus→eval-harness step between 38 and 40. **41, 42 and 43 added 2026-08-07**: 41 gates 31 after the dogfooding DB turned out to hold no real captures; 42 pins the closed-beta rung; 43 pins the open-beta and GA rungs so the pruning schedule can't be lost; 44 collects four alpha QoL items requested alongside 41 and kept separate from it deliberately; 45 audits the deviation backlog after task 24's dial change reached production without Jason ever being asked. **46, 47, 48 added 2026-08-17** from unowned residue: 46 the "every N weeks" interval handoff orphaned since task 24, 47 the stale schema snapshot, 48 the grammar primitive task 37 left loaded.)*

> ### ⚠ Read this before trusting any ✅
>
> **"Done" and "merged" were different claims, and until 2026-08-07 this table only tracked the first.** Tasks 35 and 36 were marked ✅ with reports and test counts — all true — while their work sat on an unmerged branch. The *checked-out* branch had no migration 006, no `src/services/recurrence/`, and no v2.4 spec. Since 36's whole content was a fix for a **live scoring bug affecting every recurring task**, an alpha built during those four days would have shipped the bug with the record insisting it was fixed.
>
> **New rule: a ✅ row names its branch until that branch is on `main`.** All rows below are on `main` as of `fc59b83`.
>
> ✅ **`main` IS now test-verified — 2026-08-17.** `npx jest` **794 tests / 68 suites green**, `npx tsc --noEmit` **clean**, `npx eslint .` **0 errors** (56 warnings, all inline-style lint in `src/dev/` throwaway spike screens). Run on `main` at `fc59b83`, after merging `coordinator/sessions-origin`. The long-standing 🔴 is discharged.
>
> ⚠ **Read the raw jest number with care: it says 1588, and 1588 is wrong.** A stale git worktree at `.claude/worktrees/interesting-shirley-e10fa1` (detached at `d3ead86`) is a full second copy of the tree *inside the project*, and jest collects its 68 suites too — exactly doubling the count. **The real figure is 794**, confirmed by counting test files in each tree (68 and 68) and by `d3ead86` already being an ancestor of `main`. Anyone quoting "1588 tests green" is quoting a number spanning two different commits. See the standing note below.

---

| # | Status | Task | Description | Depends on | Model |
|---|---|---|---|---|---|
| 0 | ✅ Done | Core dependency spike | Prove Ternary Bonsai loads and runs in `llama.rn` on real hardware. Established the truths everything rests on: format is **TQ1_0** (mainline) / Q2_0 (fork-only), **not** Q1_0; CPU-only on Android; chat-template `messages` API mandatory; app-private model storage. *4B only — 8B/1.7B have no working quant (→ 29).* | **P** | **You** |
| 1 | ✅ Done | Dev env + toolchain | Bare RN 0.86, New Architecture, Windows host, device deploy. `README_build.md`. | 0, **P** | **You** |
| 2 | ✅ Done | Data layer | SQLite migrations + repositories returning domain types. Ran on hardware; `POWER()` confirmed absent, so neglect math stays in TypeScript. | 1, **P** | **Sonnet** |
| 3 | ✅ Done | TypeScript types | Row/domain/scales. The `Recurrence` discriminated union is authoritative; `scales.ts` is the only legal projection path. | 2 | **Sonnet** |
| 4 | ✅ Done | Structured-output strategy + eval design | The D-series strategy (D1 recap, D8 union, D9 greedy, D10 ladder). Later confirmed on-device by Q1. | 0, 1 | **Fable** |
| 5 | ✅ Done | Schemas, grammars, validators, mappers | GBNF artifacts + zod validators + pure mappers. Rule-name lint enforced. **Carries the live hole in task 37.** | 3, 4 | **Sonnet** |
| Q1 | ✅ Done | Grammar smoke-test arc | Three-session arc proving GBNF works on this build. Root cause was one lexer quirk: **no `_` in rule names**. GREEN at ~0% overhead — the largest de-risk after the load spike. | 1, 5, **P** | **You + Sonnet** |
| 6 | ✅ Done | Provider + D10 ladder + startup guard | `TernaryBonsaiProvider`, retry ladder, and the startup guard that compiles every grammar before any user session. Device-confirmed. | 1, 3, 5, Q1, **P** | **Opus** |
| 7 | ✅ Done | System prompts | Task-input + coaching prompt engineering. Tuned 10/16 → 15/16 on-device. Surfaced the crisis-detection finding that became task 21. | 4, 6, **P** | **Opus** |
| 8 | ⬛ **RETIRED 2026-08-07** | ~~Tiering ladder (§3.1)~~ | **Retired, not deferred — its mechanism was measured and doesn't work on this hardware.** Task 8 was "runtime health check + downward degradation across 8B/4B/1.7B." The premise is that dropping to a smaller model relieves thermal pressure. **The six-model spike measured that it does not:** *"The thermal envelope does not discriminate. All six reach at least SEVERE throttling by twenty minutes. It cannot be used to choose between them."* A 0.8B throttles at the same twenty minutes as the 4B, so degrading tiers when hot buys nothing. **Its blocker also dissolved:** 29 gated 8 on "does an 8B exist that runs" — **Bonsai-8B-Q1_0** (1.08 GiB, 8B params at the 4B's footprint) is loadable on stock and **enters task 40's bake-off**, which answers it empirically for free. **What survives is already owned:** spec §3.5's *"monitor thermal state, reduce context length, defer background work when hot"* is **deferral, not tier-switching**, and orientation §8 already assigns the thermal sampler to **task 19**; "real thermal management" remains its own general-ship gate. ⚠ **One condition that could revive it:** the thermal measurement is **one device**. If **task 30**'s envelope finds a device where tier degradation genuinely helps, reopen this — with the new measurement, not the old assumption. | ~~6, 29, P~~ | — |
| 9 | ✅ Done | Scoring (§5.1–5.2) | Factor scoring + neglect multiplier. Revised per R1–R3: linear `neglectCurve` seam, context/tools moved out to a hard pre-filter, 31/23/23/23 weights, transitive-fan-out subtask ordering, multi-hop DAG guard. ⚠ **`score.ts` was binary to git until 2026-08-01** — see the standing note; every prior review of this file read a `Bin` placeholder, not lines. | 2, 3 | **Opus** |
| 10 | ✅ Done | Scoring composition review | Fable's pass over the recombination. Forks 1/2/3/6 ruled leave-as-built; found **U1** (novelty shuffle randomizes ordered chains), **U2** (parent competes with its own subtasks), **U6** (far-future neglect accrual). ⚠ **Conducted against a `score.ts` git could not diff** — its conclusions are not invalidated, but they were never diff-reviewable. | 9 | **Fable** |
| 11 | ✅ Done | Session planning (§5.3) | `src/planning/` — sizing, agenda vocabulary, pure planner core, service edge. **`runSelectionBoundary` is the pool's only entry**: capability filter → dependency filter → *then* a ranker, both reject sets retained; R7's hold wired via `pendingBreakdownCompleteTaskIds`. `AgendaTaskItem.blockKind` (countdown/openBlock) closes 33's flagged breaking change to 24. **Decision (a) RULED: no LLM call — deterministic v1 plus a typed `PlanAdjustment` hook, so no `P`.** Novelty entropy is now a permanent regression test (slot-1 ≈ 1.92 bits). Report: `docs/eval/task11_findings_report.md`. | 9, 10, 25, 33 | **Opus** |
| 12 | ✅ Done | Coaching flows + resolution dispatch | Three triggers, grammar-constrained resolution union, app-side dispatch, deterministic crisis gate. Device-confirmed. **Residue:** `add_dependency`/`add_missing_task` dispatch unexercised on-device → task 32. | 6, 7, **P** | **Opus** |
| 13 | ✅ Done | Timer + episode lifecycle + crash recovery (§8.2) | ✅ **Crash recovery RE-CONFIRMED on merged `main` 2026-08-19** — force-killed a live episode, relaunch recovery derived the same episodeId (via task 41's capture log). `src/execution/` — timer engine, five-option end-of-block prompt, both extension paths, park primitive, crash recovery. **Migration 005 → schema 2.6.0.** **Force-kill confirmed on the S23 FE** by pulling the DB: credit bounded to the block end (2 min not 7), no skip written, no coaching queued, task stays `active`. 531 → 636 tests. Report: `docs/eval/task13_findings_report.md`. **Residue:** overnight-doze Phase B unrun; migration-number contention with 36 **resolved** (13 took 005, 36 took 006). | 6, 11, **P** | **Opus** |
| 14 | 🟡 **Phase A + §13 wiring DONE (desktop-verified) · Phase B is device** | Backup/restore + corruption recovery (§8.4) | ✅ **§13 WIRING LANDED 2026-08-21 (desktop-verified, committed).** The backup ladder is now invoked by the app: the recovery ladder runs at launch **before `getConnection`**, and the pre-session backup gate sits in **`createSessionRow`** (covering planned **and** quick-start), blocking **before** any `sessions` row is written. Two presentational surfaces added (`SessionBlockedScreen`, `RecoveryAckScreen` + a pure `recoveryAck` view-model), reusing task 24 patterns. **973/86 green, `tsc` clean, `eslint` unchanged.** **All 8 builder deviations RATIFIED by Jason 2026-08-21** — D1 (integrity **blocks + defers to the launch ladder** rather than inline recovery, which would strand repos on a deleted inode), D2 (all user-facing copy), D3–D8 (architecture). 🔴 **StatFs `availableBytesFor` STILL HAS ZERO CALLERS** — the gate is **attempt-and-catch** (`VACUUM INTO` → catch `SQLITE_FULL`), so this wiring did **not** wire StatFs; it stays exposed-but-unconsumed → task 32. **Phase B (device) still owed:** real `VACUUM INTO` backup at session start, the `no_space` identity via a genuine full disk, corruption→restore/salvage→ack screen, and the integrity-block relaunch round-trip. Report: `docs/eval/task14_wiring_findings_report.md` · Brief: `docs/briefs/task_14_wiring_brief.md`. *(Prior status follows, as history.)* 🔴 **CORRECTED 2026-08-19: Phase B cannot run on device yet — the backup ladder is invoked by NOTHING in the running app.** Verified: `grep` for `services/backup`/`createBackup`/`VACUUM INTO`/`salvage` across `src/app/` and `src/execution/` returns empty; the only reference anywhere outside the module's own tests is a *comment* in `capture/nativeWriter.ts`. So VACUUM-INTO-on-device, the no-space error-identity test, corruption/restore, and salvage **have no code path to reach them** — this is not gated on Jason approving a destructive test, it is gated on **task 14's §13 wiring** (the 3-call-site session gate + a "not enough space" surface) being built. That wiring was deliberately deferred while `src/app` was churning under 41/44; **`src/app` is now settled, so the wiring is unblocked** and is the next step before any Phase B. ✅ **Phase A landed** (`d340a0a`, `347aa25`, `0286acf`). `src/services/backup/` (11 files) + two test-only helpers in `src/db/testUtils/`. **No schema change, no migration — 007 left free for task 44.** Ladder runs against an injected `DbOperations` layer, exercised by a file-backed better-sqlite3 double with **real byte-level corruption** and an injectable full disk. **57 tests / 6 suites**; suite 811→**868 real tests / 75 suites**, `tsc` clean, `eslint` unchanged — all independently re-verified by the coordinator. **`VACUUM INTO` confirmed** (SQLite 3.51.3 bundled, no OMIT flags). **`backup_log`: USED, and load-bearing** — its row commits *before* the vacuum, so each snapshot carries its own creation record, which is how the two slots get dated when the working DB is unreadable; it also holds the rotation pointer, which has nowhere else to live. **§4(c) answered two ways deliberately:** restore **clears** the runtime tables (a snapshot's `active_episode` would be a phantom crash), salvage **keeps** them (that crash is real). **§4(d):** capture logs are **not** backed up. **§4(e):** product DB blocks, capture degrades — `reclaimSpace` is a typed hook 41/43 fill. ⚠ **Nothing is wired into `src/app/` — zero files touched there, deliberately:** blocking a session without a "not enough space" surface (task 24's screens) turns a full disk into a silent no-op. Report §13 has the exact 3-call-site recipe and notes **task 44 also edits `startSession`**. 🔴 **Two deviations + one decision owed — see Open rulings owed.** Report: `docs/eval/task14_findings_report.md`. ✅ **FREE-SPACE QUERY NOW EXISTS (2026-08-18, `7d4398b`)** — Jason ruled it in, and it was appended to task 41's `NativeCaptureLog` module so **one rebuild covers both**. `availableBytesFor(path)` — `Double`, the same codegen mapping `sizeOnDisk()` already uses; **`-1` native / `null` JS means "cannot tell" and is never conflated with `0`** (pinned by test, including that a genuine `0` survives as a number), because collapsing them would make a missing module look like a full disk inside a gate that blocks the user. 🔴 **It is PATH-SCOPED, and that is load-bearing, not tidiness:** `opSqliteOperations.ts:55–56` sets `backupLocation: ANDROID_EXTERNAL_FILES_PATH` but `salvageLocation: ANDROID_DATABASE_PATH` — **two different volumes.** They are usually the same partition on modern Android, but *usually* is a device-dependent fact, so a single unqualified free-space number would have been right for the backup caller and **silently wrong for the salvage caller.** ⚠ **Exposed only — `src/services/backup/` is untouched and the wiring is UNBUILT.** Task 14 is the consumer; do not assume the check is live. *(Original brief description follows.)* Pre-session copy, integrity check, salvage ladder, block-on-no-space. **Brief rewritten 2026-08-07 — the original approach was wrong for this tree.** 🔴 **Do not file-copy the DB:** (a) task 41's Phase 1 established there is **no way to write a file from JS** here — no RNFS, no FS TurboModule, op-sqlite gives paths but no write; and (b) **a byte-copy of a live SQLite DB is unsafe anyway** — with an open WAL it can capture a torn state, which would have been a latent integrity bug even with a filesystem API. **What exists is better:** op-sqlite's DB object has `execute`, `delete()`, `attach`/`detach`, `getDbPath`, `loadFile`, so the whole ladder is reachable at SQL level with **no new native module** — **`VACUUM INTO`** for an atomic consistent backup, `PRAGMA integrity_check`, `ATTACH` + `INSERT…SELECT` for salvage (which also answers *what salvage reads* — it skips views and steps past a corrupt table). ✅ **`VACUUM INTO` availability RESOLVED headlessly 2026-08-17 — it is a build fact, not a device fact.** The brief said to verify it on device and stop if absent. **op-sqlite 17.1.2 compiles its own bundled amalgamation** (`cpp/sqlite3.c`, wired at `android/CMakeLists.txt:79`) at **SQLite 3.51.3**, with no `SQLITE_OMIT_VACUUM`/`SQLITE_OMIT_ATTACH` flags — it does **not** link Android system SQLite, so the version does not vary by device or OS. 3.51.3 ≫ the 3.27 floor. Phase A can build on it with confidence; Phase B still watches it run, as habit rather than doubt. 🔴 **One gap:** nothing reports **free disk space** — attempt-and-catch `SQLITE_FULL`, `setReservedBytes`, or a small native call; bring the tradeoff back, since "block *before* starting" implies knowing in advance. ⚠ **`backup_log` (migration 001) has never been written to or read** — 14 is its only natural consumer; use it or retire it. **New decisions:** do capture logs get backed up (*recommend no* — they're ephemeral by settled decision, large, and compete for the space the DB needs; task 42's export owns getting them off-device), and the reciprocal of 41's rule that **capture degrades where the product DB blocks**. Brief: `docs/briefs/backup_recovery_task_14.md` | 2, **P** | **Opus** |
| 15 | ⬜ Undone | Edge cases / Safe Mode (§8.3) | Finish a session without AI; queue what would have coached; health check before new sessions. The failure modes it handles only occur on-device. Brief: `docs/briefs/safe_mode_task_15.md`. *Beta hardening.* | 6, 12, **P** | **Opus** → **Sonnet** for repetitive handlers |
| 17 | 🟡 **Phase A DONE 2026-08-22 · Phase B (the six loops) queued** | Numeric learning loops (§5.4) | ✅ **PHASE A — the historical-success signal is REAL.** The `completion_count`/`success_rate` writer that had **no writer anywhere** now exists (`recordHistoricalCompletion`), closing a live scoring gap on a 23% weight. 🔴 **The gap was ONE-SIDED, worse than inert** (coordinator-verified): `skip_count` has had a writer since task 13 while `completion_count` never did, so skips raised the evidence count `n` while `rate` stayed 0 — dragging tasks **below** the neutral 0.5 prior. Worked numbers: completed 2×/skipped 8× → 0.100→**0.250**; completed 20×/never skipped → 0.500→**0.9545** (+20.2% base score; it had been scoring *identically to a task never touched*). 🔴 **Attempt definition (PRODUCT-INTENT, PROVISIONAL — Jason rules):** *an attempt is a completion or a skip*, holding `success_rate = completion_count / (completion_count + skip_count)` — deliberately the **same** definition `score.ts:73-75` already encodes, so writer and scorer are one definition rather than two that happen to agree. It collapses R6 to `(C+1)/(C+S+2)`, the Beta(1,1) posterior mean (Laplace), giving R6 a readable meaning; **Phase B changes only the prior's source.** Park → untouched (not an attempt); **crash-recovered `abandoned` → untouched *structurally*** (`recoverOpenEpisode` never reaches the primitives — like constraint #11, not a policy check); self-completion → counts fully (task 44's open sub-question, closed this way because the middle positions break the CHECK, score doing-the-task as failure, or need a migration). **No migration needed** — every pre-writer row satisfies the invariant since `0/(0+S)=0`, pinned by test. **True test-first: 18 tests red across 3 suites before the implementation existed**; 4 post-hoc mutations confirmed real detectors. **998/86 green** (+24), `tsc` clean, `eslint` unchanged — coordinator-re-verified. 🔴 **Three deviations — see Open rulings owed.** Report: `docs/eval/task17_phaseA_findings_report.md` · Brief: `docs/briefs/numeric_learning_task_17_phaseA.md`. **PHASE B (queued):** the six §5.4 loops, hierarchical shrinkage, regression protection, rollback — consuming task 41's device-confirmed thermal sampler and, per task 50, owning the internal **2/4** band as the home for the idiosyncratic activation cost extraction deliberately excludes. *(Original scope follows.)* Six loops with hierarchical shrinkage, regression protection, rollback. Replaces R6's fixed 0.5 prior with a learned one — same formula. **First real consumer of `algorithm_weights`.** **Handoff from 13:** `completion_count`/`success_rate` have **no writer anywhere** — `historicalSuccessFactor` scores every task off a permanent n=0; 17 is the natural owner. *Headless. Opus-5-worthy for the regression-protection + rollback interaction.* | 9, 11, 13 | **Opus** |
| 18 | ✅ Done | Skill-injection layer **design** (§5.5) | Flat-AND closed-vocabulary matching, `conf = Ceff/(Ceff+2·Xeff+3)` at 45-day half-life, oscillation trap closed via a fired-outcome channel, code-first distillation with one 4B judgment per call, latency-tiered injection. `docs/design/skill_layer_task18_design.md`. | 4, 7, 12 | **Fable** |
| 19 | ⬜ Undone | Skill-layer implementation | Build 18's design. **Owns the thermal sampler** (currently a stub). Two new grammars plus distillation prompts need a real model. **Planning-scope skills have exactly one legal consumer: 11's `PlanAdjustment` hook.** If it wires an LLM there, it must add a validation wrapper (the hook must never resurrect filtered tasks; nothing enforces that today). **Handoff from 13:** decide whether a recovered crash (`completion_status='abandoned'`) counts as a friction incident — §1.3 says a crash isn't user failure. | 12, 18, 26, **P** | **Opus** — ***Opus-5 for the outcome-attribution/confidence channel*** |
| 20 | ⬜ Undone | Eval harness | 🔴 **PINNED 2026-08-17 — must track junk-tag rate as an explicit eval signal.** Task 7 Phase B already watched it informally (`tagCount`/`avgTagCount`) and learned the hard way *why it must be a signal, not a score*: **`context_tags_must_include` is a SUBSET check, so a model that dumps the entire known vocabulary passes it every time** — iter 2 did exactly that and the KPI reported `context_tags` as nearly fine while the regression was invisible. Carry that lesson forward: `must_include` is a minimum, so **extra tags can never be called correct**. The residual `"work_on_it_until_did"`-class junk (~2–3/run) is a clean **model-capability** discriminator, not a prompt-quality one — see the standing note. Reusable scoring over fixtures; reuse task-5 validators + task-7 scorer. **Also owns the formal success criteria** — the valid@1 / field-correct bar. Harness is headless; *the numbers it produces are only real when run against the device model.* | 4, 5, 7, **31**, **P** | **Sonnet** — ***Opus-5 for negative-control design*** |
| 21 | ⬜ Undone | Crisis detector review + referral localization | Two human judgments: coverage of the draft phrase-matching detector (indirect expressions are the common form), and `CRISIS_REFERRAL_TEXT` content/localization. **On-device evidence: the 4B cannot self-detect distress** — coverage claims must be re-checked on hardware. ⚠ **Changed by 41/42 (2026-08-07):** 21 was briefed to judge the phrase list cold because no real data existed. Task 41's crisis-gate stream gives it an alpha corpus of **actual firings and near-misses**, retained for exactly this purpose — so **21 should be re-briefed to review against evidence**, and running it before that log accumulates wastes the best input it will ever get. Soft dependency on 41, not a hard gate. Brief: `docs/briefs/crisis_review_task_21.md`. 🔴 **Hard beta gate.** | 7, 12, *(41 soft)*, **P** | **Human** |
| 22 | ⬜ Undone | `which:"next"` weekday semantics | From a Thursday the 4B read "next Monday" as 11 days out. Decide in `resolveDue` or teach the guides; affects **every** resolved date. If solved by teaching the guides rather than in code, the fix is a model-behavior claim and needs device confirmation. **Task 31 should collect several real instances** — they cost nothing extra and become the evidence when this gets ruled. | 5, **P** | **Opus/Sonnet** |
| 23 | ✅ Done | UI/UX design | **Design substantially done.** Interactive prototype at `docs/design/Main Screen.dc.html` + `Coaching Screen.dc.html`; review `docs/eval/task23_review.md`. Built in two passes; all six divergences verified in-code. The three follow-ups were **closed for free by task 24**. The polished/designed visual pass remains a **beta** gate. | spec §6 | **Claude Design (Opus)** |
| 24 | ✅ Done | Product UI implementation | **Done — functional/personal pass, confirmed on the S23 FE (DB-verified).** `src/app/` (controllers / presentational screens / shell / alarm). The whole personal-ship loop runs on device: add-via-chat → session → timer → all five outcomes → coaching → summary. **Alarm fires 11 ms late** via an `AlarmManager.setAlarmClock` TurboModule — **constraint #13 discharged**. 636 → **711 tests**. **Phase B found 5 bugs + 1 build trap, 4 silent — all fixed.** Report: `docs/eval/task24_findings_report.md`. **Residue:** designed pass is beta; device edges → 32; five handoffs. | 23, 6, 9, 11, 12, 13, **P** | **Opus** + **Sonnet** |
| 25 | ✅ Done | Post-review scoring follow-ups (R6–R8 + U1) | **U1** dependency-blocked pre-filter (mandatory — blocked 11); **R6** smoothed historical success `(rate·n + 0.5k)/(n+k)`; **R7** keep the parent, fire immediate `breakdown_complete` coaching, hold it out of the pool; **R8** neglect accrual gate `anchor + period/(1+quota)`. Report: `docs/eval/task25_findings_report.md`. | 9, 10, 26 | **Opus** |
| 26 | ✅ Done | Skill-layer schema migration | `learning_state` table, `skills.is_active` default flip, `skill_evidence.source`, `learning_data` `"v":1`, `fireable_skills` comment. **Plus both new `coaching_queue` trigger types in one migration.** Hazard: CHECK changes need full table rebuilds. **Also fixed the migration runner**, which never applied more than one migration and could not toggle `PRAGMA foreign_keys` inside a transaction. Report: `docs/eval/task26_findings_report.md`. | 18 | **Sonnet** |
| 27 | ✅ Done | Spec fold-in (v2.2 → v2.3) | Spec v2.3 + schema v2.3 snapshot — R1–R8, the two-pre-filter selection boundary, and §7.2's five triggers. **Note:** written in parallel with 25/33, so it describes the ruled target state; its §4.5 `algorithm_weights` claim was overtaken by migration 004 and corrected in v2.4. | 10, 25 *(rulings)* | **Sonnet/Haiku** |
| 28 | ✅ Done | Multi-session work + hyperfocus extend (§8.7) | **Design delivered:** `work_state` axis orthogonal to `status`, `duration_type = estimate\|floor`, cumulative fold at one choke point, extend in +25-min quanta with tail regeneration, one resume slot in the deep-focus block. Zero scoring changes. **AMENDED 2026-07-20** — extend splits into two affordances (+5 flat/uncapped, and hyperfocus in 25-min quanta); guardrail **ruled B, hyperfocus only**. Implementation → 33 (landed) and 13 (runtime). | — | **Fable** |
| 29 | ⬜ Undone — **re-scoped 2026-08-07** | Quant path / Stage B decision | **Both halves of the original question have moved.** *(a) "Find or make an 8B that runs"* — **answered**: `Bonsai-8B-Q1_0` is loadable on stock and enters **task 40**'s bake-off; no separate work needed. *(b) "Build Stage B against the PrismML fork for Q2_0 + Vulkan"* — **newly obstructed**: the spike found `prism-ml/Ternary-Bonsai-8B`'s **`Q2_0` is absent from the build entirely and cannot load**, so the fork path isn't "untried", it's missing its premise. **What's actually left** is a narrower, later question: *is a fork build worth it purely for **Vulkan GPU** throughput?* — a general-ship performance call, not a quant-path one, and only meaningful after 40 picks a model. **No longer blocks anything** (task 8 retired). *General-ship, deferred.* | 0, 40, **P** | **Jason** |
| 30 | ⬜ Undone | Device envelope | Minimum supported spec (RAM, chipset class, OS versions). One device tested (S23 FE, SD 8 Gen 1, 8 GB). Sets the tiering floor and decides whether 8B-first is realistic or aspirational. **Needs more than one device to be a real envelope.** 🔴 **Hard beta gate.** | 0, **P** | **Jason** |
| **31** | 🔴 **Undone — CRITICAL PATH · NOW FULLY UNBLOCKED (41 + 50 both done)** | **Real task + friction corpus** | ✅ **Task 50 CLOSED 2026-08-22 — the last gate is lifted; 31 is ready to run.** 🔴 **Energy labeling: use `docs/design/energy_definition_task50.md` §2b** as the `energy` gold rubric (activation cost, take-the-higher; gym→`high` / trash→`low` calibration pair), with its **hard gold constraints (§3/§5/§6c): NO null energy golds, NO internal-2/4 golds, and golds are HUMAN-authored — a model may draft input strings but NEVER the answer key** (the 16-fixture bank's model-generated key is exactly the trap that invalidated it). ✅ **Task 41 Phase 2 has landed, so the gate is lifting** — capture accumulates the corpus by itself once the device build runs; 31's interview and reconstruction work runs **in parallel** with that window. 🔴 **Two loader facts from 41 that will silently corrupt this corpus if missed:** **(1) `escapeToEasier` writes TWO `episode` rows** — deliberate, because constraint #11 makes park-vs-skip load-bearing, so **the loader must dedupe on `interactionId`** or every escape double-counts. **(2) `mutation` emits ~20 rows per chat-created task** and will dominate that stream — size expectations and any sampling must account for it. **The measuring instrument for the model decision.** Three item types, three consumers: **extraction items** (→ 38 training, 40 held-out eval, 20 fixtures), **coaching transcripts** (→ 38's no-regression check, 19), **friction episodes** (→ 19). ⚠ **RE-SIZED 2026-08-07:** the old "20–30 real messy tasks" target *cannot do this job.* 16 fixtures resolve to ±12 pts — the reason the model decision is still open. Split 20–30 items in half and the held-out set is ~10–15 → **±23 pts, twice the uncertainty the corpus exists to remove.** Task 40 would run the full device bake-off and still not separate the contenders. **New target: 80–120 extraction items, 45–60 held out (±11 pts).** Split must be **grouped by source situation and stratified on `recurrence`/`due_resolved`**, assigned in-file, never revisited. ⚠ **Gated on task 41.** The dogfooding DB holds no real captures — migration 001 says `raw transcript never stored`, so weeks of exactly this material is gone and unrecoverable. 41 makes the corpus accumulate by itself; 31's interview/reconstruction work runs **in parallel** with that window, not after it. **Synthetic and augmented items are legitimate in the *train* split and fatal in *held-out*** — n buys precision, realism buys validity, and they're independent. Tag every item's `source`. Brief: `docs/briefs/real_task_corpus_task_31.md` · Init prompt: `docs/briefs/task_31_session_init_prompt.md` | **41** | **Jason** produces · **Opus 5** designs the split + first ~15 + report · **Sonnet** bulk transcription *(deliberate move out of the "no Opus-5 benefit" tier — a bad split poisons every downstream number)* |
| 32 | ⬜ Undone | Device verification sweep | The standing residue, batched into one session: **`add_missing_task` dispatch**, `add_dependency` dispatch, the **unmeasured D1 recap→constrain flow**, task 24 §10's device edges (resume_block recovery, overnight doze on battery, locked-screen full-screen intent, the two untapped recurrence kinds), and 19's two new grammars when they land. 🔴 **NEW from task 41's StatFs amendment (2026-08-18):** compare `ANDROID_EXTERNAL_FILES_PATH` and `ANDROID_DATABASE_PATH` free-space readings on the first build. **If equal, path-scoping is merely careful; if they differ, it is load-bearing** and task 14 must keep using it deliberately rather than collapsing to one number. 🔴 **NEW from task 37 (2026-08-17):** confirm the four tightened grammars **still compile through the startup guard on real llama.cpp**, and that normal extraction has **not regressed on Bonsai** through the `firstChar` constraint. 37 is headless-verified only — no real parse has run. **Also now due: the first build of merged `main` on the S23 FE**, which has never happened. Brief: `docs/briefs/device_sweep_task_32.md`. *Entirely device work.* | 12, 19, **P** | **You + Opus** |
| 33 | ✅ Done | Multi-session / hyperfocus **implementation** | Migration 003 (`work_state`, `duration_type`, `accumulated_minutes`, `last_worked_at`), the park primitive, the cumulative fold in `completeTask`, the three-way anchor in `listActiveByNeglect`, one grammar field. Report: `docs/eval/task33_findings_report.md`. | 28, 25 | **Opus** |
| 34 | ✅ Done | Schema reconciliation pass | Migration **004** → schema **2.5.0**. `context_fit` dropped from the `factor_name` CHECK and deleted; the four survivors reseeded to **31/23/23/23** but only where `data_points_count = 0` (a guard written for task 17's sake). **`active_tasks_with_neglect` dropped** — it computed retired `weeks²` via a `POWER()` that doesn't exist on-device. Report: `docs/eval/task34_findings_report.md`. | 27, 33 | **Sonnet** |
| 35 | ✅ Done *(merged to `main` 2026-08-07)* | Spec fold-in (v2.3 → **v2.4**) | `docs/reference/..._v2.4.md` delivered. Folds in migrations 004+005, task 11's planner contracts (`PlanAdjustment`, `blockKind`, the two-filter boundary, `replanRemaining`), and the extend amendment. Every name/formula pulled from source, not the brief. **Confirmed no cross-doc conflicts.** Report: `docs/eval/task35_findings_report.md`. **Was marked done for four days while sitting on an unmerged branch.** | 11, 34 | **Sonnet/Haiku** |
| 36 | ✅ Done *(merged to `main` 2026-08-07)* | Recurrence period engine (§4.2) | `src/services/recurrence/` — idempotent `advanceRecurrence(today)` sweep at app-open + session-start; migration **006 / schema 2.7.0**. Missed-quota boost **derived at scoring time** (never written to `importance`); `last_period_shortfall` persists the fact behind it. **Fixed a live bug worse than briefed:** urgency (23% of score) was broken in *both* directions for every recurring task — chat-created pinned at 1.0 forever, editor-created at the floor forever. DST-correct; neglect anchor **provably** untouched. 711→**794 tests**. Report: `docs/eval/task36_findings_report.md`. ✅ **CONFIRMED ON DEVICE 2026-08-18** — the `advanceRecurrence` sweep ran on the S23 FE for the first time against 3 real recurring tasks at app-open: `unscheduled` skipped, both `scheduled` advanced to the current period, `last_period_shortfall`=0, neglect anchors provably untouched. ⚠ **Open — disposition now CHECKED (2026-08-17): 36 did NOT fold it in.** Its report is explicit that *"'Every N weeks' intervals are still not representable"* — the handoff arrived via orientation §9 but brief §2's scope never listed it, so it was correctly left alone. The report adds that `recurrence_pattern` is free-form JSON, so an `interval` field needs **no migration**; what it needs is a UI control, an anchor date to count from, and one new `period.ts` function. **Unowned and unnumbered — see Open rulings owed.** | 2, 3, 25 | **Opus** |
| 37 | ✅ **Done (headless) 2026-08-17** — light `P` outstanding → 32 | Extraction-grammar separator-token hole | **Live bug in shipped extraction, found by the model spike.** `task_extraction.v1.gbnf`'s `title` rule accepts a bare `","` as a complete, **schema-valid, validator-passing** value — a task titled `,`. `description` shares it; `newTag`/`tool`/`date` need audit. Bonsai dodges it only by not ranking `","` first (luck). **Fix (spike-confirmed): first char `[a-zA-Z0-9]`** — min-length does NOT work. Add a validator defense-in-depth layer too. **Landed `0ad7184` + `8a4a34f` on `main`.** Report: `docs/eval/task37_findings_report.md`. **The audit found 15 rules across 4 grammars, not the 2 the brief confirmed** — extraction (`title`, `description`, `date`, `newTag`, `tool`), breakdown (`title`), resolution (`newTag`, `changesNotes`, `reason120`, `date`, `condition120`, `title`), summary (`keyPoint`, `disposition`, `energyNote`). Fix is `firstChar ::= [a-zA-Z0-9]` opening every free-text rule, **bounds unchanged** (`firstChar jchar{0,n-1}` spans the same 1..n). `tagKnown` is legitimately exempt (D7 closed alternation, no separator to emit) and the exemption is asserted in the test rather than silently skipped. **Validator layer adopted and deliberately *weaker*** — rejects a title with no alphanumeric *anywhere* rather than first-position, because the grammar constrains generation cheaply while validator rejection costs a D10 retry, and `"$50 to the landlord"` must survive. It also covers the **prompt-JSON fallback path where no grammar is in play at all**. Verified independently by the coordinator: **811 tests / 69 suites** green, `tsc` clean, `eslint` unchanged from baseline. ⚠ **Believed, not confirmed — no real llama.cpp parse has run.** The startup-guard compile and the no-regression-on-Bonsai check are device-only → **task 32**. *(If the compile did fail, the guard contains it by falling back to prompt-JSON + validation — where the new validator layer is the live defense.)* | 5 | **Opus/Sonnet** |
| 38 | ⬜ Undone | Train the Gemma 4 E2B LoRA | **Model-migration chain, step 2.** The six-model spike (`model_base_spike_final_findings.md`) recommends Gemma 4 E2B — trainable (Bonsai's ternary is frozen), 2× faster/capture, best distress. Train a runtime-loadable LoRA (`applyLoraAdapters`) to close its `recurrence`/`due_resolved` gap. **Try the free prompt-only null-convention fix first.** Trains on 31's **train** split only; tunes against a `calibration` slice, never `heldout`. Brief: `docs/briefs/gemma_lora_training_task_38.md`. | **31** | **Opus** (pipeline) + **Jason** (trains) |
| *39* | — *reserved* | *(optional corpus → eval-harness step)* | Held free in case 31→38 wants an explicit conversion/harness task between them. Currently unused. | — | — |
| 40 | ⬜ Undone | Three-way model bake-off (migration gate) | 🔴 **PINNED 2026-08-17 — must report junk-tag rate per model.** It costs nothing extra (the fixtures already exercise it) and it is a **differentiator the 16-fixture resolution problem does not wash out**, because it is a *rate across all items* rather than a per-field correctness call subject to the ±12-pt band. It measures instruction-following on guidance that is already explicit and already tuned — precisely the class of failure a LoRA fixes and a prompt does not, which is the entire case for Gemma. See the standing note. **Chain step 3 — gates the decision, doesn't make it.** LoRA-Gemma-E2B vs **Bonsai-8B-Q1_0** (downloaded, never tested) vs T-Bonsai-4B, on task 31's **held-out** split, the spike's exact Gate-2 protocol (same corpus, same day, same build, cooled between runs), pulled-DB verified. **37's grammar fix goes in first.** Must state the resolution it's running at, from 31's findings report. Framing: trainable-but-behind vs frozen-but-ahead — **Jason's call**, recorded to orientation §1. Brief: `docs/briefs/model_bakeoff_task_40.md`. | 38, 31, **P** | **Jason + Opus** |
| **41** | 🟡 **Phase 1 + Phase 2 DONE 2026-08-18 · device run outstanding — GATES 31** | **Lossless local event capture** | ✅ **Phase 2 landed** (`0393654`, `470a8f5`, `46e25dd`, `96e2279`, `96155ed`, `f419cfa`). `src/capture/` — twelve streams, one `record()` entry point — plus `src/specs/NativeCaptureLog.ts`, `CaptureLogModule.kt`, `CaptureLogPackage.kt` (registered in `MainApplication.kt`). Built in the ruled order: **acceptance test first**, then module + writer, then call sites, then the widened error, then `scripts/pull-capture.js`. **Coordinator-verified: 910 real tests / 80 suites** green (raw 1704/148), `tsc` clean, `eslint` **0 errors / 56 warnings — identical to baseline** *(`sha256.ts` carries a justified file-level `no-bitwise` disable; without it the count would be 110 and the baseline comparison meaningless)*. 🔴 **The force-kill test is a demonstrated regression detector, not an assumed one** — substituting a 32-record buffered writer made it fail on exactly the intended assertion (23 acked events absent from disk), and it was written and committed *before* the implementation. ⚠ It exercises the **Node** writer over a transpiled copy of the real `record.ts`, **not the Kotlin one** — stated in the file header and the report. 🔴 **Removability was VERIFIED, not asserted** — design §11 steps 1–3 were actually performed on the `crisis` stream with `tsc`, naming exactly the two production call sites (both in `chatController.ts`), then restored. That is the property task 42 depends on. **`sessionController.ts` untouched — zero lines**; the correlation frame is set from `episodeService.ts` instead, which also picks up `recoverOpenEpisode`, so **task 44 should rebase cleanly**. Five `src/app/` files touched, enumerated in report §7. **No migration, no schema change.** 🔴 **Two deviations — 2.1 recorded/attributed, 2.2 needs a ruling. Three measured numbers still owed and only the device can give them.** Report: `docs/eval/task41_findings_report.md` — *(Phase 1 record follows.)* ✅ **Phase 1 complete 2026-08-17.** Format design (`docs/design/capture_format_task41.md`) + **`capture_format_task41_amendment_rulings.md`, which closes every open question** — all six of §12 plus two follow-ups. **Rulings:** storage = **a new app-owned TurboModule with synchronous append** (not `react-native-fs` — a Promise-based `appendFile` loses exactly the events before a native crash); **twelve streams, one egress class each**; crisis stream logs **every turn the gate ran on, hit or clear** (option (c), reversing the design's own (b) recommendation — a near-miss watch list is *also* a phrase list and structurally cannot catch the indirect expressions `crisis.ts` documents itself as missing); actor vocabulary **`user`/`coach`/`system`/`planner`** with `planner` as a sentinel whose expected count is zero (a non-zero count is direct evidence of the unenforced `PlanAdjustment` contract being violated); **in-app thermal/battery sampling built here** (Jason's explicit instruction — 🔴 **a recorded deviation from orientation §8's pin to task 19**, sampling only, *no thermal policy*); **keep everything** with a single black-swan ceiling warning and no second trigger; **`fsync` per event for alpha**, reverting to boundary-only at beta. ⚠ **Four builder calls remain provisional** (amendment §8) — the **global `v`** is the one that is not cheaply reversible and needs deciding before Phase 2 writes records. **Phase 2 order:** force-kill test first, then TurboModule + `record()` + writer, call sites, widened `LlmOutputValidationError`, `scripts/pull-capture.js`, then Jason's device run. **The findings report already owes three items:** the thermal deviation, the halved test count, and app-open time with capture on vs off. ✅ **CONFIRMED WRITING ON DEVICE 2026-08-19** — `NativeCaptureLog` resolves at runtime (the `.cxx` flag genuinely worked, not just in `build.ninja`). A real chat extraction produced the full twelve-stream tree in **external** app-private storage (deviation 2.2's root confirmed): `conversation` carries the coach turn (`v:1`, process-global `seq`, per-process `run`, correct null `sessionId`); **`modeltext` carries the RAW model completion verbatim before parsing** — the exact data the app used to discard, now preserved as corpus gold; `modelio` carries metadata + `grammarId`/`grammarSha8`/`todayISO`/rung/timings and points to the raw text by `textRef` (the egress-class split intact); **`mutation` emitted 24 records for one chat-created task**, reproducing the design's ~20-per-task prediction on real data. 🔴 **Data point: the first COLD extraction took `latencyMs:83334` (~83 s)** — includes the 1 GiB model load; watch this against warm calls. ✅ **FORCE-KILL ACCEPTANCE TEST PASSED ON DEVICE 2026-08-19** — the test the whole design was built around. Drove a quick-start session to a live episode (Charlie, taskId 23), `am force-stop` mid-episode, relaunched. Result across the crash: **pre-kill run seq-contiguous (zero gaps), post-kill run boots fresh at seq=1 and is seq-contiguous (zero gaps, zero dups), zero `dropped` markers, and crash recovery derived the IDENTICAL episodeId** (`s-…#23@1787124256922`) — design §14.2's core assertion. Nothing was lost. This also re-confirms **task 13's `recoverOpenEpisode`** on device. ✅ **THERMAL SAMPLING CONFIRMED 2026-08-19** (headless, from the pull) — the `runtime` stream carries `{type:'sample', thermalStatus:0, batteryLevel:0.79, charging:false}`, so task 41's in-app `PowerManager.getCurrentThermalStatus()` + battery sampler (the seam Jason moved from task 19) works on device and lands in capture. `thermalStatus:0` = NONE (cool); returned cleanly despite an unrelated `ThermalManagerService` HAL warning in the system log. ⚠ **StatFs two-volume has NO UI path — it is 'exposed, not consumed', so nothing invokes `availableBytesFor` from a screen.** It needs task 14's (unwired) consumer or a dev affordance to test on device; flagged as a real gap, not deferrable by tapping. **Still device-pending:** thermal-UNDER-LOAD (needs ~20 min of model load) and the app-open `fsync` on/off number. Conversation logs (verbatim, both directions, incl. clarifying Q&A), task changes (user- vs model-made, field-level, with surface), task performance (episodes, outcomes, +5/hyperfocus, parks, crash recoveries). **Ruled 2026-08-07: log everything in alpha, no exclusions or redaction — and build every stream to be removable *independently*.** That's a settled decision (orientation §5), not a preference: streams separately scoped, each under a directory it owns, one module/one entry point, on-disk layout a written contract, no capture logic diffusing into instrumented code. 42 deletes **one** stream while keeping the rest, and must prove it. The crisis **detector** is untouched — only its logging is removable. **Additions:** 🔴 raw model I/O (composed prompt, **raw completion before parsing**, grammar, D10 rung, latency, tok/s), 🔴 validation failures **with payload** (`LlmOutputValidationError` currently drops it — that's why 37 cost a whole spike), scoring/planning snapshots incl. both reject sets, coaching lifecycle, crisis-gate firings + near-misses (task 21 has *zero* real data today), thermal/device conditions, app lifecycle. **Design:** out-of-band append-only JSONL on app-private storage — never in the product DB, must survive its corruption; synchronous writes (buffering loses exactly the events around a crash); capture failure never breaks the app but never silent either; correlation IDs + `"v":1` on every record; redaction seams present but off. **Acceptance test that matters: force-kill mid-episode loses nothing.** Brief: `docs/briefs/event_capture_task_41.md` | 24, 13, **P** | **Opus** + **Jason** (device) |
| **42** | 🔴 Undone | **Crisis-stream teardown + consent for the surviving capture** | 🔴 **NEW FROM 41 PHASE 2 (2026-08-18) — the one field a privacy reviewer should look at twice.** **`planning.capabilityRejects[].reason` carries the context and tool NAMES the user chose.** They are a vocabulary rather than prose and the stream stays structured, so it survives the open-beta rung under 43 — which is exactly why it needs a decision rather than a default. ✅ **Removability is now a verified property, not a hope:** 41 actually performed design §11 steps 1–3 on the `crisis` stream against `tsc` and it named **exactly the two production call sites**, both in `chatController.ts`. Job A's teardown has a demonstrated method. 🔴 **HARD CLOSED-BETA GATE. Two jobs.** **(A) Teardown:** the **crisis-gate stream only** is removed before beta — for a tester it's a record of their worst moments in a file, and no consent flow makes that worth it. Jason's own alpha crisis log is **retained** (it's task 21's only real evidence). The detector itself is untouched; only its logging goes. **(B) Governance:** the other streams **ship and record beta testers**, so consent (first-run, in 23's language, stating it never leaves the device), controls with real categories underneath, **retention** (⚠ `data_retention` — created in migration 001 with three policies, *never written to or acted on*; implement or retire it), and export/delete that provably reach **both** stores. **Defaults are the real policy** — recommend performance + model I/O on, raw transcripts off unless enabled, crisis stream nonexistent. **Verified not asserted:** grep the beta bundle for crisis symbols; fire the gate and confirm it still refers but logs nothing; toggle each control off and confirm the stream is empty; export-then-delete reaches both stores. **Private archive:** retained logs, corpus text and the crisis log live **outside the repo** — the repo travels, and retained-forever + committed-to-git means a verbatim record of Jason's life ships with the source. **Also builds the egress pipeline:** ruled 2026-08-07, **nothing leaves a tester's device un-anonymized** — anonymization runs *at the source, before export*, as a pipeline distinct from capture (consuming 41's redaction seams). Closed beta is the **last rung where free-text egress is defensible**, and only because the volume permits per-item review. **Also owes:** the `conversation_summary` comment correction and constraint #7's clause (local capture ≠ telemetry; the distinction is *transmission*). **Review with task 21.** Brief: `docs/briefs/privacy_consent_task_42.md` | 41, 21 | **Opus** implements |
| **43** | ⬜ Undone | **Capture ladder — open-beta and GA pruning** | 🔴 **NEW FROM 41 PHASE 2 (2026-08-18):** the open-beta rung keeps *structured* streams on the reasoning that they anonymize essentially completely — **but `planning.capabilityRejects[].reason` carries user-chosen context/tool names**, which is closer to free text than the rung's justification assumes. **Decide it explicitly at this rung rather than letting "structured" carry it through by category.** Reviewed with 42. 🔴 **Gates OPEN BETA, then GA** (42 owns the closed-beta rung). **Ruled 2026-08-07:** *logging will be pruned/dropped as we move from closed beta to open beta to general release.* **The mechanism, not a mood:** the closed-beta safeguard is **human review of a small volume from known people — and that does not scale, and fails silently.** The stream must narrow exactly where the protection stops being real. **Open beta: free-text conversation capture is dropped**; structured streams (performance, outcomes, timings, model-I/O *metadata*, thermal, errors) survive, because they carry 17/19/20's signal with almost none of the exposure. **GA: minimal or nothing.** Prune by **dropping, not disabling.** Anonymization is best-effort on free text and **"anonymized" never means "safe to publish"** — the real risk is **re-identification by combination**, since a task list is close to a fingerprint even fully scrubbed. That asymmetry (structured anonymizes completely, free text doesn't) is what makes the ladder principled rather than an arbitrary line. ⚠ **See the corpus-window consequence in the frontier section.** Brief: `docs/briefs/capture_ladder_task_43.md` | 42 | **Opus** implements; **Jason** rules each rung |
| **44** | 🟡 **Code DONE + desktop-verified 2026-08-18 · device (`P`) outstanding** | **Personal-use QoL pass** | ✅ **Landed** (`300eee4` migration 007, `2e880c0` items 1–4, `3339390` item 5, `3342cb9` report). **Coordinator-verified: 961 real tests / 83 suites** green (raw 1755/151), `tsc` clean, `eslint` unchanged. **Migration 007 / schema 2.8.0** (`sessions.origin`, one writer, NULL = "distinction didn't exist yet"). **Self-complete reuses `completeTask()`** (constraint #7 branch + 33's fold + 36's advance); writes an `interactions` row with explicit null runtime fields, counted for completion/neglect, excluded from duration learning. 🔴 **`completion_count`/`success_rate`: DELIBERATELY no writer** — reasoning recorded for **task 17** to inherit (self-complete is not evidence of the historical-success signal those columns feed). **Quick-start reuses the real `src/planning/` predicates** so its warning can't drift from the filter it mirrors; **`WorkScreen.tsx` false comment fixed** (the citation that spawned the whole deviation rule). **Junk-purge script written with a dry-run-first mode — but NEVER run against real data** (see rulings owed). ✅ **`sessionController.ts` collision with 41 avoided** — 41 set its frame from `episodeService.ts`, so 44's `startSession` edits landed clean. 🔴 **Three deviations, all ratified 2026-08-18 — see below.** ✅ **Migration 007 CONFIRMED ON DEVICE 2026-08-18** — `ADD COLUMN origin TEXT CHECK(...)` applied on real op-sqlite, `sessions.origin` present, 13 old rows NULL-not-backfilled. ✅ **DEVICE-VERIFIED 2026-08-19** (device subagent drove UI; coordinator verified the DB): **self-complete confirmed correct** — task 27 → `status:completed`, `interactions` row `{type:task_completion, session_id:null, energy:null, duration:null, notes:'self_completed'}` exactly as ruled, and 🔴 **`completion_count`/`success_rate` stayed 0 — the deliberate no-writer convention holds, task 17 inherits it.** **Quick-start correctly drains the coaching queue at session start** — a `session_lapsed` coaching (urgency `next_start`), queued by the earlier force-killed session, surfaced at the next quick-start and routed to Coach; correct behavior (quick-start is a normal session start), confirmed against the DB after the subagent flagged it as a possible anomaly. ✅ **Mismatch-warning screen (item d) CONFIRMED 2026-08-19** — quick-started task 31 (`path to IRS appointment`, needs IRS/appointment/government) with a `home` context: the **"Before you start"** screen named BOTH filter conditions — *"wrong context — this session doesn't have IRS, appointment, government"* and *"doesn't fit in the time planned (10 min)"* (the 15-min task in a 10-min block) — with **Start anyway / Back out** and informed-consent copy. Reuses the real `src/planning` predicates (named the exact missing contexts). ✅ **Coaching-resolution dispatch CONFIRMED** — Test D drained coaching id 16 (`session_lapsed`), model replied coherently in ~15 s (warm), `coaching_queue.status` → `resolved`. *(A device subagent's earlier report of quick-start showing 'Echo' instead was a **mis-tap** — Echo has empty context so correctly no warning; coordinator re-ran on the right task and the warning fired correctly.)* ⚠ **Still open, test-data gap only:** no dependency-blocked task exists, so blocked-button disabling (item a) is unverified for lack of a subject. 🔴 **Two real UI bugs to check — see rulings owed:** a non-functional 'Wrap this up' button on the Coach screen, and StatFs has no consumer. Junk-purge dry run: see rulings owed. Report: `docs/eval/task44_findings_report.md`. | 24, **P** | **Sonnet** |
| **45** | ⬜ Undone | **Deviation audit — what changed a human decision without sign-off** | **Ruled 2026-08-07:** *any deviation from human-made decisions must be flagged in a report AND explicitly surfaced during coordination review.* Standing rule now in **handoff §4**; this task cleans the backlog that predates it. **The trigger:** task 24 shipped a linear bar where task 23's approved design had a dial, and Jason was never asked. Three failures — the code comment cited a brief authorization **that doesn't exist**; the report filed it under *"Deferred… deliberately, not forgotten"* (reads as scheduling, was a decision); **and nobody surfaced it in review.** **The systemic risk:** two report section titles — *"Decisions this task had to make"* (24 has **8**) and *"Deferred deliberately"* (24 has **6**) — quietly absorb overrides, and **some are already folded into orientation §3/§5 as settled**, indistinguishable from Jason's own rulings. Known seeds: §4.1 three-vs-five end-of-block options (five was ruled); §4.2 tools optimistic at planning (reorders spec §6.2); §4.6 startup guard on first model use (constraint #3 says "at startup" — good argument, still a decision, already canon). Then the same pass over **13, 11, 33, 36, 12, 7**. **Deliverable:** one table — what changed / what it deviates from / where recorded (or "nowhere") / whether it entered the canon / the argument for it, fairly stated / ratify-revert-revisit. **Jason rules each; orientation §5 then distinguishes ruled-by-Jason from decided-by-builder, which it currently cannot.** Audit only — reverts become their own tasks. Brief: `docs/briefs/deviation_audit_task_45.md` | — | **Opus** produces; **Jason** rules |
| **46** | ✅ **Done 2026-08-24 — engine + amendment + editor; all four modes reachable** | **Scheduled recurrence modes** *(scope expanded by Jason 2026-08-24 from "every N weeks" to FOUR modes)* | ✅ **PHASE 1 — the engine.** `repeat?` added to the existing `scheduled` member: **`everyWeek`** (default = today), **`interval`** (every N weeks), **`ordinal`** (1st & 3rd Wed, every N months), **`dayOfMonth`** (the 15th, every N months). 🔴 **No migration, no schema change** — `recurrence_type` still holds the same five values, **asserted by a test that reads the column out of SQLite for every mode** (a new type would have meant a CHECK change on an existing column → constraint #12 rebuild). One entry point `nextScheduledOnOrAfter`, exhaustive switch, pure local-calendar arithmetic, no UTC-ms date math. ✅ **Backward compatibility pinned in THREE places** — read (legacy JSON parses with **no `repeat` property at all**), write (a weekly schedule serialises **key-for-key** to the pre-46 shape, so opening and saving an untouched task cannot rewrite live rows), and sweep (the alpha DB's literal JSON inserted by raw SQL, swept against the real engine). Jason's 3 live recurring tasks are safe. 🔴 **The drift test landed and it is the valuable one:** both schedules anchored to the same Monday agree for four occurrences, then **September 2026's fifth Wednesday** sends the fortnightly one to 30 Sep while the ordinal one waits for 7 Oct — gap sets **{14} vs {14, 21}**, 13 firings vs 12 over six months. *"Every other Wednesday" and "1st & 3rd Wednesday" are now provably different schedules.* ✅ **Constraint #5 verified per mode** (snapshot → sweep 3× months apart → anchors unchanged); R8's gate gets the modes via a `scheduledCycle` helper, 1st & 3rd → 30/(1+2) = **10 days**, matching the brief's worked example. **1121 tests / 92 suites** (from 1038/89), `tsc` clean, `eslint` unchanged — coordinator-re-verified. **Test-first: 39 failures watched in the pure suites + 18 in the DB-backed ones before implementation. No carve-outs.** 🔴 **One deviation + two choices needing a ruling — see Open rulings owed.** ⚠ **UNREACHABLE BY A USER until Phase 2** — nothing constructs a `repeat` (verified across `taskDraft.ts`, `RecurrenceEditor.tsx` and the extraction mapper). **This is deliberately the task-14 state, named rather than hidden.** **PHASE 2 (queued):** the editor — a mode selector after weekday selection, an interval stepper, ordinal chips (1st/2nd/3rd/4th/Last), a day-of-month multi-select and a month-stride stepper, all on the existing `SelectChip` primitive; report §7 also lists two engine rules the UI must respect or it will throw at the user. ✅ **PHASE 2 — THE EDITOR (2026-08-24). All four modes are now reachable end-to-end, asserted not argued** by driving the real controller and real repositories against real SQLite and reading each mode's `repeat` back out. **Top-line dropdown** carrying all nine options in Jason's ruled order, region beneath re-shaping to the selection; **6×7 grid** (Su–Sa × 1st…Last), each ticked cell one occurrence; **31-cell dates grid**. Most of the work was `taskDraft.ts` — three new kinds, four new fields, both mapping directions, per-mode validation, and `describeRecurrence` so a Dates task no longer reads *"Every week"*. ✅ **The `Dropdown` is a `Pressable` over RN's core `Modal` — no native dependency, so no rebuild and no `.cxx` risk;** a test reads `package.json` and asserts no picker dep exists. 🔴 **The clearing rule is defended TWICE** — `scheduledDays` cleared on the way in (`recurrenceKindPatch`) *and* forced empty on the way out (`recurrenceFromDraft`), so a UI slip cannot get past both; removing it from both turns **5 tests red**, the end-to-end one failing as `save() → false` (exactly the throw-in-the-user's-face it prevents). 🔴 **Round-trip fidelity pinned across six fixtures** (legacy weekly with **no `repeat` key**, plus all four modes incl. a mixed cell grid and a literal 5th), each asserted three ways, **plus a DB-level test that the raw `recurrence_pattern` string is byte-for-byte unchanged** after opening and saving a legacy row — Jason's three live recurring tasks are safe. Mutating Weekly to emit `{mode:'everyWeek'}` turns 5 tests red. **1199 tests / 93 suites**, `tsc` clean, `eslint` unchanged; **engine, domain types, schema and dependencies all untouched — coordinator-verified, not asserted.** ⚠ **Three deviations + three judgement calls — see Open rulings owed.** ⚠ **Gap flagged, not fixed (out of scope):** `src/llm/extraction/mapper.ts:35` still cannot emit a `repeat`, **so voice/chat capture can only produce weekly** — the editor is the only way to reach the other three modes. ⚠ The one thing only a device settles: whether a 36pt grid cell is comfortable to tap. Report: `docs/eval/task46_phase2_findings_report.md` · Brief: `docs/briefs/recurrence_editor_task_46_phase2.md`. *(Phase 1 amendment record follows.)* ✅ **AMENDED 2026-08-24 (ruled by Jason) — `ordinal` now carries `cells: Array<{ordinal, weekday}>` instead of an `ordinals × scheduledDays` CROSS PRODUCT**, because the ruled editor control is a **6×7 checkbox grid** (Sunday–Saturday × 1st/2nd/3rd/4th/5th/Last) where each ticked cell is one occurrence — and a cross product **cannot** express *"1st Monday + 3rd Wednesday"* (it yields four occurrences, not two, so the grid would fill in cells nobody checked). Cells are a **strict superset**; still free-form JSON, **still no migration**. **No data risk, independently verified:** exactly two places construct a `scheduled` recurrence (`taskDraft.ts:240`, `mapper.ts:35`) and **neither can emit a `repeat`**; no row anywhere contains `"mode":"ordinal"`. 🔴 **`Ordinal` gains a literal `5` alongside `'last'`, pinned as DIFFERENT** — in five-Wednesday September 2026 both resolve to 30 Sep; in four-Wednesday August `'last'` lands on the 26th while the literal 5 yields **an empty month** (asserted as a whole-month set, not inferred from a next-occurrence). 🔴 **That literal `5` exposed a real correctness bug:** `nextMonthlyOccurrence` scanned **three** on-months on the reasoning that two always suffice — **false for a 5th weekday**, where up to four consecutive months lack one and *"the 5th Sunday of February, every 12 months"* has a measured **forty-year** worst case. Now bounded at **600 on-month attempts** so the horizon scales with the stride (50 years monthly, 600 yearly), guarded by a test whose first hit is **eight on-months out**. **The invariant is one shared rule** (`monthModeScheduledDaysIssue`): `scheduledDays` is for `everyWeek`/`interval` only, enforced at both writers and **degrading safely on read**. **The drift test's intent is preserved exactly** — every assertion unchanged. **1128 tests / 92 suites**, `tsc` clean, `eslint` unchanged — coordinator-re-verified. **Test-first: 30 failures before implementation, all ordinal-related** — `everyWeek`/`interval`/`dayOfMonth` stayed green throughout, which is itself the evidence their semantics were untouched. **Deviations: none** (two unruled builder calls noted: the 600-attempt horizon and the `OrdinalCell` name). ⚠ **Note for Phase 2, recorded without back-editing:** "ordinal chips" is superseded by the 6×7 grid, and switching *into* `ordinal` must clear `scheduledDays`. Reports: `docs/eval/task46_phase1_findings_report.md` + `docs/eval/task46_phase1_amendment_report.md` · Briefs: `docs/briefs/recurrence_modes_task_46.md` + `..._phase1_amendment.md`. *(Original scope follows.)* **Numbered 2026-08-17** — it was an unowned handoff with no number since task 24. The recurrence editor's `scheduled` union carries weekdays only, so an interval control had nowhere to write and was dropped. **Task 36 explicitly did NOT fold it in** — its report says *"'Every N weeks' intervals are still not representable"* — correctly, since brief §2's scope never listed it. **Needs no migration:** `recurrence_pattern` is free-form JSON, so an `interval` field is additive. What it needs is a UI control, **an anchor date to count intervals from**, and one new `period.ts` function. Must respect constraint #5 (the R8 accrual gate is a start condition, not a cap) and constraint #7 (`null`/one-off ≠ `unscheduled`). Small, headless, file-disjoint from the capture chain. | 36 | **Sonnet** |
| **47** | ⬜ Undone | **Schema snapshot catch-up** | **Numbered 2026-08-17** — unowned since task 34. `docs/reference/ADHD_Task_Management_App_Database_Schema_v2.5.sql` is **two migrations stale** (005 `session_runtime` + 006 `recurrence_period`), and **task 44's migration 007 will make it three**. ⚠ **Sequence AFTER 44** so this is one catch-up rather than two. Regenerate the snapshot from the applied migrations, not by hand-editing the old one. ⚠ **Do not "fix" the spec-version/schema-version mismatch** — spec is v2.4, schema will be 2.8.0, and they track different things (product rulings vs applied DDL); they are *supposed* to drift. The snapshot lag is the real gap. Until this lands, **read the migration files for actual DDL, never the snapshot.** | 44 | **Sonnet/Haiku** |
| **48** | ✅ **Done 2026-08-18** | **Grammar primitive hardening** | ✅ **Landed `9c65e80`.** `boundedStringRule` now emits the hardened shape. **`min ≥ 1`** → `firstChar jchar{min-1,max-1}` (bounds preserved — spans exactly min..max). **`min = 0`** → an explicit alternation `"\"\"" \| "\"" firstChar jchar{0,max-1} "\""`, because an empty string cannot be routed through `firstChar`; collapses to just the empty-string literal when `max = 0` too. New `FIRST_CHAR_RULE_NAME`/`FIRST_CHAR_RULE` exports mirror the existing `JCHAR_RULE` convention — the rule is **referenced by name** and the caller defines it once, rather than emitted inline per field (which would duplicate the definition and risk a parse error). Re-exported from `src/llm/index.ts`. **The one stale exact-match test was replaced by four pinned cases** plus a block pinning the new exports — exact string equality throughout, a tightening not a weakening. `boundedStringRule` has no callers outside `primitives.ts`/`index.ts`/their tests, so `buildGrammar.test.ts` and `ruleNaming.test.ts` needed no change. **Coordinator-verified: 872 real tests / 75 suites** green, `tsc` clean, `eslint` unchanged. **Deviations: none, written out explicitly.** ⚠ **Believed, not confirmed** — the `min = 0` alternation is a novel shape never parsed by real llama.cpp → task 32. Report: `docs/eval/task48_findings_report.md`. **Numbered 2026-08-17** from task 37's residue. **`boundedStringRule` in `src/llm/grammar/primitives.ts` still emits the shape task 37 removed from all four checked-in grammars** — `${ruleName} ::= "\"" jchar{min,max} "\""`, with no `firstChar`. It is exported from `src/llm/index.ts`. **Not a live bug today** (all four `.gbnf` files are hand-authored and nothing generates a grammar from it at runtime) — but **a fifth grammar authored with it would silently reintroduce the separator hole**, and `freeTextFirstChar.test.ts` only guards the `.gbnf` half. ~15 minutes plus a test. Must keep constraint #2 (no underscores in rule names) and must not break `buildGrammar`'s dynamic-slot substitution. ⚠ **Staged behind task 14, before 41 Phase 2** (Jason, 2026-08-17). | 37 | **Sonnet** |
| **49** | ✅ **Done 2026-08-22** | **Widen `prevent_circular_dependencies` to real cycles** | ✅ **CLOSED — migration 008 / schema 2.9.0.** A plain `DROP TRIGGER` + `CREATE TRIGGER` whose `WHEN` clause is a `WITH RECURSIVE` walk from `NEW.depends_on_task_id` aborting if it reaches `NEW.task_id`. 🔴 **`UNION`, not `UNION ALL`** — load-bearing: a DB that already holds a cycle would send a `UNION ALL` walk round the loop forever. The **abort string is unchanged** because `dependencies.ts` regex-matches it to raise `CircularDependencyError`. ✅ **NO table rebuild** — a trigger is not a column; *the board row over-broadened constraint #12 and that was corrected in the brief* (same shape as task 44 deviation 2). 🔴 **Verified on FOUR SQLite builds, not inferred** — including **op-sqlite's own bundled amalgamation (3.51.3) compiled locally and driven through the C API**, because a parse divergence there is a **device-only** failure and this task has no `P`. Also better-sqlite3 3.53.2, `node:sqlite` 3.51.2, Android CLI 3.50.6 — identical results; no `SQLITE_OMIT_*` flags in this project. Legitimate **diamonds** (`A→B`,`A→C`,`B→D`,`C→D`) still insert. Perf: 2000-node chain ≈13 ms total, worst single insert ≈1 ms; no new index needed (001's UNIQUE covers the seek). **The two deliberately-pinned bug tests were handled, not deleted:** `findBackEdge`'s keeps its assertion (it is the *repair* half, retitled); the DB-level one **splits** into "the trigger now REJECTS" + "breaks a **PRE-EXISTING** cycle", the latter preserving the original repair assertions against a cycle seeded around the trigger via a new `seedPreExistingCycle` helper. **Mandated prior-suite sweep: 12 suites / 24 tests went red** and were updated across 002–007, `index.test.ts`, backup/restore/salvage. **1026/88 green**, `tsc` clean, `eslint` unchanged — coordinator-re-verified. **Deviations: none** (one in-scope addition — a salvage test + shared fixture — fell out of the sweep). ⚠ **Surfaced, not built: the trigger is `BEFORE INSERT` only**, so an `UPDATE` can still create a cycle — unreachable from app code today (no `UPDATE task_dependencies` anywhere in `src/`) and already `validateConsistency`'s job; recorded in 008's header and the report §8. Report: `docs/eval/task49_findings_report.md` · Brief: `docs/briefs/circular_dependency_trigger_task_49.md`. *(Original scope follows.)* **Numbered 2026-08-18**, found in passing by task 14 Phase A and **pinned by a test that asserts the bug** (`consistency.test.ts`). **Migration 001's `prevent_circular_dependencies` trigger only catches cycles of length TWO** — its `WHEN` clause tests for a single row that is the direct reverse of the one being inserted. **`A→B→C→A` inserts cleanly today**, with foreign keys and triggers fully on. **Why it matters beyond tidiness:** U1's dependency-blocked pre-filter (task 25, mandatory) holds blocked tasks out of the ranked pool — so a 3+ cycle means every task in it is **permanently filtered from every session**, while uncapped neglect (constraint #5) keeps raising scores that never surface. `validateConsistency` (task 14) is currently **the only thing in the tree that can see a cycle of length ≥3**, and only when something calls it. **Needs a migration** (a CHECK/trigger change → constraint #12's full-rebuild discipline + sweep prior migration suites). ⚠ **Take the next free number after 44's 007** — do not contend for 007. | 2, 25 | **Opus** |
| **50** | ✅ **Done 2026-08-22 — 31's gate LIFTED** | **Pin the `energy` field definition** | ✅ **SETTLED 2026-08-22** (Jason + Opus) — `docs/design/energy_definition_task50.md`. **Ruling:** `energy` = the cost to get the task done (make yourself START it + get THROUGH it, **take the higher**) = **activation cost**; **never null** (extraction always emits low/med/high → 1/3/5, every gold carries a value); the idiosyncratic half is out of extraction scope and learned via §5.4's internal **2/4** discounting — which this makes **load-bearing, not a formality**. Escape hatch (mark non-critical) **NOT taken** — energy is a defined, in-scope field for the bake-off. **No schema / grammar / migration change** (grammar keeps `null` permissive; the guide forbids it and golds never contain it). 🔴 **Big correction it surfaced (§6a): the "6–14/16 on every model" figure is INVALID as evidence** — the 16-fixture bank was thermal-test scaffolding whose expected-value column was *model-generated*, never authored as ground truth; stop quoting it as a capability floor or a measure of ambiguity. Task 50 was still right (the definition genuinely was ambiguous — the guide said "only if stated" vs the real never-null intent; three meanings, no tie-break) — the *number* was never the reason. **§7 actions:** fieldGuides.ts §2a **applied + committed (`aa9e508`)**; the line-45 reword + ~60-token budget recovery → **task 52**; task 31 brief augmented with the rubric + gold constraints; report/brief annotation + the §8 symmetric-factor fork flagged to Jason. *(Original description follows.)* **Numbered 2026-08-18 by Jason's instruction**, and it **gates task 31**. `energy` scored **6–14/16 on EVERY model measured, including Bonsai** — when all of them fail a field at that rate the cause is an ambiguous field *definition*, not weak models. A LoRA would paper over it; a clearer definition fixes it for free, with no migration. 🔴 **Why it must precede 31 rather than run beside it: writing 100+ golds against an ambiguous definition bakes the ambiguity in permanently**, and the collection window closes at open beta (43) — there is no second pass. **Deliverable: one paragraph a human and a 4B read the same way**, plus a decision on whether internal energy 2 and 4 (app-assigned behavioural discounting, never user-entered) are in scope for extraction at all. Must project through `scales.ts` (constraint #6). ⚠ Note task 7 Phase B **deliberately did not tune this**: gold expects `high` for "hit the gym" but `null` for "take out the trash", and it judged chasing a subjective oracle on a non-critical field to be over-fitting — **so the gold set itself may be part of the ambiguity.** **Format: Jason and Opus hammer it out together**, not a document handed over. | — | **Jason + Opus** |
| **51** | ⬜ Undone | **Spec §8.4 amendment — the backup scheme** | **Numbered 2026-08-18.** Jason ratified task 14's **D1** (fixed working path, snapshot to a rotating slot) and **D2** (two slots, not one) — **so the code is settled and `docs/reference`'s spec §8.4 is now wrong.** It still reads *"copy DB at session start; work on the copy; keep prior as backup"*, describing a scheme that **cannot be built on this tree** (no JS file write ⇒ no pointer to a moving working path) and a backup count that is no longer true. 🔴 **Not cosmetic:** every worst failure in this project came from a document and the tree disagreeing. A future session reads §8.4, opens `src/services/backup/`, and either files working code as a bug or "fixes" it toward something unbuildable — the same shape as `WorkScreen.tsx` citing a brief that said nothing, pointed the other way. **Carry the mechanism, not just the new wording**, so the amendment cannot be re-reverted by someone re-reading the old sentence. **Also folds in `session_ended_early`** as a §7.2 addition (ruled 2026-08-18). ⚠ **Pair with task 47** — one pass over `docs/reference/` after 44's migration 007. | 14, 44 | **Sonnet** |
| **52** | ✅ **Done 2026-08-22** | **Extraction-guide budget + enumeration pass** | ✅ **CLOSED.** (a) Line 45 now reads *"This and energy are the only two fields you may judge for yourself"* — the enumerated exception stays **closed at exactly two**, and every other field's `null`/`[]` guidance is untouched (the abstention doctrine held). (b) `context_tags`/`tool_requirements` trimmed of filler only — the *"don't list every tag"* instinct, the 0–2 guidance and the `["home"]` vs `["home","office","phone"]` calibration example all survive, the example **byte-for-byte**. ⚠ **Deviation, disclosed: recovery was ≈−11 tokens, not the ~60 target.** The JSON-bracket example is likely the biggest real cost and the brief protects it deliberately, so the builder held *"trim wording, never semantics"* as the harder constraint rather than chase the number — coordinator concurs. **No BPE tokenizer was available, so the figure is a word-count proxy.** 🔴 **Consequence: the extraction guide remains meaningfully over its ~250-token budget (strategy §5.2) — open for Jason** (accept it, or let task 31/20 measure whether the longer guide actually costs anything). Test-first **carve-out stated, not silent**: prompt-string wording only; no test asserts the changed prose. Report: `docs/eval/task52_findings_report.md`. *(Original scope follows.)* **Numbered 2026-08-22** from task 50's §7. Applying task 50's energy definition (§7.1, committed `aa9e508`) left two cleanups: **(a)** `fieldGuides.ts:45` still calls `estimated_duration_minutes` **"the ONLY field you may guess"** — now contradicted by `energy` ("ALWAYS pick one"); reword so the **two** judged fields are enumerated together, preserving the **closed enumerated-exception** shape (every other field: null/[] are correct — the abstention doctrine). **(b)** the new energy line added **~60 tokens** to a guide already over its ~250-token budget (strategy §5.2); recover it by tightening `tool_requirements` + the `context_tags` example (task 50 design §3's named sources), **trimming wording, never semantics** (`context_tags`'s "don't list every tag" instinct is load-bearing vs the `must_include` subset trap → 20). No grammar/schema/mapper change. ⚠ **Headless edit; real no-regression confirmation rides with task 31's eval** (the 16-fixture answer key is known-invalid, task 50 §6a — not a valid oracle) and/or the next device session (32). Brief: `docs/briefs/extraction_guide_budget_task_52.md` | 50 | **Sonnet** |
| **53** | ✅ **Done 2026-08-22 (audit complete)** | **Test-suite integrity audit — are tests confirming bad code?** | ✅ **AUDIT DELIVERED** — `docs/eval/test_audit_task53_findings.md`. **Method was run, not opined:** **33 mutations, each confirmed against the full 973-test suite** via a harness that applied one substitution, ran jest, and reverted under a `trap`; a control mutation proved the harness detects breaks. **18 caught, 15 survived.** **12 confirmed weak** (9 demonstrated by surviving mutation, 3 structural), **3 known-bug pins correctly flagged** (left alone), **11 areas demonstrably fine**, 1 handed to 20/31/40. 🔴 **Two suite-wide POSITIVES:** zero `expect(true)`-class tautologies and **zero snapshot tests** — taxonomy classes 1 and 4 are essentially absent; the weakness is concentrated in class 2 (passes against a stub) and 3 (assertion can't fail on what it names). 🔴 **W1 (top, coordinator-verified by reading the source):** `defaultAcceptSalvage`'s **rejecting branch has no test** — `return tasksRecovered && report.taskRowsRecovered > 0` → `return tasksRecovered` passes 973/973. Three tests look like they cover it: `:68` hits only the accepting branch (400 tasks), `:123` injects the seam, and `:97` — *titled* "falls through to restore when the salvage recovers nothing worth keeping" — corrupts the file **header**, so `salvageDatabase` throws and `accept()` is never called. The accepting branch calls `promoteToWorking` and returns, so **restore never runs**: a wrong policy silently replaces a recoverable DB with an empty one while a good backup sits unused. ⚠ **Urgency raised by this session's task 14 §13 wiring — the ladder now runs at every launch, so this is live code, not dormant.** 🔴 **W2:** `scoreTask` can drop `skipCount` from the R6 evidence count with the suite green — **no fixture in `src/scoring` or `src/planning` sets `skipCount` nonzero** (coordinator-verified by grep); a task skipped 20× would read as *no evidence* and stay at the 0.5 prior, on a 23% weight. 🔴 **W5 (systemic):** seven tunable constants (`URGENCY_HORIZON_DAYS`, `BASE_SENSITIVITY_CEILING`, `MISSED_QUOTA_BOOST_MAX`, `BREAK_MINUTES`, `DIFFICULTY_JITTER`, +2) are asserted **against themselves** (or fixtures computed *from* them), so any value change passes — **the remedy already exists in-repo twice** (`DEFAULT_IMPORTANCE_INTERNAL` and all of `src/execution/constants.ts` pin literals and were caught), so it is one extra `expect` per constant, not a rewrite. **Remediation grouping → tasks 54–58** (see those rows). **Audit-only: no test fixed, nothing committed by the subagent, tree left clean.** *(Original scope follows.)* **Numbered 2026-08-22 by Jason's instruction.** Audit the existing jest suite (973 tests / 86 suites) for tests that give **false confidence** — that would still pass if the code were wrong. The complement to the test-first policy (`CLAUDE.md`): that governs new tests; this audits the ones that accreted before the bar existed. Look for: **vacuous/tautological** assertions (a mock's own return, a value against itself); tests that **pass against a stub/no-op** (the **mutation heuristic** — would breaking the code make this fail? task 41's force-kill is the gold standard, *proven* to fail on the intended assertion); **subset / `toContain` / `must_include`** checks that can't fail on **over-production** (task 20 — a model dumping the whole tag vocabulary passes every time); **snapshot/exact-match** tests that ossify output nobody validated (the 16-fixture answer-key class, task 50 §6a); **over-mocked** seams whose double diverges from production (better-sqlite3 vs op-sqlite; task 41's Node-not-Kotlin writer caveat); and tests **pinning known-buggy/placeholder behavior as correct**. ⚠ **Legitimate exception:** a test that *deliberately* pins a known bug **with a flag** is fine — `consistency.test.ts` asserts the length-2-only circular-dep trigger on purpose (task 49); the audit hunts the **unflagged** confirmers. **Method:** prioritize by blast radius (scoring → planning → execution → migrations/runtime → backup/capture → llm validators/grammar), not all 973 blindly; demonstrate weakness by mutation where cheap. ⚠ **Eval-oracle quality (the 16-fixture bank, the `must_include` KPI, corpus golds) is task 20/31/40's domain** — flag and hand over. **Deliverable:** `docs/eval/test_audit_task53_findings.md` triaging tests into *confirmed-weak/misleading* (with the specific "would pass if X were wrong"), *pins-a-known-bug-but-flagged* (list), and *fine*, ranked by blast radius, with a remediation each. 🔴 **Audit only — fixes become their own tasks and are themselves test-first** (strengthen the test to FAIL against the bad code first, prove it, then it guards). Brief: `docs/briefs/test_audit_task_53.md` | — | **Opus** *(Opus 5 for the audit methodology / mutation heuristic)* |
| **54** | ✅ **Done 2026-08-22** | **Guard the recovery ladder's salvage-rejection policy (53 W1)** | ✅ **CLOSED 2026-08-22 — the guard exists and is a DEMONSTRATED detector.** New test `rejects a salvage that rebuilds the tasks table EMPTY and restores the backup instead`; **`ladder.ts` byte-identical to HEAD — no production change** (coordinator-verified). 🔴 **Mutation proof:** against `return tasksRecovered;` it fails `Expected "restored" / Received "salvaged"`, and with the intermediate assertions lifted it fails on the consequence itself — `Expected 400 / Received 0` task rows. **974/86 green** (+1), `tsc` clean, `eslint` unchanged — all coordinator-re-verified. ⚠ **Deviation (disclosed, ratified-by-need): the brief's suggested fixture did not work and was measured, not assumed** — after `DELETE FROM tasks` the corrupted tail is freelist, so `integrity_check` **passes** and the ladder returns `healthy`; all four file-corruption modes either miss the policy or make salvage throw (the `:97` trap). Shipped instead: real `'lastPage'` damage **plus** `setQueryFault` on the source's task-row reads, leaving the rowid scan working so `copyTable` degrades to the row-at-a-time path, skips all 400 rows, and still reports `tasks` recovered — the true data-loss shape (400 real tasks lost, not zero present). `setQueryFault` is the fixture's own documented seam with precedent in `salvage.test.ts:151,169`. **Self-guarding:** the test asserts `tasks` recovered **and** `taskRowsRecovered === 0` *before* the status assertion, so if a future change makes salvage throw again it fails loudly instead of passing for the wrong reason. ✅ **Misleading title fixed** — `:97` is now *"falls through to restore when the file cannot be salvaged at all"* (no assertion touched). 🔴 **Two policy properties SURFACED, not decided — see Open rulings owed.** Report: `docs/eval/task54_findings_report.md`. *(Original scope follows.)* **Numbered 2026-08-22** from task 53's top finding. `defaultAcceptSalvage`'s **rejecting branch is asserted by nothing** — `return tasksRecovered && report.taskRowsRecovered > 0` → `return tasksRecovered` passes **973/973**. The accepting branch calls `promoteToWorking` (overwrites the working DB) and returns `salvaged`, so **restore never runs** — a wrong policy silently replaces a recoverable database with an **empty** one while a good backup sits unused. `ladder.ts`'s own header names this class: *"the single worst bug this task could ship."* ⚠ **Live, not dormant — task 14's §13 wiring (2026-08-21) means the ladder now runs at every launch.** ⚠ Also **fix the misleading test title** at `ladder.test.ts:97` (*"falls through to restore when the salvage recovers nothing worth keeping"* corrupts the **header**, so salvage *throws* — it tests the exception path, not the policy). **Test-first:** new fixture where `tasks` is recovered but **empty** (seed zero tasks / corrupt a data page, not the header); assert `status === 'restored'`; **confirm it fails against `return tasksRecovered;` before landing.** | 14 | **Opus** |
| **55** | ✅ **Done 2026-08-22 (folded into 17 Phase A)** | **Scoring assertion strength (53 W2 + W5)** | ✅ **CLOSED — both findings guarded, each proven against its named mutation.** **W2:** the `skipCount` wiring is pinned — the audit's exact mutation now yields `Expected: 0.25 / Received: 0.35`. Folded into 17 Phase A deliberately: W2 *is* the "what counts as an attempt" question from the scorer's side, so splitting them risked writer and scorer silently disagreeing. **W5: 7/7 constants caught** — six pinned **behaviourally** with literals (the urgency horizon straddled at literal 7 and 14 days; the major-task threshold at literal 24 vs 25 minutes), copying the in-repo remedy rather than inventing one. ⚠ `DIFFICULTY_JITTER` is **value-pinned only**, and is commented as **not** a substitute for W6's behavioural guard (→ task 56). *(Original scope follows.)* **Numbered 2026-08-22.** **W2:** `scoreTask` can drop `skipCount` from the R6 evidence denominator with the suite green — **no fixture in `src/scoring`/`src/planning` sets it nonzero** (verified). A task skipped 20× reads as *no evidence* and stays pinned at the 0.5 prior instead of converging to its real low success rate, on a **23% weight across every ranking**. Fix: one test with `completionCount: 2, skipCount: 8` asserting the factor equals `historicalSuccessFactor(rate, 10)`, not `(rate, 2)`. **W5:** seven tunable constants are asserted **against themselves** (or fixtures derived from them), so any value change passes silently — these are *design decisions* (`factors.ts` marks several `REVIEW(task10): a reasoned starting horizon, not a measured one`). 🔴 **The remedy exists in-repo twice** — `DEFAULT_IMPORTANCE_INTERNAL` pins the literal `0.5` alongside the symbol (and was **caught**), and `src/execution/constants.ts` pins literals throughout (**4/4 caught**). One extra literal `expect` per constant, not a rewrite. Test-first against the exact mutations in the findings doc. | 53 | **Opus/Sonnet** |
| **56** | ✅ **Done 2026-08-22** | **Planner assertion strength (53 W3/W4/W6/W7/W9)** | ✅ **CLOSED — all five mutations are now proven detectors**, each watched red and reverted. 🔴 **`planner.ts` is untouched and NONE of the five was a behaviour bug** — the planner was right in every case; only the tests were blind. **W3** re-sized to 150 min so only the `>= 2` limit can stop the third task (`>= 3` → `Received: [1, 2, +3]`); **W4** added a 50-min top-ranked task rejected solely because 50 > 45 work-minutes, plus a 45/46 boundary case; **W6** a **200-seed** mean-index *plus* adjacent-swap assertion — false under **both** the sort-reversal (mean 0.27→1.73) and `jitter=0` (swaps→0), and tolerant of the jitter **by construction**; **W7** a minute-budget assertion (`<= 30`, `Received: 35` when the break goes uncounted); **W9** a fixture where insertion order and score order genuinely disagree. ⚠ **Deviation, disclosed — and it is a correction to task 53's own recommendation:** the audit's W3 remedy (*"three 15-minute major tasks"*) **is impossible** — 15 is below `DEEP_FOCUS_MAJOR_MIN_MINUTES` (25), so all three would be skipped as non-major and nothing placed. Intent preserved by enlarging the session instead. *(A findings report's **recommendations** deserve the same scrutiny as its findings — this brief had passed it through unexamined.)* Report: `docs/eval/task56_findings_report.md`. *(Original scope follows.)* **Numbered 2026-08-22.** Five planner guards that name a spec rule and don't measure it: **W3** *"at most two major tasks"* is enforced by **capacity, not the limit** (`>= 2` → `>= 3` survives — the fixture's third task is rejected by `isPlaceableInBlock`); **W4** the **25% deep-focus overrun buffer can be deleted entirely** (`workMinutes = blockMinutes` survives — the only discriminating assertion is a 40-min task that fits in both 45 and 60); **W6** the §5.3.2 **difficulty gradient is entirely unguarded** — both *reversing* the easy→hard sort and setting `DIFFICULTY_JITTER = 0` survive; **W7** the pre-deep-block break isn't counted against front-section capacity; **W9** the equal-energy group tie-break is unguarded. Each remediation is named with its exact mutation in the findings doc (re-size fixtures for headroom; a seeded statistical assertion for the gradient). Test-first. | 53 | **Opus/Sonnet** |
| **57** | ✅ **Done 2026-08-22** | **`src/capture/retention.ts` — no test file at all (53 W10)** | ✅ **CLOSED — 10 tests, both mutations proven to turn them red.** Rotation-deletes-the-newest-day now fails loudly (the module's own rule: *"never the newest, which is what you are debugging"*), and `CAPTURE_WARN_BYTES` is pinned with the **literal** `429496729` plus a boundary pair, per W5's lesson that a constant asserted only against itself pins nothing. Also covers stop-once-under-ceiling, no-rotation-under-ceiling, a throwing `sizeOnDisk()`/`deleteDay()` not propagating (with the `lifecycle.capture` health record still emitted), the pending/dismiss warning state, and no-writer→`null`. 🔴 **It avoided an over-mocked-seam trap of exactly the kind task 53 catalogued:** the existing fakes in `record.test.ts`/`mutationCapture.test.ts` hardcode `sizeOnDisk: () => 0`, which would have hidden **every** rotation behaviour — so its fake is backed by a real mutable `day → bytes` map that actually shrinks after `deleteDay()`. **No production code touched** (coordinator-verified by diff). **Deviations: none.** Report: `docs/eval/task57_findings_report.md` · Brief: `docs/briefs/capture_retention_coverage_task_57.md`. *(Original scope follows.)* **Numbered 2026-08-22.** An **absent** test rather than a confirming one, surfaced by the same mutation pass: `src/capture/__tests__/` has `availableBytes`, `forceKill`, `mutationCapture`, `record`, `sha256` — **no `retention`**. Two mutations survived 973/973, including one the file's own comment **forbids**: dropping `.slice(0, days.length - 1)` so **rotation deletes the newest day**, against retention.ts's explicit *"never the newest, which is what you are debugging"*; also `CAPTURE_WARN_BYTES` 80% → 20% of the ceiling. Matters because the **512 MB ceiling is the bound task 14 reasons about** when it treats `capture/` as reclaimable space (and 41/43 fill `reclaimSpace`). Small unit suite over a fake `CaptureWriter`: rotate-oldest-first, never-the-newest, the warn threshold. Test-first. | 41 | **Sonnet** |
| **59** | ✅ **Done 2026-08-22** | **Fix the realm-fragile error assertion (the reproduced flake)** | ✅ **CLOSED — the suite is no longer order-fragile.** Took the **boundary** fix, not the narrow one: `wrapDatabase` now normalises driver errors into a **real `Error` of the current realm**, preserving `message`/`name`/`code`/stack and passing genuine same-realm Errors through **by identity** (so `toBeInstanceOf(NoSpaceError)` and `isDiskFullError`'s `.code` check are untouched). 🔴 **`consistency.test.ts` was NOT edited** — the assertion still asserts exactly what it did; the infrastructure stopped lying to it. The narrow fix was rejected because it repairs one call site and **leaves the trap armed for the next `.rejects.toThrow()` against a driver error**, which would resurface as another mysterious order-dependent flake. 🔴 **Made DETERMINISTIC rather than hoped-for:** new `foreignRealmError.ts` manufactures a genuine second realm via `vm.runInNewContext`, so the new `sqliteTestConnection` suite reproduces the mechanism on demand (**3 red before the fix, 6/6 after**) instead of depending on suite order. **Proven not to weaken the test:** reverting migration 008's trigger to the length-2 form makes consistency-last fail with a *different* message (*"resolved instead of rejected"*), i.e. it still catches a genuine non-rejection. ✅ **Coordinator-verified: 4/4 green under a consistency-LAST sequencer — the exact ordering that was 6/6 FAIL before the fix.** **1038/1038, 89 suites**, `tsc` clean, `eslint` unchanged. ⚠ **Residual, stated honestly:** `conn.raw` and any directly-constructed `Database` still bypass the normaliser — in practice those call sites are synchronous, and the sync `expect(() => …).toThrow()` form never consults `isError`. ✅ **`dependencies.ts:52` needs no hardening** — established by measurement + mutation (replacing its `String(err)` fallback with `""` makes it fail), and the quirk is *inferred* not to arise in the app at all: production is op-sqlite in a single Hermes realm with no module-registry reset. **Deviation: one, disclosed** — also normalised `fileDbOperations.ts`'s `openError`, one line reusing the same helper, test-only. Report: `docs/eval/task59_findings_report.md`. *(Original scope follows.)* **Numbered 2026-08-22** from the unnumbered housekeeping pass, which **reproduced and diagnosed** a flake the coordinator had seen once and lost. 🔴 **The failing test is always the same one:** `consistency.test.ts` → *"the schema trigger now REJECTS a three-task cycle outright (migration 008, task 49)"*. **It is ORDER-DEPENDENT, not load-dependent** — load was *excluded* (0/10 failures under CPU load; failing runs were **faster** than passing ones, and it is an assertion failure, not a timeout). Controlled A/B with a command-line sequencer: **consistency first = 6/6 PASS, consistency last = 6/6 FAIL.** *(Jest 29 reruns previously-failed suites first, which is why the run right after a failure always passes — the coordinator hit exactly that alternation twice.)* 🔴 **Mechanism, instrumented not guessed:** `better-sqlite3`'s `SqliteError` is a **hand-rolled pseudo-Error** (`Object.setPrototypeOf(SqliteError.prototype, Error.prototype)`) with no `[[ErrorData]]` slot, and `database.js:59` hands it to the **native addon, which Node caches per PROCESS, not per Jest realm**. So `err instanceof Error` is **true** when the suite runs first and **false** when it runs later, and Jest's `isError()` falls through to a cross-realm `instanceof`. **Migration 008 and the product code are correct — the assertion is fragile.** Explains why `008_transitiveCycleGuard.test.ts` never flakes: it uses the synchronous `expect(() => …).toThrow()` form, which never consults `isError`. **Fix (test-first — reproduce with the sequencer FIRST):** narrow = assert the rejection *value* rather than `Error` recognition at `consistency.test.ts:89-91`; better = normalise driver errors into a real `Error` of the current realm in `wrapDatabase` (`src/db/testUtils/sqliteTestConnection.ts`). ⚠ **Related, and worth a look while in there:** `dependencies.ts:52` uses the same `err instanceof Error` check **in production** — safe today only because its `String(err)` fallback still yields the matched message. Report: `docs/eval/housekeeping_2026-08-22_report.md` §Part B. | 49 | **Opus/Sonnet** |
| **58** | ✅ **Done 2026-08-22** | **Test-hygiene sweep (53 W8/W11/W12)** | ✅ **CLOSED.** **W8:** `contextGroupKey`'s NUL-sentinel claim is now asserted and **proven red** against `'\x00flexible'` → `'flexible'`; the sentinel value itself is untouched (it is a `Map` key, never persisted — and the reason `score.ts` was binary to git until 2026-08-01). **W11 (carve-out, stated):** `blockKindsAgree` kept, renamed to say what it really is, with a comment that the **type annotation** is the guard, `tsc --noEmit` the only enforcer, and 🔴 **a jest-only CI path would silently lose it.** **W12 (carve-out, stated):** recorded in `migrations/index.ts` that each migration's bump is guarded by the **next** migration's legacy fixture, so the last entry is always unguarded — **today that is 008**; a direct guard is *proposed in the report, not built*, since it would change the sweep convention. **No production behaviour change** (`score.ts` untouched; `migrations/index.ts` comment-only — both coordinator-verified by diff). **Deviations: none.** Report: `docs/eval/task58_findings_report.md`. *(Original scope follows.)* **Numbered 2026-08-22.** Three small items. **W8:** `contextGroupKey`'s NUL escape makes an explicit behavioural claim in its comment (*"a task tagged literally `flexible` never merges into the no-tags group"*) with **zero tests** — `'\x00flexible'` → `'flexible'` survives; one test with `contextTags: ['flexible']` vs `[]`. **W11:** `blockKindsAgree` is a **compile-time guard dressed as a runtime assertion** — the real enforcement is the *type annotation*, but jest runs through **babel-jest (types stripped)**, so `npx jest` **cannot** detect the drift the test appears to guard; only `tsc --noEmit` can. Rename + comment so a green jest run is never read as proof (⚠ a jest-only CI path would silently lose it). **W12:** migration forward-sweep is sound **by construction** — each migration's version bump is guarded by the *next* migration's legacy fixture — but the **last** migration in `MIGRATIONS` has no downstream guard, so when 008 lands, 007 becomes guarded and 008 does not. A note in the migration-authoring convention, not a code change. | 53 | **Sonnet** |

---

## Branch state (new section — this is what went wrong)

| Branch | Carried | Status |
|---|---|---|
| **`main`** | everything below, merged | ✅ **Pushed; `origin/main` is level (2026-08-21).** 973 tests / 86 suites green; carries the board hygiene, task 14's §13 wiring, and the task 50 brief + orientation refresh. *(First-ever green was `fc59b83` / 794 tests, 2026-08-17; the merged tree ran on the S23 FE 2026-08-19.)* |
| `claude/interesting-shirley-e10fa1` | the `score.ts` NUL-byte fix | merged (fast-forward). ⚠ Still checked out as a **stale worktree** — see the standing note. |
| `task-36-recurrence-period-engine` | 35 + 36 + a docs WIP commit | merged. Local ref still ahead 1 (a WIP commit); nothing on it is needed. |
| `opus/batch-a-headless` | the 37-commit six-model spike | merged |
| `coordinator/sessions-origin` | `sessions.origin` ruling, task 8 retirement, task 14 rewrite, `score.ts` correction, handoff refresh (6 commits) | ✅ **merged 2026-08-17** (`67c0618`, not a fast-forward) and branch deleted. |
| `coordinator/tasks-41-42` | at `a5d4107`, an ancestor of `main` | fully contained in `main`; deletable. |

**Housekeeping — done 2026-08-17:** `_probe` / `_probe2` deleted; `coordinator/sessions-origin` deleted after merge; the stale `.git/*.lock` files are gone; **`.gitattributes` added** (`* text=auto eol=lf`, commit `fc59b83`).

✅ **RESOLVED 2026-08-22 — the stash was TAGGED, then the `gc` ran.** Jason approved the coordinator's recommendation to keep both: `git tag archive/stash-2026-08-17 f1ff7082` made the stash **reachable** (verified: `fsck` reported nothing dangling *before* the gc), then `git gc --prune=now`. **Result: 2509 loose objects → 0 (2518 in-pack), `.git` 15 MB → 3.9 MB, `fsck` clean, and the stash is intact and readable via the tag** (all 7 files still show). ⚠ **The 17 `incoming-*` dirs SURVIVE — `git gc` does not touch them** (they are staging areas from interrupted fetches). 20 of their objects were spot-checked and **all resolve from the pack, so they are redundant**, ~1.2 MB. **Deliberately left in place:** hand-deleting inside `.git/objects` is precisely what the note below warns against, and 1.2 MB against a 3.9 MB repo does not justify the risk. *(Prior state follows.)* 🔴 **2026-08-22 — `git fsck` was NOT clean, which is why `gc` had not run.** The housekeeping pass stopped at its own gate: `fsck` reports **`dangling commit f1ff7082`**, which is **a `git stash` of Jason's from 2026-08-17** ("On main: pre-merge: sessions-origin content already in worktree", 7 docs files). **`git gc --prune=now` would have destroyed it permanently** — the gate earned its keep. **Awaiting Jason's call (see Open rulings owed).** Also measured: the repo has **never been packed** — `in-pack: 0`, 2509 loose objects, 128 `tmp_obj_*` files, 17 `incoming-*` dirs, `.git` ≈15 MB; a `gc` sweeps all of it **once the stash decision is made.** *(Original note follows.)* ⚠ **`rm -rf .git/objects/incoming-*` was NOT done, and the instruction to do it rested on a false premise.** Those 17 dirs were described as harmless leftovers — but **15 of them contain loose objects** (1.2 MB total), not nothing. `git fsck` is clean and does not complain about them. Deleting object directories by hand is how you corrupt a repo; **`git gc --prune=now` is the correct instrument** and will sweep them safely. Left in place deliberately.

**Line endings — resolved.** The old note here said the working tree's ~220-file diff reduced to two real files. That was **stale and would have been dangerous to act on**: by 2026-08-17 the tree held **seven** files of real, uncommitted content (205 insertions) — the entire `coordinator/sessions-origin` doc set, applied to the working tree but never committed. `git checkout -- .` at that point would have discarded it. It was preserved by hash-comparing every file against the branch (all seven matched), stashing, and merging. **Adding `.gitattributes` then staged zero renormalization changes — the index was already LF throughout**, so the churn was purely a working-tree artifact.

---

## Standing notes (not tasks)

- 🔴 **`src/scoring/score.ts` was a *binary* file to git from its first commit (`8903e74`) until 2026-08-01.** `contextGroupKey`'s no-tags sentinel was a raw `0x00` byte, inside the first 8 KB git samples for binary detection. **Every diff of the scoring composition ever reviewed showed `Bin 8860 → 9177 bytes` instead of lines** — the most-reviewed file in the project never once appeared in a readable diff. Fixed in `db16645`; write-up in `docs/briefs/nul_byte_score_ts.md`. ⚠ **Corrected 2026-08-07 — this overstated the risk.** `db16645` **re-encoded, it did not change**: `contextGroupKey` still returns `'\x00flexible'`, now a source escape rather than a raw byte, so the value is **byte-identical**. It's a `Map` key — never persisted, never displayed — so there was **no behavioural defect and no false positives or negatives, ever**. File reads always worked, so **task 10's composition review stands**. Only **diffs** were unreadable, so the unreviewed surface is *what changed after task 10*: **six commits** (9, 10-R1, 10-R3, 25-U1, 11, 36), **all readable today** via `git show --text`. The ruled constants are pinned by tests (`factors.test.ts` asserts 0.31/0.23/0.23/0.23; `score.test.ts` covers R6's k=2; `noveltyEntropy.test.ts` is the permanent novelty regression). Verification folded into **task 45**.
- ✅ **RESOLVED 2026-08-22 — the worktree duplication is GONE, and `npx jest`'s raw number is now the TRUE number (1026 / 88).** The unnumbered housekeeping pass removed all four worktrees after proving, file-by-file (hash-per-blob, tracked *and* untracked), that **zero** files in any of them were absent from `main`. This **discharges Jason's 2026-08-17 ruling** ("leave it, separate cleanup task later"). 🔴 **The standing "halve any test count from this tree" rule is RETIRED — do not apply it.** ⚠ **The old note below understated the problem by the end:** it recorded raw as 1820/156, but with Wave 1's three agent worktrees the raw count had reached **4842 tests / 416 suites — 4.7× the truth** (run time 147 s → 36 s after cleanup). *(Historic note follows.)* 🔴 **`npx jest` reports 1588 tests, and the real number is 794.** A stale git worktree lives at `.claude/worktrees/interesting-shirley-e10fa1` — a full second checkout of the tree, detached at `d3ead86`, **inside the project directory**. Jest collects its 68 suites alongside `main`'s 68, exactly doubling every count. `d3ead86` is already an ancestor of `main`, so the duplicate is pure redundancy carrying no unique work. **Consequences worth knowing:** the doubled run is a statement about *two different commits*, so a green there does not mean what it looks like; and a future regression on `main` could be masked or duplicated in a way that reads as an unrelated failure. ✅ **RULED by Jason 2026-08-17: leave it in place for now**, flag it in the findings report, and let a separate cleanup task remove it later. *(This overrules the coordinator's recommendation to apply `testPathIgnorePatterns` and/or `git worktree remove` — recorded so the reversal is visible rather than silent.)* **The standing consequence: any test count quoted from this tree needs halving until the worktree is removed.**

**Verified duplicate, not assumed** (`capture_format_task41_amendment_rulings.md` §6): diffing `src/`, `__tests__/`, `package.json` and `jest.config.js` between `d3ead86` and `main` yields **exactly one file** — `src/dev/ModelBaseSpikeScreen.tsx`, present on `main`, absent in the worktree, and it has no test. Suite counts 68 and 68; test content identical. Cost is run time only (~7 s → ~15 s). Nothing is broken.
- ⚠ **There are TWO grammar-text drift guards, not one** — a correction task 37 owed to its own brief. `src/llm/grammar/grammarText.ts` is the one every document names; **`src/dev/extractionGrammarText.ts` holds a second byte-identical copy of the extraction grammar with its own drift test.** A session that regenerates only the named one will fail the suite on a file no brief mentions, and the failure reads like unrelated breakage. Regenerate both.
- ⚠ **→ NOW TASK 48.** **`boundedStringRule` in `src/llm/grammar/primitives.ts` still emits the vulnerable shape** that task 37 removed from all four checked-in grammars — `${ruleName} ::= "\"" jchar{min,max} "\""`, no `firstChar`. It is exported from `src/llm/index.ts`. **Not a live bug today** (all four `.gbnf` files are hand-authored; nothing generates a grammar from it at runtime), but **a fifth grammar authored with it would silently reintroduce the separator hole.** `freeTextFirstChar.test.ts` would catch the resulting `.gbnf`, which is the important half of the defense. Unowned; worth folding into whoever next touches `src/llm/grammar/`.
- **Junk tags split into two root causes** (task 37 §5). `":mixing"` / `":episode"` were the separator hole in milder form and are **closed**. **`"work_on_it_until_did"` starts with `w` — a phrasing failure, not a separator artifact, and is NOT fixed.** Separately: **junk tags already persisted in the DB keep being re-offered forever via `tagKnown`**, since that rule builds a closed alternation from tags the app already holds. The grammar fix stops *new* leading-separator junk entering; it cannot clean what is already stored. ✅ **Data pass FOLDED INTO TASK 44** (Jason, 2026-08-17) — it carries a product question 44 must put to Jason: delete the junk tags, merge them into a real tag, or leave them to age out.
- 🔴 **`"work_on_it_until_did"`-class junk is a TRACKED SIGNAL, deliberately not a task** (Jason, 2026-08-17). **Pinned to tasks 20 and 40 — see those rows.** **The mechanism, so nobody re-files it as a bug:** the field guide is already explicit (`fieldGuides.ts:49` — *"ONE plain lowercase phrase — never punctuation, fragments, or non-English text"*), and task 7 Phase B already tuned it across five on-device iterations (junk tags to ~0, avg tags 3.0 → 1.5). The residual ~2–3/run is the model **disobeying a clear instruction**, not resolving an ambiguous one — which makes it a measure of **model capability, not prompt quality**. **A grammar cannot fix it:** grammars constrain *shape*, and no character class separates `"work_on_it_until_did"` from `"deep work"` — forbidding `_` just moves the failure to `"work on it until did"`. That is the D3.2 split the prompts were built on. ⚠ **Do not spend more prompt budget on it** — the extraction guide is *already over* its ~250-token budget (strategy §5.2), and tuning it further over-fits the incumbent model on exactly the axis the migration decision is trying to measure.
- **Every migration must sweep the *prior* migrations' test suites.** `runMigrations` walks forward, so 00N−1's "latest version" and "full object list" assertions silently become assertions about 00N — surfacing as a failure in an unrelated file that reads like "I broke someone else's test." Confirmed live through migrations 002–006.
- ⚠ **Next free migration is 009 / schema 2.10.0.** *(008 / 2.9.0 landed with task 49 — the transitive cycle guard. Note for whoever writes 009: 008 is currently the LAST migration, so its version bump has no downstream legacy fixture guarding it — task 53 W12. 009 should add one.)* *(Historic:)* **Next migration is 007 / schema 2.8.0 — and it is now claimed by task 44** (`sessions.origin`, ruled 2026-08-07). Task 36 took **006 / 2.7.0**. Whoever else lands a migration first renumbers and re-sweeps. *(The old note here said "next is 006, and if 36 adds one it is 006" — 36 did, and it's landed.)*
- **Spec version ≠ schema version.** Spec is **v2.4** (task 35); on-device `schema_metadata` is **2.7.0** (migration 006). These track different things — product rulings vs applied DDL — and are *supposed* to drift. Don't "fix" that mismatch. ⚠ **But the `v2.5.sql` reference snapshot is genuinely two migrations stale** (005 + 006) — that's a real gap, unowned, worth numbering when someone next touches `docs/reference/`. For actual DDL read the migration files, not the snapshot.
- **`docs/build_allocation.md` is retired** (2026-07-20) — tombstone pointing at orientation §10. Routing logic lives in the handoff §1; per-task allocation lives in this table's Model column. Don't resurrect it. *(Its tombstone still names Fable as the top routing tier; Fable access has ended and Opus 5 inherits that slot.)*
- **`PlanAdjustment` is a stated contract, not an enforced one.** Nothing validates a hostile adjuster; the hook must not resurrect filtered tasks. Task 19 owns the wrapper.
- **The expiry alarm cannot be a JS timer** (device-confirmed; `setTimeout` fires 38–45 s late from doze). Discharged by task 24's `AlarmManager` TurboModule. Never add a "JS backup timer" on this path.
- ✅ **`energy` DEFINED — task 50, 2026-08-22** (`docs/design/energy_definition_task50.md`): it is **activation cost, never null**; internal 2/4 stay app-assigned (learned, §5.4). ⚠ **The old "6–14/16 on every model" claim is retired as evidence** — task 50 §6a found that number rests on the 16-fixture bank, which was **thermal-test scaffolding with a model-generated answer key**, not ground truth. Energy was ambiguous on independent grounds (the written guide said "only if stated" vs the real never-null intent; three meanings, no tie-break), which is why 50 was still worth doing — but the number was never the reason. **Standing rule from §6c:** a model may draft eval *inputs*, never its own answer key → pinned into task 31's gold constraints.

## Open rulings owed

**Ruled 2026-07-20:** extend splits into +5 / hyperfocus; +5 is **never capped**; repeated +5 queues coaching at **3 presses or 50% over, whichever first**; guardrail **B**, hyperfocus only; recurrence engine **split into task 36**; task 35 **commissioned**.

Still open:

| Ruling | Owner | Gates |
|---|---|---|
| ~~**Corpus size for task 31**~~ → ✅ **RULED 2026-08-18: 100+ extraction items.** Jason takes the generous end. **Consequence task 40 must carry: ~50+ held out ⇒ ≈±11 pts** — finally better than the seed set's ±20, and tight enough that Bonsai's 14/16-vs-12/16 lead can actually be tested instead of vanishing into the error bar. **Task 40 must still state the resolution it ran at**, computed from 31's final held-out n. | ~~Jason~~ | — |
| ~~**The `energy` field definition**~~ → ✅ **CLOSED 2026-08-22 (task 50).** `energy` = activation cost, never null; 2/4 app-assigned. `docs/design/energy_definition_task50.md`. | ~~Jason~~ | — |
| `which:"next"` weekday semantics (22) | Opus/Sonnet to propose, Jason to rule | Every resolved date |
| **Multi-day `scheduled` neglect gap** (25 §3.1) — ✅ **reviewed 2026-08-18; no action, left as a known knob.** `scheduled` carries neither `period` nor `quota`, only `scheduledDays`, so task 25 derived `period = 7` and `quota = max(1, scheduledDays.length)`. Single-day weekly → `7/(1+1)` = **3.5 d**, matching the ruled example exactly; Mon+Thu → `7/3` ≈ **2.33 d**. Accepted because it is more faithful to *"half the distance between occurrences"* than hardcoding quota-1, and it errs in the **safe direction** — a shorter gap surfaces a task sooner, and a fail-safe that surfaces early never hides anything. **Revisit only if a real multi-day schedule nags too early.** | Jason (only if it bites) | Nothing |
| ~~`sessions.model_tier`~~ → ✅ **RULED 2026-08-18: leave it NULL.** Ignoring it breaks nothing — the column is nullable, unwritten and unread. **Two reasons not to fill it:** the CHECK vocabulary `('8B','4B','1.7B')` is about to be **wrong** if task 40 picks Gemma 4 E2B (the real value would be *rejected*), and writing `'4B'` unconditionally is a **fiction** for every timer-only session that never loads a model — a fiction task 17 would then learn from. **Task 40's outcome defines what the column means**, at which point it is a migration that also fixes the CHECK. | ~~Jason~~ | — |
| ~~`session_ended_early`~~ → ✅ **RULED 2026-08-18: YES — backing out of a session with time left should coach.** It is already a first-class `coaching_queue` trigger type but **not** one of spec §7.2's five rows, which is why task 24 declined to invent a sixth conversation. **Now a §7.2 addition, folded into task 51**; the conversation content and trigger conditions still need authoring. | Jason ruled; **needs authoring** | Spec §7.2 + one coaching conversation |
| ~~Merged-branch green check~~ → ✅ **DISCHARGED 2026-08-17.** 794 tests green, `tsc` clean, `eslint` 0 errors, on `main` at `fc59b83`. | ~~Jason~~ | — |
| ~~**The stale worktree**~~ → ✅ **RULED 2026-08-17: leave it, flag it in the report, separate cleanup task later.** Halve any test count quoted from this tree until then. | ~~Jason~~ | — |
| ~~**Task 41 §12** — six open questions on the capture format~~ → ✅ **ALL RULED 2026-08-17**, plus two follow-ups (`system` actor, single ceiling warning). `docs/design/capture_format_task41_amendment_rulings.md`. Nothing in the format design waits on a decision. | ~~Jason~~ | — |
| ~~**Task 37 deviation** — `summary.v1.gbnf` edited outside stated scope~~ → ✅ **RATIFIED by Jason, 2026-08-18.** The three summary rules keep the `firstChar` fix. | ~~Jason~~ | — |
| ~~**Task 14 D1** — fixed working path vs spec §8.4's "work on the copy"~~ → ✅ **RATIFIED by Jason, 2026-08-18.** Settled; see orientation §5. | ~~Jason~~ | — |
| ~~**Task 14 D2** — two rotating backup slots vs spec §8.4's one~~ → ✅ **RATIFIED by Jason, 2026-08-18.** Settled; see orientation §5. | ~~Jason~~ | — |
| 🔴 **Task 14 — free-disk-space mechanism (build-cost call).** Nothing in this tree reports free space. **Shipped: attempt-and-catch** (`VACUUM INTO` → catch `SQLITE_FULL`/`ENOSPC` → `NoSpaceError`), which correctly implements the *blocking* rule but gives no advance warning and no number to show the user. **Option 3 is a ~30-line Kotlin `StatFs` TurboModule** — buys "free 40 MB" warnings before the wall; costs a **device build cycle**, which is why the builder recommends **bundling it with task 41's `NativeCaptureLog` rebuild** rather than spending a build on it alone. ⚠ **`setReservedBytes` should NOT be used — a correction to brief §2's preference order:** `SQLITE_FCNTL_RESERVE_BYTES` reserves bytes *inside a database file's pages*; it does not measure free space on the volume and does nothing for the backup, which is a different file. **It would read like a guard and guard something else.** *(Coordinator concurs.)* **Recommend: ship option 1 now, bundle option 3 into 41's rebuild.** | **Jason** | Advance-warning UX, not correctness |
| ~~**Task 41 deviation 2.2** — capture root computed natively~~ → ✅ **RATIFIED by Jason, 2026-08-18.** The boot-time cross-check stands: design §6 rule 6 becomes a *check* rather than an assumption. | ~~Jason~~ | — |
| ~~**`coaching` stream's missing writer**~~ → ✅ **RULED 2026-08-18 — a nomenclature ruling, and it is about avoiding collision, not about renaming anything.** A coaching conversation left without a disposition is **"closed without resolution"**. **The vocabulary map, stated positively so nobody re-derives it:** `sessions.status = 'abandoned'` means *this session did not close cleanly* (crash-truthful, **constraint #14** — a session is *born* `'abandoned'` so a crash leaves the true value on disk); `interactions.completion_status = 'abandoned'` means *this episode did not complete*; **"abandoned" at the TASK level means a task was dropped**, which per **constraint #11** happens **only by explicit user disposition — the app never abandons a task by inference**. A coaching conversation is none of those, so it gets its own word. ⚠ **No schema value is renamed and no migration is implied** — the existing session- and episode-level uses are legitimate and stay exactly as they are. | ~~Jason~~ | — |
| ~~**Task 44 — three deviations**~~ → ✅ **ALL RATIFIED by Jason, 2026-08-18.** **(1) "Missing tools" is not a quick-start warning-screen condition** — tools were never a check-in-time filter for *any* session in this codebase (`session.tools` is the optimistic union of active tasks' requirements; the real check is per-task via `ToolsCheckScreen`, which quick-start reaches naturally). Quick-start instead *ends* the session when tools are declined. A more faithful reading of "a normal session that happens to be one task long" than pre-empting a check the app has never made pre-emptively. **(2) Migration 007 skips the rebuild dance** — correct: `ADD COLUMN` with a *self-referential* CHECK needs no rebuild (SQLite validates only the new column, existing rows go NULL); the dance is only for *changing* a CHECK on an *existing* column. Precedent is migration 003. **Constraint #12 already says "*change* a CHECK", which never covered adding a CHECK-bearing column — the brief over-broadened it to "any"; clarified in handoff §6 constraint #12.** **(3) Edited `src/services/backup/__tests__/`** despite its "not yours" scope line — a mechanical `'2.7.0'`→`'2.8.0'` sweep forced by 007's version bump, coordinator-reviewed line-by-line, no logic or assertion changed. Leaving the suite red was the worse option. | ~~Jason~~ | — |
| 🔴 **Task 41 — FOUR provisional builder calls** (`capture_format_task41_amendment_rulings.md` §8): verbatim `messages`, grammar as id+sha8, **global `v`**, ambient correlation context. **All four shipped in Phase 2 and are writing on device** — including global `v`, which the amendment flagged "not cheaply reversible, decide before Phase 2 writes records." So this is now **ratify-or-revisit, not decide-before-build.** *(The prior row here read "THREE" — an undercount; the amendment lists four.)* | **Jason** | Whether any are revisited before beta |
| 🔴 **Task 44's junk-purge has NEVER run against real data** — `scripts/purge-junk-tags.js` has a dry-run-first mode but has only been exercised against fixtures. **Before the destructive mode touches the alpha DB: (a) take a backup** (task 14's `VACUUM INTO` exists but is NOT wired into the app, so this is a manual/scripted step), **(b) run the dry run and read what it would delete**, **(c) confirm its "junk" definition matches intent** — it targets the leading-separator class only and lists ambiguous tags rather than deleting them. ✅ **DRY RUN DONE 2026-08-19 against the real pulled DB: 0 leading-separator junk found.** 🔴 **Scope gap surfaced — needs Jason:** the purge targets the `:mixing`/`:episode` leading-separator class, which has **zero instances** in the alpha DB. The ONE junk tag actually present is **`use_breath_pause`** — the snake_case/phrasing class deliberately scoped OUT (it is the *tracked signal* pinned to 20/40, because nothing automated distinguishes it from a real tag). **So the purge as ruled cleans a class that isn't there, and the visible clutter is the class it won't touch.** If `use_breath_pause` in the tag picker bothers Jason it needs a *different* mechanism — a manual one-off review/delete of that specific tag from the vocabulary, since by definition the phrasing class needs human judgment. **Nothing to apply; the destructive `--apply` was NOT run.** | **Jason** | Whether/how to remove `use_breath_pause`-class tags |
| 🔴 **Task 54 — two `defaultAcceptSalvage` policy properties, surfaced not decided.** **(1)** A **legitimately empty** `tasks` table is indistinguishable from a lost one — a user who cleared their list and then hits corruption gets an older backup restored, **resurrecting tasks they deliberately deleted**. **(2)** `report.lost` is **not weighed at all**, so a salvage recovering one task row but losing every other table is accepted while a complete, minutes-old backup goes unused. Both feed the salvage-vs-backup-age question `ladder.ts:15-21` already leaves open and deliberately does not settle. **The guard (task 54) is now in place for the policy AS WRITTEN** — changing the policy is a separate, product-intent call. | **Jason** | Recovery behaviour on corruption |
| ~~🔴 **A dropped `git stash` of Jason's is sitting in the object store — keep or discard?**~~ → ✅ **RULED 2026-08-22: KEEP IT, tag-then-gc.** Preserved as tag **`archive/stash-2026-08-17`** and the `gc` then ran — the stash survives *and* the repo is packed (15 MB → 3.9 MB). *(Original question follows.)* **A dropped `git stash` of Jason's was sitting in the object store** `git fsck` reports `dangling commit f1ff7082`, authored by Jason 2026-08-17: *"On main: pre-merge: sessions-origin content already in worktree"*, touching 7 docs files. It is almost certainly the stash from the line-ending/merge operation the Branch-state section records — where all seven files were hash-matched to the branch before stashing, and the merge then carried the content in — and its 119 lines not in `main` read as **superseded prose from six living documents**. But it is **Jason's data and dropping it is irreversible**, so the housekeeping pass stopped rather than guess. 🔴 **It also blocks the `gc`:** the repo has never been packed (2509 loose objects, 128 `tmp_obj_*`, 17 `incoming-*`, ~15 MB), and `gc --prune=now` would sweep the stash along with the garbage. **Coordinator recommendation: `git tag archive/stash-2026-08-17 f1ff7082` FIRST — that makes it reachable so `gc` cannot prune it — then run the `gc`.** That keeps the stash *and* packs the repo, instead of trading one for the other. | **Jason** | The `gc`; ~15 MB of `.git` |
| ⚠ **Task 46 Phase 2 — three deviations + three judgement calls, all small.** **(1)** the *"every N months"* stride is offered for **Weeks of the month** too, not just Dates — **without it a stored `{mode:'ordinal', months:3}` cannot round-trip**; reversing is deleting one line. **(2)** no inline *"Repeats:"* caption, because `TaskEditorScreen` already prints *"How often"* directly above. **(3)** `describeRecurrence` extended beyond the brief's letter so the task list reads correctly. **Judgement calls:** switching *out of* a month mode keeps the grid contents; `months: 1` is stored by absence; `weekInterval` defaults to "2". | **Jason** | Editor behaviour |
| 🔴 **Task 46 Phase 1 — one deviation + two product-visible choices.** **(1) DEVIATION — the new modes deliberately do NOT roll `reset_date`/`last_period_shortfall`**, which brief §4 asked for. **The mechanism, and it is a good catch:** nothing anywhere increments `current_period_progress` for a `scheduled` task (`incrementPeriodProgress` refuses the type by design), so a monthly roll would record `quota − 0` **every month — a permanent fabricated miss** on the very column the importance boost derives from. Inert today (`missedQuotaFromEntity` returns null for non-quota types) but it is the invisible-corruption shape constraint #7 exists to prevent. It implemented §4's *second* sentence instead — occurrences-per-month feeds R8's gate, where the brief's own worked example lands — guarded by a per-mode test asserting `resetDate` stays null. **Reversing it is small and localised, but needs a decision about what would increment the progress it would be measured against.** *(Coordinator concurs with the builder.)* **(2)** `dayOfMonth` **clamps** 29/30/31 to the month's last day (ruled) — **consequence needing its own call: "the 30th and 31st" both clamp onto 28 Feb**, so the occurrence set is de-duplicated and fires **once**. **(3)** `interval` weeks are **anchor-aligned seven-day blocks, not ISO Mon–Sun weeks** — so "every other Wednesday" set up on a Saturday fires in **4 days**, where the ISO reading would wait **11**. | **Jason** | Recurrence semantics |
| 🔴 **Task 17 Phase A — three deviations, all provisional.** **(1) THE ATTEMPT DEFINITION** *(the one that matters)*: an attempt = a completion or a skip, so `success_rate = completions/(completions+skips)`. Park and crash-recovered abandonment never count. **The sub-question task 44 left open — does a self-completion count toward `success_rate`? — was closed as YES, counts fully**, because the middle positions each break something (the CHECK constraint, or scoring doing-the-task as a failure, or needing a third stored count = a migration). Five alternatives are tabled in the report. **This is the one most likely to be overturned.** **(2)** `recordSkipEpisode` now writes a **second** column — task 13 explicitly said it would not, but that statement was conditional on no completion writer existing; the comment was rewritten to say what is now true rather than deleted. **(3)** `withMutationCapture` gained an entry (task 41/42's module, outside the brief) — without it the mutation stream would record a task's **skips but never its completions**, in exactly the columns this task makes load-bearing. | **Jason** | The historical-success signal (23% of score) |
| **`fsync` per-event → boundary-only at beta** (task 41 amendment §1). Alpha writes `fsync`-per-event for crash-safety; the ruling is to revert to boundary-only at beta. Ruled, not yet actioned — the revert is a beta-pass change on the capture write path. | Jason ruled; **task 42 actions** | Beta capture write path |
| ~~**"Every N weeks" recurrence intervals**~~ → ✅ **NUMBERED task 46, 2026-08-17.** | ~~Jason~~ | — |

---

## Device (`P`) batches

**21 of 45 tasks carry a `P`** (task 8 retired). *(The old count of "16 of 37" was stale twice over.)* Because you run the device sessions yourself, these are the scheduling constraint — group them rather than paying setup cost per task.

- **Done and confirmed (9):** 0, 1, 2, Q1, 6, 7, 12, 13, 24
- **Open, beta (5):** 21 (crisis coverage), 30 (envelope), 20 (real eval numbers), 22 (if solved in the guides), 32 (residue sweep)
- **Open, strategic (2):** **41** (event capture — gates the whole chain), 40 (the bake-off)
- **Open, QoL (1):** **44** — batch it onto 41's device session rather than paying setup twice
- **Open, later (4):** 14, 15, 19, 29

**No `P`, so they can proceed any time without you (23):** 3, 4, 5, 9, 10, 11, 17, 18, 23, 25, 26, 27, 28, 31, 33, 34, 35, 36, 37, 38, 42, 43, 45.

**41 is the exception that reshapes the schedule.** It's device work and it now sits in front of 31, so the model-migration chain starts with a device session rather than ending with one. Everything after it (31, 37, 38) is still headless until the bake-off.

---

## Counts

**Total: 59 tasks — 1 retired (8)**  ·  **39 done, 4 partial (14/17/41/44), 16 undone** (0–59, no 16, plus Q1, minus reserved-and-unused 39). **54–58 added 2026-08-22** from task 53's audit findings — the remediation groups, numbered so they can't rot. *(Counts now reconcile exactly: 30+3+25=58. 44 sits in the partial list only; it was previously double-counted in both.)* **46, 47 and 48 added 2026-08-17**, numbering unowned residue that had accumulated in the standing notes — the handoff's rule is that dependencies which aren't tasks rot.

**✅ Done (39, +44's code):** 0, 1, 2, 3, 4, 5, Q1, 6, 7, 9, 10, 11, 12, 13, 18, 23, 24, 25, 26, 27, 28, 33, 34, 35, 36, **37** *(headless; light `P` → 32)*, **48**, **50**, **49**, **52**, **46**, **53**, **54**, **55**, **56**, **57**, **58**, **59**
**🟡 Partial (4):** **17** — Phase A done, the six loops queued as Phase B · **41** — Phases 1+2 done, device run outstanding · **14** — Phase A done, Phase B is device · **44** — code done + desktop-verified, device (`P`) outstanding
**⬜ / 🔴 Undone (16):** 15, 19, 20, 21, 22, 29, 30, **31**, 32, **38**, **40**, **42**, **43**, **45**, **47**, **51**
*(14 is 🟡 Phase A in progress, listed above.)*

**Test baseline as of 2026-08-24: 1199 tests / 93 suites green** *(811/69 → 868/75 t14 → 872/75 t48 → 910/80 t41.2 → 961/83 t44 → 973/86 t14-wiring → 974/86 t54 → 998/86 t17A+55 → 1026/88 wave 1 (49+52+57) → 1038/89 wave 2 (58+56+59) → 1121/92 t46.1 → 1128/92 t46.1a → 1199/93 t46.2)* on `main` ✅ **raw `npx jest` now reports the TRUE 1026 / 88 — all worktrees removed 2026-08-22, no subtraction needed** *(it had reached 4842/416)* (historic: raw reported **1820 / 156** — the stale worktree contributes an unchanging 794 / 68). ⚠ *The long-quoted "961/83" was itself 1 test / 1 suite stale — it predated commit `2c9c546` (the gen-table 6-cell assertion), so the true pre-wiring baseline was 962/84, and the wiring added exactly its 2 suites / 11 tests.* `tsc --noEmit` clean; `eslint .` 0 errors / 56 warnings, all inline-style in `src/dev/`.

## Personal ship: MET (2026-07-29) — with one caveat now visible

**The whole loop runs on the S23 FE, DB-verified** — add-via-chat → session → timer → all five outcomes → coaching → summary, with the alarm firing 11 ms late from the background (task 24). Tasks 23 and 24 both landed; the backend (6/7/9/11/12/13 + data layer) is complete beneath them.

**The caveat, now half-discharged.** What was confirmed on device was a *build from a branch* that did not include 35, 36, or the recurrence fix. As of **2026-08-17** `main` **is test-verified green** (794 tests, `tsc` clean, `eslint` 0 errors) — so the *headless* half of the caveat is closed. 🔴 **DEVICE SESSION REPORT: `docs/eval/device_session_2026-08-19_findings.md`** — the authoritative evidence record for the first on-device run of merged `main`; raw artifacts (3 DB snapshots incl. the irreplaceable 2.6.0 pre-migration DB, the capture corpus, screenshots) are in `todoAI_private_archive/device_session_2026-08-19/` outside the repo. **Confirmed on device 2026-08-19:** build + `.cxx` flag, migrations 006+007, the recurrence engine on 3 real tasks, capture writing losslessly (raw completion preserved), the **force-kill acceptance test**, crash recovery (13), thermal sampling, the grammar gate, quick-start's full check-in, the resume slot (33), self-complete + no-writer convention, coaching-queue drain, coaching-resolution dispatch (12), and the mismatch-warning screen (44d). **Still blocked/believed:** task 14 Phase B (unwired), StatFs (no consumer), the fsync number (needs rebuild), blocked-buttons (no test data). **Defect lead:** a non-functional 'Wrap this up' button on the Coach screen. **The device half — PARTIALLY DISCHARGED 2026-08-18.** Merged `main` (`c1c02d4`) was **built and run on the S23 FE for the first time**: the debug APK installs, the `.cxx` codegen flag took (`-DREACT_NATIVE_APP_MODULE_PROVIDER`), the app launches clean to the dashboard (Fabric on, no redbox), and **migrations 006+007 applied on real data (2.6.0→2.8.0)** with the recurrence engine's advance sweep running correctly against 3 real recurring tasks (skipped `unscheduled`, advanced both `scheduled` to the current period, neglect anchors untouched). ⚠ **Still believed-not-confirmed:** the grammar-compile gate (needs first model use), the capture force-kill test, backup/restore, StatFs, thermal, and the task-44 flows — the interactive half of the session. See `docs/briefs/device_session_runbook_2026-08.md`.

## Where the frontier is — two threads, and they don't compete

🔴 **The corpus collection window is finite, and that reprices everything below.** The capture ladder (43) **drops free-text conversation capture at open beta** — permanently ending the supply of real conversational input that 31's corpus, 38's training data and 40's held-out eval all depend on. **The corpus is built during alpha and closed beta, or it is largely not built at all**, and every future LoRA iteration and every re-run of the bake-off draws on a corpus whose window has already shut. This is an argument for starting **41 now** rather than after the beta-hardening work, and for closed beta being *generous* with free-text capture while consent and review are still real protections. It is not an argument for extending the window — the mechanism in 43 doesn't bend.

**Thread 1 — the strategic one (the model-migration decision).** Now **`41 → 31 → 38 → 40`**, then Jason's call. **Task 41 is the new front of the chain** and it's the move that can happen today: once capture is on, the corpus accumulates while you do everything else, which is the only input on the board that grows without effort. 31's interview and reconstruction work runs in parallel with the capture window rather than waiting on it.

**Thread 2 — the tree, then beta hardening.** ✅ (a) the three checks on `main` — **done 2026-08-17, green**; ✅ (b) push `main` to GitHub — **done**; ✅ (c) task **37** — **done**. Landed since: 48, 14 Phase A, 41 (both phases), 44. Remaining: **15 / 17 / 21 / 32** and the designed visual pass.

**Sequencing note — task 17 vs. the device session (2026-08-18).** 17 is headless, Opus-worthy, and the natural owner of the `completion_count`/`success_rate` writer task 44 just left as a deliberate seam — so its context is freshest now. **But the recommendation is the device session first, for three reasons:** (1) **four tasks' worth of code — 14, 41, 48, 44 — have never run on the S23 FE**, and it is the biggest unverified surface on the board; running 17 first only piles more never-run code on an unverified base, and if the first build reveals a problem (a grammar that won't compile, migration 007 failing on real op-sqlite, the capture TurboModule not loading) everything above it is suspect. (2) **Capture accumulating the corpus is the one time-limited resource** — the window closes at open beta, and capture only starts collecting once it is on the phone; every day it is off the phone is corpus not gathered. (3) **17 learns from real usage data**, so its *value* (not just its mechanism) is only real once device use has generated some. **17 is the right parallel work only if the device session cannot happen soon** — it is exactly the headless set-and-forget batch to hand off when Jason is away from the phone, with the caveat that it builds on ground not yet verified.

✅ **37-before-41 is confirmed and in motion** — Jason is running task 37 before task 41 Phase 2 resumes. The amendment (§9) verifies the two tracks are **file-disjoint**: 37 touches `task_extraction.v1.gbnf`, its embedded copy in `grammarText.ts`, and possibly `extraction/validator.ts`; 41 Phase 2 touches `src/capture/` (new), `llm/errors.ts`, `llm/provider/ladder.ts`, `src/app/`, `src/execution/`, `src/specs/`, the Kotlin sources, `scripts/`, `jest.setup.js`. They share `src/llm/` but no file. One benign interaction: 41 widens `LlmOutputValidationError`'s constructor with an **optional** third parameter, so any tightening 37 does still compiles.

**The working order (serial through the `src/app/`-contended tasks, now all landed):** ~~14 Phase A~~ ✅ → ~~48~~ ✅ → ~~41 Phase 2~~ ✅ → ~~44~~ ✅ → ~~the DEVICE SESSION~~ ✅ **DONE 2026-08-19** (`docs/eval/device_session_2026-08-19_findings.md`) — first build of merged `main`, grammar-compile gate, 41 force-kill + capture, 44 flows, thermal, coaching dispatch all confirmed. **What that session left open is now the frontier:** ~~task 14 §13 wiring~~ ✅ **DONE 2026-08-21** (desktop-verified, all 8 deviations ratified, committed; report `task14_wiring_findings_report.md`) — unblocks **14 Phase B** on the next device run (⚠ **not** StatFs: the gate is attempt-and-catch, so `availableBytesFor` still has no caller); then ~~**50**~~ ✅ **DONE 2026-08-22** (energy = activation cost, never null; `energy_definition_task50.md`) → 🔴 **31 (corpus) — NOW THE LIVE STRATEGIC MOVE, fully unblocked (41 + 50 done)** → **47 + 51** (one `docs/reference/` pass, after 007 landed). Still device-pending from the session: 14 Phase B, StatFs, the `fsync` number, blocked-buttons (no test data), thermal-under-load, and the "Wrap this up" defect lead. **46**, **49** and **17** are headless and slot in anywhere — see the sequencing note below on 17.

**Sequencing note added 2026-08-17 — 37 before 41, and it is nearly free.** Task 37 is a small, headless, no-`P` grammar fix that is already spike-confirmed (first char `[a-zA-Z0-9]`; min-length does not work). Task 41 turns on capture, and **every capture taken through the unfixed grammar is corpus recorded against a known-defective extractor**. Since 41's whole purpose is to feed 31/38/40, landing 37 first costs a few hours and protects the entire corpus window; landing it after means some fraction of the irreplaceable alpha corpus carries a defect the board already knows about. The two do not compete for the same resource — 37 is headless, 41 needs the phone.

**Beta gates:** the designed/polished pass (24, consuming 23's tokens), **21** (crisis, human), **42** (privacy/consent — pinned here for the same reason as 21, and reviewed with it), **30** (device envelope, Jason), **20** (real eval numbers), **32**'s device residue. **General:** 29 only, re-scoped to a Vulkan-throughput question. **Task 8 retired** — the spike measured that thermal throttling doesn't discriminate between model sizes, so downward tier degradation buys nothing on this hardware.
