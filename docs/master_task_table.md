# todoAI — Master Task Table

*Every task in numeric order, as of **2026-08-17** (verification pass: `coordinator/sessions-origin` merged, **`main` test-verified green for the first time**, pushed to `origin/main` at `fc59b83`, `.gitattributes` added, probe branches cleaned). Status reflects the evidence base in `docs/eval/`, not summaries of it.*

**This file is the single source for per-task status.** It moved into the repo on 2026-08-07; `orientation_for_opus.md` no longer carries a second copy of it. On conflict: **this table wins on status**; orientation wins on contracts, constraints, settled decisions, and ship gates; a per-task brief wins for its own task.

**`master_task_table.html` is generated — do not hand-edit it.** After changing this file run `node scripts/gen-task-table.js`.

**Status key:** ✅ **Done** · 🟡 **Partial** · ⬜ **Undone** · 🔴 **Undone + gate/live bug**
**Model key:** **You** (device/human judgment) · **Fable** *(access ended)* · **Opus** · **Sonnet** · **Jason/Human**
**`P`** in the Dep column = **requires on-device (Phase B) verification.** Desktop reasoning does not close these; a `P` task is not done until it has run on the S23 FE. Sequence headless work and device batches separately.

*(There is no task 16 — retired and split into 23 + 24. Q1 sits between 5 and 6, where it ran. **39 is reserved and unused** — held for an optional corpus→eval-harness step between 38 and 40. **41, 42 and 43 added 2026-08-07**: 41 gates 31 after the dogfooding DB turned out to hold no real captures; 42 pins the closed-beta rung; 43 pins the open-beta and GA rungs so the pruning schedule can't be lost; 44 collects four alpha QoL items requested alongside 41 and kept separate from it deliberately; 45 audits the deviation backlog after task 24's dial change reached production without Jason ever being asked. **46, 47, 48 added 2026-08-17** from unowned residue: 46 the "every N weeks" interval handoff orphaned since task 24, 47 the stale schema snapshot, 48 the grammar primitive task 37 left loaded.)*

> ### ⚠ Read this before trusting any ✅
>
> **"Done" and "merged" were different claims, and until 2026-08-07 this table only tracked the first.** Tasks 35 and 36 were marked ✅ with reports and test counts — all true — while their work sat on an unmerged branch. The *checked-out* branch had no migration 006, no `src/services/recurrence/`, and no v2.4 spec. Since 36's whole content was a fix for a **live scoring bug affecting every recurring task**, an alpha built during those four days would have shipped the bug with the record insisting it was fixed.
>
> **New rule: a ✅ row names its branch until that branch is on `main`.** All rows below are on `main` as of `fc59b83`.
>
> ✅ **`main` IS now test-verified — 2026-08-17.** `npx jest` **794 tests / 68 suites green**, `npx tsc --noEmit` **clean**, `npx eslint .` **0 errors** (56 warnings, all inline-style lint in `src/dev/` throwaway spike screens). Run on `main` at `fc59b83`, after merging `coordinator/sessions-origin`. The long-standing 🔴 is discharged.
>
> ⚠ **Read the raw jest number with care: it says 1588, and 1588 is wrong.** A stale git worktree at `.claude/worktrees/interesting-shirley-e10fa1` (detached at `d3ead86`) is a full second copy of the tree *inside the project*, and jest collects its 68 suites too — exactly doubling the count. **The real figure is 794**, confirmed by counting test files in each tree (68 and 68) and by `d3ead86` already being an ancestor of `main`. Anyone quoting "1588 tests green" is quoting a number spanning two different commits. See the standing note below.

---

| # | Status | Task | Description | Depends on | Model |
|---|---|---|---|---|---|
| 0 | ✅ Done | Core dependency spike | Prove Ternary Bonsai loads and runs in `llama.rn` on real hardware. Established the truths everything rests on: format is **TQ1_0** (mainline) / Q2_0 (fork-only), **not** Q1_0; CPU-only on Android; chat-template `messages` API mandatory; app-private model storage. *4B only — 8B/1.7B have no working quant (→ 29).* | **P** | **You** |
| 1 | ✅ Done | Dev env + toolchain | Bare RN 0.86, New Architecture, Windows host, device deploy. `README_build.md`. | 0, **P** | **You** |
| 2 | ✅ Done | Data layer | SQLite migrations + repositories returning domain types. Ran on hardware; `POWER()` confirmed absent, so neglect math stays in TypeScript. | 1, **P** | **Sonnet** |
| 3 | ✅ Done | TypeScript types | Row/domain/scales. The `Recurrence` discriminated union is authoritative; `scales.ts` is the only legal projection path. | 2 | **Sonnet** |
| 4 | ✅ Done | Structured-output strategy + eval design | The D-series strategy (D1 recap, D8 union, D9 greedy, D10 ladder). Later confirmed on-device by Q1. | 0, 1 | **Fable** |
| 5 | ✅ Done | Schemas, grammars, validators, mappers | GBNF artifacts + zod validators + pure mappers. Rule-name lint enforced. **Carries the live hole in task 37.** | 3, 4 | **Sonnet** |
| Q1 | ✅ Done | Grammar smoke-test arc | Three-session arc proving GBNF works on this build. Root cause was one lexer quirk: **no `_` in rule names**. GREEN at ~0% overhead — the largest de-risk after the load spike. | 1, 5, **P** | **You + Sonnet** |
| 6 | ✅ Done | Provider + D10 ladder + startup guard | `TernaryBonsaiProvider`, retry ladder, and the startup guard that compiles every grammar before any user session. Device-confirmed. | 1, 3, 5, Q1, **P** | **Opus** |
| 7 | ✅ Done | System prompts | Task-input + coaching prompt engineering. Tuned 10/16 → 15/16 on-device. Surfaced the crisis-detection finding that became task 21. | 4, 6, **P** | **Opus** |
| 8 | ⬛ **RETIRED 2026-08-07** | ~~Tiering ladder (§3.1)~~ | **Retired, not deferred — its mechanism was measured and doesn't work on this hardware.** Task 8 was "runtime health check + downward degradation across 8B/4B/1.7B." The premise is that dropping to a smaller model relieves thermal pressure. **The six-model spike measured that it does not:** *"The thermal envelope does not discriminate. All six reach at least SEVERE throttling by twenty minutes. It cannot be used to choose between them."* A 0.8B throttles at the same twenty minutes as the 4B, so degrading tiers when hot buys nothing. **Its blocker also dissolved:** 29 gated 8 on "does an 8B exist that runs" — **Bonsai-8B-Q1_0** (1.08 GiB, 8B params at the 4B's footprint) is loadable on stock and **enters task 40's bake-off**, which answers it empirically for free. **What survives is already owned:** spec §3.5's *"monitor thermal state, reduce context length, defer background work when hot"* is **deferral, not tier-switching**, and orientation §8 already assigns the thermal sampler to **task 19**; "real thermal management" remains its own general-ship gate. ⚠ **One condition that could revive it:** the thermal measurement is **one device**. If **task 30**'s envelope finds a device where tier degradation genuinely helps, reopen this — with the new measurement, not the old assumption. | ~~6, 29, P~~ | — |
| 9 | ✅ Done | Scoring (§5.1–5.2) | Factor scoring + neglect multiplier. Revised per R1–R3: linear `neglectCurve` seam, context/tools moved out to a hard pre-filter, 31/23/23/23 weights, transitive-fan-out subtask ordering, multi-hop DAG guard. ⚠ **`score.ts` was binary to git until 2026-08-01** — see the standing note; every prior review of this file read a `Bin` placeholder, not lines. | 2, 3 | **Opus** |
| 10 | ✅ Done | Scoring composition review | Fable's pass over the recombination. Forks 1/2/3/6 ruled leave-as-built; found **U1** (novelty shuffle randomizes ordered chains), **U2** (parent competes with its own subtasks), **U6** (far-future neglect accrual). ⚠ **Conducted against a `score.ts` git could not diff** — its conclusions are not invalidated, but they were never diff-reviewable. | 9 | **Fable** |
| 11 | ✅ Done | Session planning (§5.3) | `src/planning/` — sizing, agenda vocabulary, pure planner core, service edge. **`runSelectionBoundary` is the pool's only entry**: capability filter → dependency filter → *then* a ranker, both reject sets retained; R7's hold wired via `pendingBreakdownCompleteTaskIds`. `AgendaTaskItem.blockKind` (countdown/openBlock) closes 33's flagged breaking change to 24. **Decision (a) RULED: no LLM call — deterministic v1 plus a typed `PlanAdjustment` hook, so no `P`.** Novelty entropy is now a permanent regression test (slot-1 ≈ 1.92 bits). Report: `docs/eval/task11_findings_report.md`. | 9, 10, 25, 33 | **Opus** |
| 12 | ✅ Done | Coaching flows + resolution dispatch | Three triggers, grammar-constrained resolution union, app-side dispatch, deterministic crisis gate. Device-confirmed. **Residue:** `add_dependency`/`add_missing_task` dispatch unexercised on-device → task 32. | 6, 7, **P** | **Opus** |
| 13 | ✅ Done | Timer + episode lifecycle + crash recovery (§8.2) | `src/execution/` — timer engine, five-option end-of-block prompt, both extension paths, park primitive, crash recovery. **Migration 005 → schema 2.6.0.** **Force-kill confirmed on the S23 FE** by pulling the DB: credit bounded to the block end (2 min not 7), no skip written, no coaching queued, task stays `active`. 531 → 636 tests. Report: `docs/eval/task13_findings_report.md`. **Residue:** overnight-doze Phase B unrun; migration-number contention with 36 **resolved** (13 took 005, 36 took 006). | 6, 11, **P** | **Opus** |
| 14 | 🟡 **Phase A DONE 2026-08-17 · Phase B (device) outstanding — "no Phase B, no done"** | Backup/restore + corruption recovery (§8.4) | ✅ **Phase A landed** (`d340a0a`, `347aa25`, `0286acf`). `src/services/backup/` (11 files) + two test-only helpers in `src/db/testUtils/`. **No schema change, no migration — 007 left free for task 44.** Ladder runs against an injected `DbOperations` layer, exercised by a file-backed better-sqlite3 double with **real byte-level corruption** and an injectable full disk. **57 tests / 6 suites**; suite 811→**868 real tests / 75 suites**, `tsc` clean, `eslint` unchanged — all independently re-verified by the coordinator. **`VACUUM INTO` confirmed** (SQLite 3.51.3 bundled, no OMIT flags). **`backup_log`: USED, and load-bearing** — its row commits *before* the vacuum, so each snapshot carries its own creation record, which is how the two slots get dated when the working DB is unreadable; it also holds the rotation pointer, which has nowhere else to live. **§4(c) answered two ways deliberately:** restore **clears** the runtime tables (a snapshot's `active_episode` would be a phantom crash), salvage **keeps** them (that crash is real). **§4(d):** capture logs are **not** backed up. **§4(e):** product DB blocks, capture degrades — `reclaimSpace` is a typed hook 41/43 fill. ⚠ **Nothing is wired into `src/app/` — zero files touched there, deliberately:** blocking a session without a "not enough space" surface (task 24's screens) turns a full disk into a silent no-op. Report §13 has the exact 3-call-site recipe and notes **task 44 also edits `startSession`**. 🔴 **Two deviations + one decision owed — see Open rulings owed.** Report: `docs/eval/task14_findings_report.md`. | *(Original brief description follows.)* Pre-session copy, integrity check, salvage ladder, block-on-no-space. **Brief rewritten 2026-08-07 — the original approach was wrong for this tree.** 🔴 **Do not file-copy the DB:** (a) task 41's Phase 1 established there is **no way to write a file from JS** here — no RNFS, no FS TurboModule, op-sqlite gives paths but no write; and (b) **a byte-copy of a live SQLite DB is unsafe anyway** — with an open WAL it can capture a torn state, which would have been a latent integrity bug even with a filesystem API. **What exists is better:** op-sqlite's DB object has `execute`, `delete()`, `attach`/`detach`, `getDbPath`, `loadFile`, so the whole ladder is reachable at SQL level with **no new native module** — **`VACUUM INTO`** for an atomic consistent backup, `PRAGMA integrity_check`, `ATTACH` + `INSERT…SELECT` for salvage (which also answers *what salvage reads* — it skips views and steps past a corrupt table). ✅ **`VACUUM INTO` availability RESOLVED headlessly 2026-08-17 — it is a build fact, not a device fact.** The brief said to verify it on device and stop if absent. **op-sqlite 17.1.2 compiles its own bundled amalgamation** (`cpp/sqlite3.c`, wired at `android/CMakeLists.txt:79`) at **SQLite 3.51.3**, with no `SQLITE_OMIT_VACUUM`/`SQLITE_OMIT_ATTACH` flags — it does **not** link Android system SQLite, so the version does not vary by device or OS. 3.51.3 ≫ the 3.27 floor. Phase A can build on it with confidence; Phase B still watches it run, as habit rather than doubt. 🔴 **One gap:** nothing reports **free disk space** — attempt-and-catch `SQLITE_FULL`, `setReservedBytes`, or a small native call; bring the tradeoff back, since "block *before* starting" implies knowing in advance. ⚠ **`backup_log` (migration 001) has never been written to or read** — 14 is its only natural consumer; use it or retire it. **New decisions:** do capture logs get backed up (*recommend no* — they're ephemeral by settled decision, large, and compete for the space the DB needs; task 42's export owns getting them off-device), and the reciprocal of 41's rule that **capture degrades where the product DB blocks**. Brief: `docs/briefs/backup_recovery_task_14.md` | 2, **P** | **Opus** |
| 15 | ⬜ Undone | Edge cases / Safe Mode (§8.3) | Finish a session without AI; queue what would have coached; health check before new sessions. The failure modes it handles only occur on-device. Brief: `docs/briefs/safe_mode_task_15.md`. *Beta hardening.* | 6, 12, **P** | **Opus** → **Sonnet** for repetitive handlers |
| 17 | ⬜ Undone | Numeric learning loops (§5.4) | Six loops with hierarchical shrinkage, regression protection, rollback. Replaces R6's fixed 0.5 prior with a learned one — same formula. **First real consumer of `algorithm_weights`.** **Handoff from 13:** `completion_count`/`success_rate` have **no writer anywhere** — `historicalSuccessFactor` scores every task off a permanent n=0; 17 is the natural owner. *Headless. Opus-5-worthy for the regression-protection + rollback interaction.* | 9, 11, 13 | **Opus** |
| 18 | ✅ Done | Skill-injection layer **design** (§5.5) | Flat-AND closed-vocabulary matching, `conf = Ceff/(Ceff+2·Xeff+3)` at 45-day half-life, oscillation trap closed via a fired-outcome channel, code-first distillation with one 4B judgment per call, latency-tiered injection. `docs/design/skill_layer_task18_design.md`. | 4, 7, 12 | **Fable** |
| 19 | ⬜ Undone | Skill-layer implementation | Build 18's design. **Owns the thermal sampler** (currently a stub). Two new grammars plus distillation prompts need a real model. **Planning-scope skills have exactly one legal consumer: 11's `PlanAdjustment` hook.** If it wires an LLM there, it must add a validation wrapper (the hook must never resurrect filtered tasks; nothing enforces that today). **Handoff from 13:** decide whether a recovered crash (`completion_status='abandoned'`) counts as a friction incident — §1.3 says a crash isn't user failure. | 12, 18, 26, **P** | **Opus** — ***Opus-5 for the outcome-attribution/confidence channel*** |
| 20 | ⬜ Undone | Eval harness | 🔴 **PINNED 2026-08-17 — must track junk-tag rate as an explicit eval signal.** Task 7 Phase B already watched it informally (`tagCount`/`avgTagCount`) and learned the hard way *why it must be a signal, not a score*: **`context_tags_must_include` is a SUBSET check, so a model that dumps the entire known vocabulary passes it every time** — iter 2 did exactly that and the KPI reported `context_tags` as nearly fine while the regression was invisible. Carry that lesson forward: `must_include` is a minimum, so **extra tags can never be called correct**. The residual `"work_on_it_until_did"`-class junk (~2–3/run) is a clean **model-capability** discriminator, not a prompt-quality one — see the standing note. Reusable scoring over fixtures; reuse task-5 validators + task-7 scorer. **Also owns the formal success criteria** — the valid@1 / field-correct bar. Harness is headless; *the numbers it produces are only real when run against the device model.* | 4, 5, 7, **31**, **P** | **Sonnet** — ***Opus-5 for negative-control design*** |
| 21 | ⬜ Undone | Crisis detector review + referral localization | Two human judgments: coverage of the draft phrase-matching detector (indirect expressions are the common form), and `CRISIS_REFERRAL_TEXT` content/localization. **On-device evidence: the 4B cannot self-detect distress** — coverage claims must be re-checked on hardware. ⚠ **Changed by 41/42 (2026-08-07):** 21 was briefed to judge the phrase list cold because no real data existed. Task 41's crisis-gate stream gives it an alpha corpus of **actual firings and near-misses**, retained for exactly this purpose — so **21 should be re-briefed to review against evidence**, and running it before that log accumulates wastes the best input it will ever get. Soft dependency on 41, not a hard gate. Brief: `docs/briefs/crisis_review_task_21.md`. 🔴 **Hard beta gate.** | 7, 12, *(41 soft)*, **P** | **Human** |
| 22 | ⬜ Undone | `which:"next"` weekday semantics | From a Thursday the 4B read "next Monday" as 11 days out. Decide in `resolveDue` or teach the guides; affects **every** resolved date. If solved by teaching the guides rather than in code, the fix is a model-behavior claim and needs device confirmation. **Task 31 should collect several real instances** — they cost nothing extra and become the evidence when this gets ruled. | 5, **P** | **Opus/Sonnet** |
| 23 | ✅ Done | UI/UX design | **Design substantially done.** Interactive prototype at `docs/design/Main Screen.dc.html` + `Coaching Screen.dc.html`; review `docs/eval/task23_review.md`. Built in two passes; all six divergences verified in-code. The three follow-ups were **closed for free by task 24**. The polished/designed visual pass remains a **beta** gate. | spec §6 | **Claude Design (Opus)** |
| 24 | ✅ Done | Product UI implementation | **Done — functional/personal pass, confirmed on the S23 FE (DB-verified).** `src/app/` (controllers / presentational screens / shell / alarm). The whole personal-ship loop runs on device: add-via-chat → session → timer → all five outcomes → coaching → summary. **Alarm fires 11 ms late** via an `AlarmManager.setAlarmClock` TurboModule — **constraint #13 discharged**. 636 → **711 tests**. **Phase B found 5 bugs + 1 build trap, 4 silent — all fixed.** Report: `docs/eval/task24_findings_report.md`. **Residue:** designed pass is beta; device edges → 32; five handoffs. | 23, 6, 9, 11, 12, 13, **P** | **Opus** + **Sonnet** |
| 25 | ✅ Done | Post-review scoring follow-ups (R6–R8 + U1) | **U1** dependency-blocked pre-filter (mandatory — blocked 11); **R6** smoothed historical success `(rate·n + 0.5k)/(n+k)`; **R7** keep the parent, fire immediate `breakdown_complete` coaching, hold it out of the pool; **R8** neglect accrual gate `anchor + period/(1+quota)`. Report: `docs/eval/task25_findings_report.md`. | 9, 10, 26 | **Opus** |
| 26 | ✅ Done | Skill-layer schema migration | `learning_state` table, `skills.is_active` default flip, `skill_evidence.source`, `learning_data` `"v":1`, `fireable_skills` comment. **Plus both new `coaching_queue` trigger types in one migration.** Hazard: CHECK changes need full table rebuilds. **Also fixed the migration runner**, which never applied more than one migration and could not toggle `PRAGMA foreign_keys` inside a transaction. Report: `docs/eval/task26_findings_report.md`. | 18 | **Sonnet** |
| 27 | ✅ Done | Spec fold-in (v2.2 → v2.3) | Spec v2.3 + schema v2.3 snapshot — R1–R8, the two-pre-filter selection boundary, and §7.2's five triggers. **Note:** written in parallel with 25/33, so it describes the ruled target state; its §4.5 `algorithm_weights` claim was overtaken by migration 004 and corrected in v2.4. | 10, 25 *(rulings)* | **Sonnet/Haiku** |
| 28 | ✅ Done | Multi-session work + hyperfocus extend (§8.7) | **Design delivered:** `work_state` axis orthogonal to `status`, `duration_type = estimate\|floor`, cumulative fold at one choke point, extend in +25-min quanta with tail regeneration, one resume slot in the deep-focus block. Zero scoring changes. **AMENDED 2026-07-20** — extend splits into two affordances (+5 flat/uncapped, and hyperfocus in 25-min quanta); guardrail **ruled B, hyperfocus only**. Implementation → 33 (landed) and 13 (runtime). | — | **Fable** |
| 29 | ⬜ Undone — **re-scoped 2026-08-07** | Quant path / Stage B decision | **Both halves of the original question have moved.** *(a) "Find or make an 8B that runs"* — **answered**: `Bonsai-8B-Q1_0` is loadable on stock and enters **task 40**'s bake-off; no separate work needed. *(b) "Build Stage B against the PrismML fork for Q2_0 + Vulkan"* — **newly obstructed**: the spike found `prism-ml/Ternary-Bonsai-8B`'s **`Q2_0` is absent from the build entirely and cannot load**, so the fork path isn't "untried", it's missing its premise. **What's actually left** is a narrower, later question: *is a fork build worth it purely for **Vulkan GPU** throughput?* — a general-ship performance call, not a quant-path one, and only meaningful after 40 picks a model. **No longer blocks anything** (task 8 retired). *General-ship, deferred.* | 0, 40, **P** | **Jason** |
| 30 | ⬜ Undone | Device envelope | Minimum supported spec (RAM, chipset class, OS versions). One device tested (S23 FE, SD 8 Gen 1, 8 GB). Sets the tiering floor and decides whether 8B-first is realistic or aspirational. **Needs more than one device to be a real envelope.** 🔴 **Hard beta gate.** | 0, **P** | **Jason** |
| **31** | 🔴 **Undone — CRITICAL PATH (41 is now UNBLOCKED — see below)** | **Real task + friction corpus** | ✅ **Task 41 Phase 2 has landed, so the gate is lifting** — capture accumulates the corpus by itself once the device build runs; 31's interview and reconstruction work runs **in parallel** with that window. 🔴 **Two loader facts from 41 that will silently corrupt this corpus if missed:** **(1) `escapeToEasier` writes TWO `episode` rows** — deliberate, because constraint #11 makes park-vs-skip load-bearing, so **the loader must dedupe on `interactionId`** or every escape double-counts. **(2) `mutation` emits ~20 rows per chat-created task** and will dominate that stream — size expectations and any sampling must account for it. | **The measuring instrument for the model decision.** Three item types, three consumers: **extraction items** (→ 38 training, 40 held-out eval, 20 fixtures), **coaching transcripts** (→ 38's no-regression check, 19), **friction episodes** (→ 19). ⚠ **RE-SIZED 2026-08-07:** the old "20–30 real messy tasks" target *cannot do this job.* 16 fixtures resolve to ±12 pts — the reason the model decision is still open. Split 20–30 items in half and the held-out set is ~10–15 → **±23 pts, twice the uncertainty the corpus exists to remove.** Task 40 would run the full device bake-off and still not separate the contenders. **New target: 80–120 extraction items, 45–60 held out (±11 pts).** Split must be **grouped by source situation and stratified on `recurrence`/`due_resolved`**, assigned in-file, never revisited. ⚠ **Gated on task 41.** The dogfooding DB holds no real captures — migration 001 says `raw transcript never stored`, so weeks of exactly this material is gone and unrecoverable. 41 makes the corpus accumulate by itself; 31's interview/reconstruction work runs **in parallel** with that window, not after it. **Synthetic and augmented items are legitimate in the *train* split and fatal in *held-out*** — n buys precision, realism buys validity, and they're independent. Tag every item's `source`. Brief: `docs/briefs/real_task_corpus_task_31.md` · Init prompt: `docs/briefs/task_31_session_init_prompt.md` | **41** | **Jason** produces · **Opus 5** designs the split + first ~15 + report · **Sonnet** bulk transcription *(deliberate move out of the "no Opus-5 benefit" tier — a bad split poisons every downstream number)* |
| 32 | ⬜ Undone | Device verification sweep | The standing residue, batched into one session: **`add_missing_task` dispatch**, `add_dependency` dispatch, the **unmeasured D1 recap→constrain flow**, task 24 §10's device edges (resume_block recovery, overnight doze on battery, locked-screen full-screen intent, the two untapped recurrence kinds), and 19's two new grammars when they land. 🔴 **NEW from task 37 (2026-08-17):** confirm the four tightened grammars **still compile through the startup guard on real llama.cpp**, and that normal extraction has **not regressed on Bonsai** through the `firstChar` constraint. 37 is headless-verified only — no real parse has run. **Also now due: the first build of merged `main` on the S23 FE**, which has never happened. Brief: `docs/briefs/device_sweep_task_32.md`. *Entirely device work.* | 12, 19, **P** | **You + Opus** |
| 33 | ✅ Done | Multi-session / hyperfocus **implementation** | Migration 003 (`work_state`, `duration_type`, `accumulated_minutes`, `last_worked_at`), the park primitive, the cumulative fold in `completeTask`, the three-way anchor in `listActiveByNeglect`, one grammar field. Report: `docs/eval/task33_findings_report.md`. | 28, 25 | **Opus** |
| 34 | ✅ Done | Schema reconciliation pass | Migration **004** → schema **2.5.0**. `context_fit` dropped from the `factor_name` CHECK and deleted; the four survivors reseeded to **31/23/23/23** but only where `data_points_count = 0` (a guard written for task 17's sake). **`active_tasks_with_neglect` dropped** — it computed retired `weeks²` via a `POWER()` that doesn't exist on-device. Report: `docs/eval/task34_findings_report.md`. | 27, 33 | **Sonnet** |
| 35 | ✅ Done *(merged to `main` 2026-08-07)* | Spec fold-in (v2.3 → **v2.4**) | `docs/reference/..._v2.4.md` delivered. Folds in migrations 004+005, task 11's planner contracts (`PlanAdjustment`, `blockKind`, the two-filter boundary, `replanRemaining`), and the extend amendment. Every name/formula pulled from source, not the brief. **Confirmed no cross-doc conflicts.** Report: `docs/eval/task35_findings_report.md`. **Was marked done for four days while sitting on an unmerged branch.** | 11, 34 | **Sonnet/Haiku** |
| 36 | ✅ Done *(merged to `main` 2026-08-07)* | Recurrence period engine (§4.2) | `src/services/recurrence/` — idempotent `advanceRecurrence(today)` sweep at app-open + session-start; migration **006 / schema 2.7.0**. Missed-quota boost **derived at scoring time** (never written to `importance`); `last_period_shortfall` persists the fact behind it. **Fixed a live bug worse than briefed:** urgency (23% of score) was broken in *both* directions for every recurring task — chat-created pinned at 1.0 forever, editor-created at the floor forever. DST-correct; neglect anchor **provably** untouched. 711→**794 tests**. Report: `docs/eval/task36_findings_report.md`. ⚠ **Open — disposition now CHECKED (2026-08-17): 36 did NOT fold it in.** Its report is explicit that *"'Every N weeks' intervals are still not representable"* — the handoff arrived via orientation §9 but brief §2's scope never listed it, so it was correctly left alone. The report adds that `recurrence_pattern` is free-form JSON, so an `interval` field needs **no migration**; what it needs is a UI control, an anchor date to count from, and one new `period.ts` function. **Unowned and unnumbered — see Open rulings owed.** | 2, 3, 25 | **Opus** |
| 37 | ✅ **Done (headless) 2026-08-17** — light `P` outstanding → 32 | Extraction-grammar separator-token hole | **Live bug in shipped extraction, found by the model spike.** `task_extraction.v1.gbnf`'s `title` rule accepts a bare `","` as a complete, **schema-valid, validator-passing** value — a task titled `,`. `description` shares it; `newTag`/`tool`/`date` need audit. Bonsai dodges it only by not ranking `","` first (luck). **Fix (spike-confirmed): first char `[a-zA-Z0-9]`** — min-length does NOT work. Add a validator defense-in-depth layer too. **Landed `0ad7184` + `8a4a34f` on `main`.** Report: `docs/eval/task37_findings_report.md`. **The audit found 15 rules across 4 grammars, not the 2 the brief confirmed** — extraction (`title`, `description`, `date`, `newTag`, `tool`), breakdown (`title`), resolution (`newTag`, `changesNotes`, `reason120`, `date`, `condition120`, `title`), summary (`keyPoint`, `disposition`, `energyNote`). Fix is `firstChar ::= [a-zA-Z0-9]` opening every free-text rule, **bounds unchanged** (`firstChar jchar{0,n-1}` spans the same 1..n). `tagKnown` is legitimately exempt (D7 closed alternation, no separator to emit) and the exemption is asserted in the test rather than silently skipped. **Validator layer adopted and deliberately *weaker*** — rejects a title with no alphanumeric *anywhere* rather than first-position, because the grammar constrains generation cheaply while validator rejection costs a D10 retry, and `"$50 to the landlord"` must survive. It also covers the **prompt-JSON fallback path where no grammar is in play at all**. Verified independently by the coordinator: **811 tests / 69 suites** green, `tsc` clean, `eslint` unchanged from baseline. ⚠ **Believed, not confirmed — no real llama.cpp parse has run.** The startup-guard compile and the no-regression-on-Bonsai check are device-only → **task 32**. *(If the compile did fail, the guard contains it by falling back to prompt-JSON + validation — where the new validator layer is the live defense.)* | 5 | **Opus/Sonnet** |
| 38 | ⬜ Undone | Train the Gemma 4 E2B LoRA | **Model-migration chain, step 2.** The six-model spike (`model_base_spike_final_findings.md`) recommends Gemma 4 E2B — trainable (Bonsai's ternary is frozen), 2× faster/capture, best distress. Train a runtime-loadable LoRA (`applyLoraAdapters`) to close its `recurrence`/`due_resolved` gap. **Try the free prompt-only null-convention fix first.** Trains on 31's **train** split only; tunes against a `calibration` slice, never `heldout`. Brief: `docs/briefs/gemma_lora_training_task_38.md`. | **31** | **Opus** (pipeline) + **Jason** (trains) |
| *39* | — *reserved* | *(optional corpus → eval-harness step)* | Held free in case 31→38 wants an explicit conversion/harness task between them. Currently unused. | — | — |
| 40 | ⬜ Undone | Three-way model bake-off (migration gate) | 🔴 **PINNED 2026-08-17 — must report junk-tag rate per model.** It costs nothing extra (the fixtures already exercise it) and it is a **differentiator the 16-fixture resolution problem does not wash out**, because it is a *rate across all items* rather than a per-field correctness call subject to the ±12-pt band. It measures instruction-following on guidance that is already explicit and already tuned — precisely the class of failure a LoRA fixes and a prompt does not, which is the entire case for Gemma. See the standing note. **Chain step 3 — gates the decision, doesn't make it.** LoRA-Gemma-E2B vs **Bonsai-8B-Q1_0** (downloaded, never tested) vs T-Bonsai-4B, on task 31's **held-out** split, the spike's exact Gate-2 protocol (same corpus, same day, same build, cooled between runs), pulled-DB verified. **37's grammar fix goes in first.** Must state the resolution it's running at, from 31's findings report. Framing: trainable-but-behind vs frozen-but-ahead — **Jason's call**, recorded to orientation §1. Brief: `docs/briefs/model_bakeoff_task_40.md`. | 38, 31, **P** | **Jason + Opus** |
| **41** | 🟡 **Phase 1 + Phase 2 DONE 2026-08-18 · device run outstanding — GATES 31** | **Lossless local event capture** | ✅ **Phase 2 landed** (`0393654`, `470a8f5`, `46e25dd`, `96e2279`, `96155ed`, `f419cfa`). `src/capture/` — twelve streams, one `record()` entry point — plus `src/specs/NativeCaptureLog.ts`, `CaptureLogModule.kt`, `CaptureLogPackage.kt` (registered in `MainApplication.kt`). Built in the ruled order: **acceptance test first**, then module + writer, then call sites, then the widened error, then `scripts/pull-capture.js`. **Coordinator-verified: 910 real tests / 80 suites** green (raw 1704/148), `tsc` clean, `eslint` **0 errors / 56 warnings — identical to baseline** *(`sha256.ts` carries a justified file-level `no-bitwise` disable; without it the count would be 110 and the baseline comparison meaningless)*. 🔴 **The force-kill test is a demonstrated regression detector, not an assumed one** — substituting a 32-record buffered writer made it fail on exactly the intended assertion (23 acked events absent from disk), and it was written and committed *before* the implementation. ⚠ It exercises the **Node** writer over a transpiled copy of the real `record.ts`, **not the Kotlin one** — stated in the file header and the report. 🔴 **Removability was VERIFIED, not asserted** — design §11 steps 1–3 were actually performed on the `crisis` stream with `tsc`, naming exactly the two production call sites (both in `chatController.ts`), then restored. That is the property task 42 depends on. **`sessionController.ts` untouched — zero lines**; the correlation frame is set from `episodeService.ts` instead, which also picks up `recoverOpenEpisode`, so **task 44 should rebase cleanly**. Five `src/app/` files touched, enumerated in report §7. **No migration, no schema change.** 🔴 **Two deviations — 2.1 recorded/attributed, 2.2 needs a ruling. Three measured numbers still owed and only the device can give them.** Report: `docs/eval/task41_findings_report.md` | *(Phase 1 record follows.)* ✅ **Phase 1 complete 2026-08-17.** Format design (`docs/design/capture_format_task41.md`) + **`capture_format_task41_amendment_rulings.md`, which closes every open question** — all six of §12 plus two follow-ups. **Rulings:** storage = **a new app-owned TurboModule with synchronous append** (not `react-native-fs` — a Promise-based `appendFile` loses exactly the events before a native crash); **twelve streams, one egress class each**; crisis stream logs **every turn the gate ran on, hit or clear** (option (c), reversing the design's own (b) recommendation — a near-miss watch list is *also* a phrase list and structurally cannot catch the indirect expressions `crisis.ts` documents itself as missing); actor vocabulary **`user`/`coach`/`system`/`planner`** with `planner` as a sentinel whose expected count is zero (a non-zero count is direct evidence of the unenforced `PlanAdjustment` contract being violated); **in-app thermal/battery sampling built here** (Jason's explicit instruction — 🔴 **a recorded deviation from orientation §8's pin to task 19**, sampling only, *no thermal policy*); **keep everything** with a single black-swan ceiling warning and no second trigger; **`fsync` per event for alpha**, reverting to boundary-only at beta. ⚠ **Four builder calls remain provisional** (amendment §8) — the **global `v`** is the one that is not cheaply reversible and needs deciding before Phase 2 writes records. **Phase 2 order:** force-kill test first, then TurboModule + `record()` + writer, call sites, widened `LlmOutputValidationError`, `scripts/pull-capture.js`, then Jason's device run. **The findings report already owes three items:** the thermal deviation, the halved test count, and app-open time with capture on vs off. **Nothing gets discarded.** Conversation logs (verbatim, both directions, incl. clarifying Q&A), task changes (user- vs model-made, field-level, with surface), task performance (episodes, outcomes, +5/hyperfocus, parks, crash recoveries). **Ruled 2026-08-07: log everything in alpha, no exclusions or redaction — and build every stream to be removable *independently*.** That's a settled decision (orientation §5), not a preference: streams separately scoped, each under a directory it owns, one module/one entry point, on-disk layout a written contract, no capture logic diffusing into instrumented code. 42 deletes **one** stream while keeping the rest, and must prove it. The crisis **detector** is untouched — only its logging is removable. **Additions:** 🔴 raw model I/O (composed prompt, **raw completion before parsing**, grammar, D10 rung, latency, tok/s), 🔴 validation failures **with payload** (`LlmOutputValidationError` currently drops it — that's why 37 cost a whole spike), scoring/planning snapshots incl. both reject sets, coaching lifecycle, crisis-gate firings + near-misses (task 21 has *zero* real data today), thermal/device conditions, app lifecycle. **Design:** out-of-band append-only JSONL on app-private storage — never in the product DB, must survive its corruption; synchronous writes (buffering loses exactly the events around a crash); capture failure never breaks the app but never silent either; correlation IDs + `"v":1` on every record; redaction seams present but off. **Acceptance test that matters: force-kill mid-episode loses nothing.** Brief: `docs/briefs/event_capture_task_41.md` | 24, 13, **P** | **Opus** + **Jason** (device) |
| **42** | 🔴 Undone | **Crisis-stream teardown + consent for the surviving capture** | 🔴 **NEW FROM 41 PHASE 2 (2026-08-18) — the one field a privacy reviewer should look at twice.** **`planning.capabilityRejects[].reason` carries the context and tool NAMES the user chose.** They are a vocabulary rather than prose and the stream stays structured, so it survives the open-beta rung under 43 — which is exactly why it needs a decision rather than a default. ✅ **Removability is now a verified property, not a hope:** 41 actually performed design §11 steps 1–3 on the `crisis` stream against `tsc` and it named **exactly the two production call sites**, both in `chatController.ts`. Job A's teardown has a demonstrated method. | 🔴 **HARD CLOSED-BETA GATE. Two jobs.** **(A) Teardown:** the **crisis-gate stream only** is removed before beta — for a tester it's a record of their worst moments in a file, and no consent flow makes that worth it. Jason's own alpha crisis log is **retained** (it's task 21's only real evidence). The detector itself is untouched; only its logging goes. **(B) Governance:** the other streams **ship and record beta testers**, so consent (first-run, in 23's language, stating it never leaves the device), controls with real categories underneath, **retention** (⚠ `data_retention` — created in migration 001 with three policies, *never written to or acted on*; implement or retire it), and export/delete that provably reach **both** stores. **Defaults are the real policy** — recommend performance + model I/O on, raw transcripts off unless enabled, crisis stream nonexistent. **Verified not asserted:** grep the beta bundle for crisis symbols; fire the gate and confirm it still refers but logs nothing; toggle each control off and confirm the stream is empty; export-then-delete reaches both stores. **Private archive:** retained logs, corpus text and the crisis log live **outside the repo** — the repo travels, and retained-forever + committed-to-git means a verbatim record of Jason's life ships with the source. **Also builds the egress pipeline:** ruled 2026-08-07, **nothing leaves a tester's device un-anonymized** — anonymization runs *at the source, before export*, as a pipeline distinct from capture (consuming 41's redaction seams). Closed beta is the **last rung where free-text egress is defensible**, and only because the volume permits per-item review. **Also owes:** the `conversation_summary` comment correction and constraint #7's clause (local capture ≠ telemetry; the distinction is *transmission*). **Review with task 21.** Brief: `docs/briefs/privacy_consent_task_42.md` | 41, 21 | **Opus** implements |
| **43** | ⬜ Undone | **Capture ladder — open-beta and GA pruning** | 🔴 **NEW FROM 41 PHASE 2 (2026-08-18):** the open-beta rung keeps *structured* streams on the reasoning that they anonymize essentially completely — **but `planning.capabilityRejects[].reason` carries user-chosen context/tool names**, which is closer to free text than the rung's justification assumes. **Decide it explicitly at this rung rather than letting "structured" carry it through by category.** Reviewed with 42. | 🔴 **Gates OPEN BETA, then GA** (42 owns the closed-beta rung). **Ruled 2026-08-07:** *logging will be pruned/dropped as we move from closed beta to open beta to general release.* **The mechanism, not a mood:** the closed-beta safeguard is **human review of a small volume from known people — and that does not scale, and fails silently.** The stream must narrow exactly where the protection stops being real. **Open beta: free-text conversation capture is dropped**; structured streams (performance, outcomes, timings, model-I/O *metadata*, thermal, errors) survive, because they carry 17/19/20's signal with almost none of the exposure. **GA: minimal or nothing.** Prune by **dropping, not disabling.** Anonymization is best-effort on free text and **"anonymized" never means "safe to publish"** — the real risk is **re-identification by combination**, since a task list is close to a fingerprint even fully scrubbed. That asymmetry (structured anonymizes completely, free text doesn't) is what makes the ladder principled rather than an arbitrary line. ⚠ **See the corpus-window consequence in the frontier section.** Brief: `docs/briefs/capture_ladder_task_43.md` | 42 | **Opus** implements; **Jason** rules each rung |
| **44** | ⬜ Undone | **Personal-use QoL pass** | ✅ **41 Phase 2 left this clean:** `sessionController.ts` was touched **zero lines**, so 44's `startSession` edit rebases without conflict. **`origin` is deliberately absent from every `episode` capture record until 44 lands** — capture has no truthful value and will not guess one. **44's wiring is a single argument:** `captureContext.setSession(id, origin)` in `episodeService.startSessionRuntime`. | 🔴 **FIFTH ITEM ADDED 2026-08-17 — the junk-tag data pass.** `tagKnown` builds its alternation from tags the app already holds, so **any junk tag already persisted keeps being offered back to the model as legitimate, forever** — task 37's grammar fix stops new leading-separator junk entering but cannot clean what is stored. Audit the alpha DB's `context_tags`. ⚠ **Carries a product question for Jason, do not decide it:** delete the junk tags, merge each into a real tag, or leave them to age out? Deleting rewrites history the corpus may want; merging is a judgment per tag; leaving them keeps feeding the model noise. **Bring the tradeoff back.** *(Note this is the `":mixing"`/`":episode"` class only — the `"work_on_it_until_did"` class is a tracked signal pinned to 20/40, not a data problem.)* Four alpha QoL items, **numbered apart from 41 deliberately** (41 is critical-path with a closing corpus window; this is product UI). **Batch onto 41's device session.** **All product questions RULED 2026-08-07** — build to them: **(a)** a task **blocked by other tasks disables both** quick-start and self-complete (dependency-blocked *and* R7-held parents; disabled with a visible reason, not hidden); **(b)** self-completed tasks are **excluded from completion-time calculations**; **(c)** quick-start runs the **full check-in** — a normal session that's one task long; **(d)** if any check-in condition would have filtered the task out (**including missing tools**) → **warning screen naming the condition, with a back-out button**; proceed allowed, but informed; **(e)** logs distinguish **quick-start vs normal** sessions. **Items:** **(1)** model warm-up on coaching-screen open — `modelHost.ts` already has `ensure()` + `phase()`, so it's *when* `ensure()` is called; makes constraint #3 safer, not weaker. **(2)** Timer dial — **deferred to the designed pass**; in this task only **fix the false code comment** (`WorkScreen.tsx` cites "per the task brief" for a bar the brief never mentions → task 45). **(3)** Quick-start, reusing the real `src/planning/` predicates for the warning so it can't drift from the filter it mirrors. **(4)** Self-complete — **reuse `completeTask()`** (already branches per constraint #7, runs 33's fold + 36's advance); `interactions` row with **explicit null runtime fields**, counts for completion/neglect, excluded from duration learning. ⚠ Candidate **first writer** for `completion_count`/`success_rate` — record the convention so **task 17** inherits it. **RULED 2026-08-07:** session origin goes in **`sessions.origin`** (`planned`\|`quickstart`) as well as the capture logs — **migration 007 / schema 2.8.0**, one writer in `sessionController.startSession`, NULL meaning *the distinction didn't exist yet*. **Why, so nobody reads it as a redundant duplicate:** capture is *deletable by design* (removable streams, consent-gated at 42, pruned by 43) and **a permanent consumer must not depend on an ephemeral store** — the general rule being *anything the app reasons about itself with belongs in the DB; capture is for diagnosis and corpus*. Quick-start also **bypasses `runSelectionBoundary`**, so it's a **confounder not a label**: its outcome says nothing about whether the ranker chose well, and pooling it credits the scorer for the user's own pick. And it **can't be backfilled** — deferring until 17 exists destroys the record for every session in the gap. **Scope: record it; how 17 consumes it is 17's call.** Brief: `docs/briefs/personal_qol_task_44.md` | 24, **P** | **Sonnet** |
| **45** | ⬜ Undone | **Deviation audit — what changed a human decision without sign-off** | **Ruled 2026-08-07:** *any deviation from human-made decisions must be flagged in a report AND explicitly surfaced during coordination review.* Standing rule now in **handoff §4**; this task cleans the backlog that predates it. **The trigger:** task 24 shipped a linear bar where task 23's approved design had a dial, and Jason was never asked. Three failures — the code comment cited a brief authorization **that doesn't exist**; the report filed it under *"Deferred… deliberately, not forgotten"* (reads as scheduling, was a decision); **and nobody surfaced it in review.** **The systemic risk:** two report section titles — *"Decisions this task had to make"* (24 has **8**) and *"Deferred deliberately"* (24 has **6**) — quietly absorb overrides, and **some are already folded into orientation §3/§5 as settled**, indistinguishable from Jason's own rulings. Known seeds: §4.1 three-vs-five end-of-block options (five was ruled); §4.2 tools optimistic at planning (reorders spec §6.2); §4.6 startup guard on first model use (constraint #3 says "at startup" — good argument, still a decision, already canon). Then the same pass over **13, 11, 33, 36, 12, 7**. **Deliverable:** one table — what changed / what it deviates from / where recorded (or "nowhere") / whether it entered the canon / the argument for it, fairly stated / ratify-revert-revisit. **Jason rules each; orientation §5 then distinguishes ruled-by-Jason from decided-by-builder, which it currently cannot.** Audit only — reverts become their own tasks. Brief: `docs/briefs/deviation_audit_task_45.md` | — | **Opus** produces; **Jason** rules |
| **46** | ⬜ Undone | **"Every N weeks" recurrence intervals** | **Numbered 2026-08-17** — it was an unowned handoff with no number since task 24. The recurrence editor's `scheduled` union carries weekdays only, so an interval control had nowhere to write and was dropped. **Task 36 explicitly did NOT fold it in** — its report says *"'Every N weeks' intervals are still not representable"* — correctly, since brief §2's scope never listed it. **Needs no migration:** `recurrence_pattern` is free-form JSON, so an `interval` field is additive. What it needs is a UI control, **an anchor date to count intervals from**, and one new `period.ts` function. Must respect constraint #5 (the R8 accrual gate is a start condition, not a cap) and constraint #7 (`null`/one-off ≠ `unscheduled`). Small, headless, file-disjoint from the capture chain. | 36 | **Sonnet** |
| **47** | ⬜ Undone | **Schema snapshot catch-up** | **Numbered 2026-08-17** — unowned since task 34. `docs/reference/ADHD_Task_Management_App_Database_Schema_v2.5.sql` is **two migrations stale** (005 `session_runtime` + 006 `recurrence_period`), and **task 44's migration 007 will make it three**. ⚠ **Sequence AFTER 44** so this is one catch-up rather than two. Regenerate the snapshot from the applied migrations, not by hand-editing the old one. ⚠ **Do not "fix" the spec-version/schema-version mismatch** — spec is v2.4, schema will be 2.8.0, and they track different things (product rulings vs applied DDL); they are *supposed* to drift. The snapshot lag is the real gap. Until this lands, **read the migration files for actual DDL, never the snapshot.** | 44 | **Sonnet/Haiku** |
| **48** | ✅ **Done 2026-08-18** | **Grammar primitive hardening** | ✅ **Landed `9c65e80`.** `boundedStringRule` now emits the hardened shape. **`min ≥ 1`** → `firstChar jchar{min-1,max-1}` (bounds preserved — spans exactly min..max). **`min = 0`** → an explicit alternation `"\"\"" | "\"" firstChar jchar{0,max-1} "\""`, because an empty string cannot be routed through `firstChar`; collapses to just the empty-string literal when `max = 0` too. New `FIRST_CHAR_RULE_NAME`/`FIRST_CHAR_RULE` exports mirror the existing `JCHAR_RULE` convention — the rule is **referenced by name** and the caller defines it once, rather than emitted inline per field (which would duplicate the definition and risk a parse error). Re-exported from `src/llm/index.ts`. **The one stale exact-match test was replaced by four pinned cases** plus a block pinning the new exports — exact string equality throughout, a tightening not a weakening. `boundedStringRule` has no callers outside `primitives.ts`/`index.ts`/their tests, so `buildGrammar.test.ts` and `ruleNaming.test.ts` needed no change. **Coordinator-verified: 872 real tests / 75 suites** green, `tsc` clean, `eslint` unchanged. **Deviations: none, written out explicitly.** ⚠ **Believed, not confirmed** — the `min = 0` alternation is a novel shape never parsed by real llama.cpp → task 32. Report: `docs/eval/task48_findings_report.md`. | **Numbered 2026-08-17** from task 37's residue. **`boundedStringRule` in `src/llm/grammar/primitives.ts` still emits the shape task 37 removed from all four checked-in grammars** — `${ruleName} ::= "\"" jchar{min,max} "\""`, with no `firstChar`. It is exported from `src/llm/index.ts`. **Not a live bug today** (all four `.gbnf` files are hand-authored and nothing generates a grammar from it at runtime) — but **a fifth grammar authored with it would silently reintroduce the separator hole**, and `freeTextFirstChar.test.ts` only guards the `.gbnf` half. ~15 minutes plus a test. Must keep constraint #2 (no underscores in rule names) and must not break `buildGrammar`'s dynamic-slot substitution. ⚠ **Staged behind task 14, before 41 Phase 2** (Jason, 2026-08-17). | 37 | **Sonnet** |
| **49** | ⬜ Undone | **Widen `prevent_circular_dependencies` to real cycles** | **Numbered 2026-08-18**, found in passing by task 14 Phase A and **pinned by a test that asserts the bug** (`consistency.test.ts`). **Migration 001's `prevent_circular_dependencies` trigger only catches cycles of length TWO** — its `WHEN` clause tests for a single row that is the direct reverse of the one being inserted. **`A→B→C→A` inserts cleanly today**, with foreign keys and triggers fully on. **Why it matters beyond tidiness:** U1's dependency-blocked pre-filter (task 25, mandatory) holds blocked tasks out of the ranked pool — so a 3+ cycle means every task in it is **permanently filtered from every session**, while uncapped neglect (constraint #5) keeps raising scores that never surface. `validateConsistency` (task 14) is currently **the only thing in the tree that can see a cycle of length ≥3**, and only when something calls it. **Needs a migration** (a CHECK/trigger change → constraint #12's full-rebuild discipline + sweep prior migration suites). ⚠ **Take the next free number after 44's 007** — do not contend for 007. | 2, 25 | **Opus** |
| **50** | 🔴 **Undone — GATES 31** | **Pin the `energy` field definition** | **Numbered 2026-08-18 by Jason's instruction**, and it **gates task 31**. `energy` scored **6–14/16 on EVERY model measured, including Bonsai** — when all of them fail a field at that rate the cause is an ambiguous field *definition*, not weak models. A LoRA would paper over it; a clearer definition fixes it for free, with no migration. 🔴 **Why it must precede 31 rather than run beside it: writing 100+ golds against an ambiguous definition bakes the ambiguity in permanently**, and the collection window closes at open beta (43) — there is no second pass. **Deliverable: one paragraph a human and a 4B read the same way**, plus a decision on whether internal energy 2 and 4 (app-assigned behavioural discounting, never user-entered) are in scope for extraction at all. Must project through `scales.ts` (constraint #6). ⚠ Note task 7 Phase B **deliberately did not tune this**: gold expects `high` for "hit the gym" but `null` for "take out the trash", and it judged chasing a subjective oracle on a non-critical field to be over-fitting — **so the gold set itself may be part of the ambiguity.** **Format: Jason and Opus hammer it out together**, not a document handed over. | — | **Jason + Opus** |
| **51** | ⬜ Undone | **Spec §8.4 amendment — the backup scheme** | **Numbered 2026-08-18.** Jason ratified task 14's **D1** (fixed working path, snapshot to a rotating slot) and **D2** (two slots, not one) — **so the code is settled and `docs/reference`'s spec §8.4 is now wrong.** It still reads *"copy DB at session start; work on the copy; keep prior as backup"*, describing a scheme that **cannot be built on this tree** (no JS file write ⇒ no pointer to a moving working path) and a backup count that is no longer true. 🔴 **Not cosmetic:** every worst failure in this project came from a document and the tree disagreeing. A future session reads §8.4, opens `src/services/backup/`, and either files working code as a bug or "fixes" it toward something unbuildable — the same shape as `WorkScreen.tsx` citing a brief that said nothing, pointed the other way. **Carry the mechanism, not just the new wording**, so the amendment cannot be re-reverted by someone re-reading the old sentence. **Also folds in `session_ended_early`** as a §7.2 addition (ruled 2026-08-18). ⚠ **Pair with task 47** — one pass over `docs/reference/` after 44's migration 007. | 14, 44 | **Sonnet** |

---

## Branch state (new section — this is what went wrong)

| Branch | Carried | Status |
|---|---|---|
| **`main`** | everything below, merged | ✅ **`fc59b83` — pushed to GitHub 2026-08-17.** `origin/main` is level. Test-verified green (794 tests). |
| `claude/interesting-shirley-e10fa1` | the `score.ts` NUL-byte fix | merged (fast-forward). ⚠ Still checked out as a **stale worktree** — see the standing note. |
| `task-36-recurrence-period-engine` | 35 + 36 + a docs WIP commit | merged. Local ref still ahead 1 (a WIP commit); nothing on it is needed. |
| `opus/batch-a-headless` | the 37-commit six-model spike | merged |
| `coordinator/sessions-origin` | `sessions.origin` ruling, task 8 retirement, task 14 rewrite, `score.ts` correction, handoff refresh (6 commits) | ✅ **merged 2026-08-17** (`67c0618`, not a fast-forward) and branch deleted. |
| `coordinator/tasks-41-42` | at `a5d4107`, an ancestor of `main` | fully contained in `main`; deletable. |

**Housekeeping — done 2026-08-17:** `_probe` / `_probe2` deleted; `coordinator/sessions-origin` deleted after merge; the stale `.git/*.lock` files are gone; **`.gitattributes` added** (`* text=auto eol=lf`, commit `fc59b83`).

⚠ **`rm -rf .git/objects/incoming-*` was NOT done, and the instruction to do it rested on a false premise.** Those 17 dirs were described as harmless leftovers — but **15 of them contain loose objects** (1.2 MB total), not nothing. `git fsck` is clean and does not complain about them. Deleting object directories by hand is how you corrupt a repo; **`git gc --prune=now` is the correct instrument** and will sweep them safely. Left in place deliberately.

**Line endings — resolved.** The old note here said the working tree's ~220-file diff reduced to two real files. That was **stale and would have been dangerous to act on**: by 2026-08-17 the tree held **seven** files of real, uncommitted content (205 insertions) — the entire `coordinator/sessions-origin` doc set, applied to the working tree but never committed. `git checkout -- .` at that point would have discarded it. It was preserved by hash-comparing every file against the branch (all seven matched), stashing, and merging. **Adding `.gitattributes` then staged zero renormalization changes — the index was already LF throughout**, so the churn was purely a working-tree artifact.

---

## Standing notes (not tasks)

- 🔴 **`src/scoring/score.ts` was a *binary* file to git from its first commit (`8903e74`) until 2026-08-01.** `contextGroupKey`'s no-tags sentinel was a raw `0x00` byte, inside the first 8 KB git samples for binary detection. **Every diff of the scoring composition ever reviewed showed `Bin 8860 → 9177 bytes` instead of lines** — the most-reviewed file in the project never once appeared in a readable diff. Fixed in `db16645`; write-up in `docs/briefs/nul_byte_score_ts.md`. ⚠ **Corrected 2026-08-07 — this overstated the risk.** `db16645` **re-encoded, it did not change**: `contextGroupKey` still returns `'\x00flexible'`, now a source escape rather than a raw byte, so the value is **byte-identical**. It's a `Map` key — never persisted, never displayed — so there was **no behavioural defect and no false positives or negatives, ever**. File reads always worked, so **task 10's composition review stands**. Only **diffs** were unreadable, so the unreviewed surface is *what changed after task 10*: **six commits** (9, 10-R1, 10-R3, 25-U1, 11, 36), **all readable today** via `git show --text`. The ruled constants are pinned by tests (`factors.test.ts` asserts 0.31/0.23/0.23/0.23; `score.test.ts` covers R6's k=2; `noveltyEntropy.test.ts` is the permanent novelty regression). Verification folded into **task 45**.
- 🔴 **`npx jest` reports 1588 tests, and the real number is 794.** A stale git worktree lives at `.claude/worktrees/interesting-shirley-e10fa1` — a full second checkout of the tree, detached at `d3ead86`, **inside the project directory**. Jest collects its 68 suites alongside `main`'s 68, exactly doubling every count. `d3ead86` is already an ancestor of `main`, so the duplicate is pure redundancy carrying no unique work. **Consequences worth knowing:** the doubled run is a statement about *two different commits*, so a green there does not mean what it looks like; and a future regression on `main` could be masked or duplicated in a way that reads as an unrelated failure. ✅ **RULED by Jason 2026-08-17: leave it in place for now**, flag it in the findings report, and let a separate cleanup task remove it later. *(This overrules the coordinator's recommendation to apply `testPathIgnorePatterns` and/or `git worktree remove` — recorded so the reversal is visible rather than silent.)* **The standing consequence: any test count quoted from this tree needs halving until the worktree is removed.**

**Verified duplicate, not assumed** (`capture_format_task41_amendment_rulings.md` §6): diffing `src/`, `__tests__/`, `package.json` and `jest.config.js` between `d3ead86` and `main` yields **exactly one file** — `src/dev/ModelBaseSpikeScreen.tsx`, present on `main`, absent in the worktree, and it has no test. Suite counts 68 and 68; test content identical. Cost is run time only (~7 s → ~15 s). Nothing is broken.
- ⚠ **There are TWO grammar-text drift guards, not one** — a correction task 37 owed to its own brief. `src/llm/grammar/grammarText.ts` is the one every document names; **`src/dev/extractionGrammarText.ts` holds a second byte-identical copy of the extraction grammar with its own drift test.** A session that regenerates only the named one will fail the suite on a file no brief mentions, and the failure reads like unrelated breakage. Regenerate both.
- ⚠ **→ NOW TASK 48.** **`boundedStringRule` in `src/llm/grammar/primitives.ts` still emits the vulnerable shape** that task 37 removed from all four checked-in grammars — `${ruleName} ::= "\"" jchar{min,max} "\""`, no `firstChar`. It is exported from `src/llm/index.ts`. **Not a live bug today** (all four `.gbnf` files are hand-authored; nothing generates a grammar from it at runtime), but **a fifth grammar authored with it would silently reintroduce the separator hole.** `freeTextFirstChar.test.ts` would catch the resulting `.gbnf`, which is the important half of the defense. Unowned; worth folding into whoever next touches `src/llm/grammar/`.
- **Junk tags split into two root causes** (task 37 §5). `":mixing"` / `":episode"` were the separator hole in milder form and are **closed**. **`"work_on_it_until_did"` starts with `w` — a phrasing failure, not a separator artifact, and is NOT fixed.** Separately: **junk tags already persisted in the DB keep being re-offered forever via `tagKnown`**, since that rule builds a closed alternation from tags the app already holds. The grammar fix stops *new* leading-separator junk entering; it cannot clean what is already stored. ✅ **Data pass FOLDED INTO TASK 44** (Jason, 2026-08-17) — it carries a product question 44 must put to Jason: delete the junk tags, merge them into a real tag, or leave them to age out.
- 🔴 **`"work_on_it_until_did"`-class junk is a TRACKED SIGNAL, deliberately not a task** (Jason, 2026-08-17). **Pinned to tasks 20 and 40 — see those rows.** **The mechanism, so nobody re-files it as a bug:** the field guide is already explicit (`fieldGuides.ts:49` — *"ONE plain lowercase phrase — never punctuation, fragments, or non-English text"*), and task 7 Phase B already tuned it across five on-device iterations (junk tags to ~0, avg tags 3.0 → 1.5). The residual ~2–3/run is the model **disobeying a clear instruction**, not resolving an ambiguous one — which makes it a measure of **model capability, not prompt quality**. **A grammar cannot fix it:** grammars constrain *shape*, and no character class separates `"work_on_it_until_did"` from `"deep work"` — forbidding `_` just moves the failure to `"work on it until did"`. That is the D3.2 split the prompts were built on. ⚠ **Do not spend more prompt budget on it** — the extraction guide is *already over* its ~250-token budget (strategy §5.2), and tuning it further over-fits the incumbent model on exactly the axis the migration decision is trying to measure.
- **Every migration must sweep the *prior* migrations' test suites.** `runMigrations` walks forward, so 00N−1's "latest version" and "full object list" assertions silently become assertions about 00N — surfacing as a failure in an unrelated file that reads like "I broke someone else's test." Confirmed live through migrations 002–006.
- **Next migration is 007 / schema 2.8.0 — and it is now claimed by task 44** (`sessions.origin`, ruled 2026-08-07). Task 36 took **006 / 2.7.0**. Whoever else lands a migration first renumbers and re-sweeps. *(The old note here said "next is 006, and if 36 adds one it is 006" — 36 did, and it's landed.)*
- **Spec version ≠ schema version.** Spec is **v2.4** (task 35); on-device `schema_metadata` is **2.7.0** (migration 006). These track different things — product rulings vs applied DDL — and are *supposed* to drift. Don't "fix" that mismatch. ⚠ **But the `v2.5.sql` reference snapshot is genuinely two migrations stale** (005 + 006) — that's a real gap, unowned, worth numbering when someone next touches `docs/reference/`. For actual DDL read the migration files, not the snapshot.
- **`docs/build_allocation.md` is retired** (2026-07-20) — tombstone pointing at orientation §10. Routing logic lives in the handoff §1; per-task allocation lives in this table's Model column. Don't resurrect it. *(Its tombstone still names Fable as the top routing tier; Fable access has ended and Opus 5 inherits that slot.)*
- **`PlanAdjustment` is a stated contract, not an enforced one.** Nothing validates a hostile adjuster; the hook must not resurrect filtered tasks. Task 19 owns the wrapper.
- **The expiry alarm cannot be a JS timer** (device-confirmed; `setTimeout` fires 38–45 s late from doze). Discharged by task 24's `AlarmManager` TurboModule. Never add a "JS backup timer" on this path.
- **`energy` is an ambiguous field *definition*, not a weak-model problem** — it scored 6–14/16 on **every** model including Bonsai. Cheapest available win, needs no migration. **Task 31 must decide explicitly** whether to pin the definition or mark `energy` non-critical for the corpus; writing 100 golds against an ambiguous definition bakes the ambiguity in permanently.

## Open rulings owed

**Ruled 2026-07-20:** extend splits into +5 / hyperfocus; +5 is **never capped**; repeated +5 queues coaching at **3 presses or 50% over, whichever first**; guardrail **B**, hyperfocus only; recurrence engine **split into task 36**; task 35 **commissioned**.

Still open:

| Ruling | Owner | Gates |
|---|---|---|
| ~~**Corpus size for task 31**~~ → ✅ **RULED 2026-08-18: 100+ extraction items.** Jason takes the generous end. **Consequence task 40 must carry: ~50+ held out ⇒ ≈±11 pts** — finally better than the seed set's ±20, and tight enough that Bonsai's 14/16-vs-12/16 lead can actually be tested instead of vanishing into the error bar. **Task 40 must still state the resolution it ran at**, computed from 31's final held-out n. | ~~Jason~~ | — |
| ~~**The `energy` field definition**~~ → ✅ **NUMBERED task 50, 2026-08-18**, gating 31. Jason + Opus hammer it out together. | ~~Jason~~ | — |
| `which:"next"` weekday semantics (22) | Opus/Sonnet to propose, Jason to rule | Every resolved date |
| **Multi-day `scheduled` neglect gap** (25 §3.1) — ✅ **reviewed 2026-08-18; no action, left as a known knob.** `scheduled` carries neither `period` nor `quota`, only `scheduledDays`, so task 25 derived `period = 7` and `quota = max(1, scheduledDays.length)`. Single-day weekly → `7/(1+1)` = **3.5 d**, matching the ruled example exactly; Mon+Thu → `7/3` ≈ **2.33 d**. Accepted because it is more faithful to *"half the distance between occurrences"* than hardcoding quota-1, and it errs in the **safe direction** — a shorter gap surfaces a task sooner, and a fail-safe that surfaces early never hides anything. **Revisit only if a real multi-day schedule nags too early.** | Jason (only if it bites) | Nothing |
| ~~`sessions.model_tier`~~ → ✅ **RULED 2026-08-18: leave it NULL.** Ignoring it breaks nothing — the column is nullable, unwritten and unread. **Two reasons not to fill it:** the CHECK vocabulary `('8B','4B','1.7B')` is about to be **wrong** if task 40 picks Gemma 4 E2B (the real value would be *rejected*), and writing `'4B'` unconditionally is a **fiction** for every timer-only session that never loads a model — a fiction task 17 would then learn from. **Task 40's outcome defines what the column means**, at which point it is a migration that also fixes the CHECK. | ~~Jason~~ | — |
| ~~`session_ended_early`~~ → ✅ **RULED 2026-08-18: YES — backing out of a session with time left should coach.** It is already a first-class `coaching_queue` trigger type but **not** one of spec §7.2's five rows, which is why task 24 declined to invent a sixth conversation. **Now a §7.2 addition, folded into task 51**; the conversation content and trigger conditions still need authoring. | Jason ruled; **needs authoring** | Spec §7.2 + one coaching conversation |
| ~~Merged-branch green check~~ → ✅ **DISCHARGED 2026-08-17.** 794 tests green, `tsc` clean, `eslint` 0 errors, on `main` at `fc59b83`. | ~~Jason~~ | — |
| ~~**The stale worktree**~~ → ✅ **RULED 2026-08-17: leave it, flag it in the report, separate cleanup task later.** Halve any test count quoted from this tree until then. | ~~Jason~~ | — |
| ~~**Task 41 §12** — six open questions on the capture format~~ → ✅ **ALL RULED 2026-08-17**, plus two follow-ups (`system` actor, single ceiling warning). `docs/design/capture_format_task41_amendment_rulings.md`. Nothing in the format design waits on a decision. | ~~Jason~~ | — |
| ~~**Task 37 deviation** — `summary.v1.gbnf` edited outside stated scope~~ → ✅ **RATIFIED by Jason, 2026-08-18.** The three summary rules keep the `firstChar` fix. | ~~Jason~~ | — |
| ~~**Task 14 D1** — fixed working path vs spec §8.4's "work on the copy"~~ → ✅ **RATIFIED by Jason, 2026-08-18.** Settled; see orientation §5. | ~~Jason~~ | — |
| ~~**Task 14 D2** — two rotating backup slots vs spec §8.4's one~~ → ✅ **RATIFIED by Jason, 2026-08-18.** Settled; see orientation §5. | ~~Jason~~ | — |
| 🔴 **Task 14 — free-disk-space mechanism (build-cost call).** Nothing in this tree reports free space. **Shipped: attempt-and-catch** (`VACUUM INTO` → catch `SQLITE_FULL`/`ENOSPC` → `NoSpaceError`), which correctly implements the *blocking* rule but gives no advance warning and no number to show the user. **Option 3 is a ~30-line Kotlin `StatFs` TurboModule** — buys "free 40 MB" warnings before the wall; costs a **device build cycle**, which is why the builder recommends **bundling it with task 41's `NativeCaptureLog` rebuild** rather than spending a build on it alone. ⚠ **`setReservedBytes` should NOT be used — a correction to brief §2's preference order:** `SQLITE_FCNTL_RESERVE_BYTES` reserves bytes *inside a database file's pages*; it does not measure free space on the volume and does nothing for the backup, which is a different file. **It would read like a guard and guard something else.** *(Coordinator concurs.)* **Recommend: ship option 1 now, bundle option 3 into 41's rebuild.** | **Jason** | Advance-warning UX, not correctness |
| ~~**Task 41 deviation 2.2** — capture root computed natively~~ → ✅ **RATIFIED by Jason, 2026-08-18.** The boot-time cross-check stands: design §6 rule 6 becomes a *check* rather than an assumption. | ~~Jason~~ | — |
| ~~**`coaching` stream's missing writer**~~ → ✅ **RULED 2026-08-18 — a nomenclature ruling, and it is about avoiding collision, not about renaming anything.** A coaching conversation left without a disposition is **"closed without resolution"**. **The vocabulary map, stated positively so nobody re-derives it:** `sessions.status = 'abandoned'` means *this session did not close cleanly* (crash-truthful, **constraint #14** — a session is *born* `'abandoned'` so a crash leaves the true value on disk); `interactions.completion_status = 'abandoned'` means *this episode did not complete*; **"abandoned" at the TASK level means a task was dropped**, which per **constraint #11** happens **only by explicit user disposition — the app never abandons a task by inference**. A coaching conversation is none of those, so it gets its own word. ⚠ **No schema value is renamed and no migration is implied** — the existing session- and episode-level uses are legitimate and stay exactly as they are. | ~~Jason~~ | — |
| **THREE** provisional builder calls in task 41 (amendment §8): verbatim `messages`, grammar as id+sha8, ambient correlation context. Explained in review, **not ruled**. They stand as the design unless overruled and **must not enter orientation §5 as settled.** All three are **cleanly reversible after the device measurement**, so they can wait for real numbers. *(The fourth, global `v`, was **ratified 2026-08-17** — see orientation §5.)* | **Jason** | Nothing blocking; revisit on 41's findings report |
| **`fsync` revert for beta** (amendment §7) — per-event in alpha, reverting to boundary-only at closed beta. Pinned to **task 42**; pull forward if app-open measurement is bad. | **Jason** (on 41's measurement) | Capture write cost |
| ~~**"Every N weeks" recurrence intervals**~~ → ✅ **NUMBERED task 46, 2026-08-17.** | ~~Jason~~ | — |

---

## Device (`P`) batches

**21 of 45 tasks carry a `P`** (task 8 retired). *(The old count of "16 of 37" was stale twice over.)* Because you run the device sessions yourself, these are the scheduling constraint — group them rather than paying setup cost per task.

- **Done and confirmed (9):** 0, 1, 2, Q1, 6, 7, 12, 13, 24
- **Open, beta (5):** 21 (crisis coverage), 30 (envelope), 20 (real eval numbers), 22 (if solved in the guides), 32 (residue sweep)
- **Open, strategic (2):** **41** (event capture — gates the whole chain), 40 (the bake-off)
- **Open, QoL (1):** **44** — batch it onto 41's device session rather than paying setup twice
- **Open, later (4):** 14, 15, 19, 29

**No `P`, so they can proceed any time without you (23):** 3, 4, 5, 9, 10, 11, 17, 18, 23, 25, 26, 27, 28, 31, 33, 34, 35, 36, 37, 38, 42, 43, 45.

**41 is the exception that reshapes the schedule.** It's device work and it now sits in front of 31, so the model-migration chain starts with a device session rather than ending with one. Everything after it (31, 37, 38) is still headless until the bake-off.

---

## Counts

**Total: 51 tasks — 1 retired (8)** (0–51, no 16, plus Q1, minus reserved-and-unused 39). **46, 47 and 48 added 2026-08-17**, numbering unowned residue that had accumulated in the standing notes — the handoff's rule is that dependencies which aren't tasks rot.

**✅ Done (27):** 0, 1, 2, 3, 4, 5, Q1, 6, 7, 9, 10, 11, 12, 13, 18, 23, 24, 25, 26, 27, 28, 33, 34, 35, 36, **37** *(headless; light `P` → 32)*, **48**
**🟡 Partial (2):** **41** — Phase 1 done and fully ruled, Phase 2 not started · **14** — **Phase A done**, Phase B is device ("no Phase B, no done")
**⬜ / 🔴 Undone (22):** 15, 17, 19, 20, 21, 22, 29, 30, **31**, 32, **38**, **40**, **42**, **43**, **44**, **45**, **46**, **47**, **49**, **50**, **51**
*(14 is 🟡 Phase A in progress, listed above.)*

**Test baseline as of 2026-08-18: 910 tests / 80 suites green** *(811/69 → 868/75 task 14 → 872/75 task 48 → 910/80 task 41 Phase 2)* on `main` (raw `npx jest` reports **1704 / 148** — the stale worktree contributes an unchanging 794 / 68). `tsc --noEmit` clean; `eslint .` 0 errors / 56 warnings, all inline-style in `src/dev/`.

## Personal ship: MET (2026-07-29) — with one caveat now visible

**The whole loop runs on the S23 FE, DB-verified** — add-via-chat → session → timer → all five outcomes → coaching → summary, with the alarm firing 11 ms late from the background (task 24). Tasks 23 and 24 both landed; the backend (6/7/9/11/12/13 + data layer) is complete beneath them.

**The caveat, now half-discharged.** What was confirmed on device was a *build from a branch* that did not include 35, 36, or the recurrence fix. As of **2026-08-17** `main` **is test-verified green** (794 tests, `tsc` clean, `eslint` 0 errors) — so the *headless* half of the caveat is closed. **The device half is not:** `main` as it now exists has still **never been built or run on the S23 FE**. Personal ship is met for the loop that was demonstrated; the tree you would build from today has passed on the desktop and remains unproven on hardware. Per the standing habit, that makes it **believed, not confirmed** — and the next device session (41 + 44) is the natural place to close it.

## Where the frontier is — two threads, and they don't compete

🔴 **The corpus collection window is finite, and that reprices everything below.** The capture ladder (43) **drops free-text conversation capture at open beta** — permanently ending the supply of real conversational input that 31's corpus, 38's training data and 40's held-out eval all depend on. **The corpus is built during alpha and closed beta, or it is largely not built at all**, and every future LoRA iteration and every re-run of the bake-off draws on a corpus whose window has already shut. This is an argument for starting **41 now** rather than after the beta-hardening work, and for closed beta being *generous* with free-text capture while consent and review are still real protections. It is not an argument for extending the window — the mechanism in 43 doesn't bend.

**Thread 1 — the strategic one (the model-migration decision).** Now **`41 → 31 → 38 → 40`**, then Jason's call. **Task 41 is the new front of the chain** and it's the move that can happen today: once capture is on, the corpus accumulates while you do everything else, which is the only input on the board that grows without effort. 31's interview and reconstruction work runs in parallel with the capture window rather than waiting on it.

**Thread 2 — the tree, then beta hardening.** ✅ (a) the three checks on `main` — **done 2026-08-17, green**; ✅ (b) push `main` to GitHub — **done**. Next: (c) task **37**, the live grammar bug, before any alpha capture *or* the bake-off; then **14 / 15 / 17 / 21 / 32** and the designed visual pass.

✅ **37-before-41 is confirmed and in motion** — Jason is running task 37 before task 41 Phase 2 resumes. The amendment (§9) verifies the two tracks are **file-disjoint**: 37 touches `task_extraction.v1.gbnf`, its embedded copy in `grammarText.ts`, and possibly `extraction/validator.ts`; 41 Phase 2 touches `src/capture/` (new), `llm/errors.ts`, `llm/provider/ladder.ts`, `src/app/`, `src/execution/`, `src/specs/`, the Kotlin sources, `scripts/`, `jest.setup.js`. They share `src/llm/` but no file. One benign interaction: 41 widens `LlmOutputValidationError`'s constructor with an **optional** third parameter, so any tightening 37 does still compiles.

**The working order as ruled by Jason, 2026-08-17 (serial, not parallel — 14, 41.2 and 44 all want `src/app/`):** ~~**14 Phase A**~~ ✅ → ~~**48**~~ ✅ → ~~**41 Phase 2**~~ ✅ → **44** ← **RUNNING** *(+ a StatFs amendment appended to 41's native module, so one rebuild covers both)* → the **device session** (41 Phase B + 44 + 14 Phase B + the first build of merged `main`, with 37/48's grammar-compile check at the front) → **50** (energy definition, gates 31) → **31** → **47 + 51** (one `docs/reference/` pass). **46** and **49** slot in anywhere. → **44** (+ its junk-tag data pass) → the **device session** (41 Phase B + 44 + the first build of merged `main`, with 37's grammar-compile check at the front) → **47** (schema snapshot, after 44's migration 007) → **31**. Task **46** is small, headless and file-disjoint from all of it, so it can slot in anywhere.

**Sequencing note added 2026-08-17 — 37 before 41, and it is nearly free.** Task 37 is a small, headless, no-`P` grammar fix that is already spike-confirmed (first char `[a-zA-Z0-9]`; min-length does not work). Task 41 turns on capture, and **every capture taken through the unfixed grammar is corpus recorded against a known-defective extractor**. Since 41's whole purpose is to feed 31/38/40, landing 37 first costs a few hours and protects the entire corpus window; landing it after means some fraction of the irreplaceable alpha corpus carries a defect the board already knows about. The two do not compete for the same resource — 37 is headless, 41 needs the phone.

**Beta gates:** the designed/polished pass (24, consuming 23's tokens), **21** (crisis, human), **42** (privacy/consent — pinned here for the same reason as 21, and reviewed with it), **30** (device envelope, Jason), **20** (real eval numbers), **32**'s device residue. **General:** 29 only, re-scoped to a Vulkan-throughput question. **Task 8 retired** — the spike measured that thermal throttling doesn't discriminate between model sizes, so downward tier degradation buys nothing on this hardware.
